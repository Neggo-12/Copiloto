# Cronograma — Ambulancia (Emergency Corridor) y nuevas funcionalidades

Este documento traduce el roadmap maestro (`docs/decisions/04_ROADMAP_Y_ALCANCE.md`,
Etapas 0–12) en un plan de ejecución concreto, respondiendo directamente al pedido
de empezar por la función de ambulancia y organizar el resto de lo nuevo.

## Punto de partida honesto

Hoy, según `docs/architecture/MISSING_CAPABILITIES.md`, todo el dominio Emergency
(corredor dinámico, Conflict Engine, Alert Policy) está en **0% de implementación** —
solo existe como diseño conceptual. Lo mismo aplica a Location/Maps/Navigation y al
Asistente de voz: no hay ni una línea de código todavía.

Eso importa porque la función de ambulancia **no se puede construir aislada**. Según
la referencia técnica del propio proyecto (`references/mobility-emergency.md`), el
corredor de emergencia sigue este pipeline obligatorio:

```
ruta (geometría real) → segmentos de ruta → buffer dinámico → conductores candidatos
→ detección de conflicto → política de alertas
```

Sin una ruta real (Google Routes) y sin tracking de GPS en vivo de los conductores,
no hay "corredor" que calcular ni "conflicto" que detectar — sería una función
decorativa, no la ambulancia real que se pidió. Por eso el cronograma abajo pone
primero la base mínima de Location/Navigation que la ambulancia necesita para
funcionar de verdad, y deja todo lo que NO bloquea la ambulancia corriendo en
paralelo o después.

## Cronograma

| Fase | Contenido | Depende de | Bloquea la ambulancia | Estimado* |
|---|---|---|---|---|
| 0 — Hecho | Mensajería core: contactos, chats 1-a-1, texto en tiempo real, entregado/leído, CI | — | — | ✅ completado |
| 1 — Fundación | Redis/BullMQ (estado caliente, cooldowns), capa de permisos/autorización, observabilidad básica, habilitar PostGIS en Supabase | Fase 0 | Sí | 1–2 semanas |
| 2 — Location & Navigation | Permiso y sesión de GPS, adapters `RoutingProvider`/`GeocodingProvider`/`PlacesProvider`/`NavigationProvider` sobre Google Maps Platform, ubicación actual/heading/speed, cálculo de ETA | Fase 1 | Sí | 2–3 semanas |
| 3 — Emergency Corridor (la ambulancia) | ✅ Rol "ambulancia verificada" (ADR-0006), con panel real de administrador (`EmergencyAdminController`/`AdminGuard`, 2026-09-01 — verificar/asignar por teléfono del conductor, activar/desactivar sin borrar historial; antes solo por SQL/MCP directo), activación por ruta real (`RouteSessionService`), corredor geoespacial con buffer dinámico por velocidad (ADR-0021), candidatos (ADR-0012), Alert Policy con dedup+cooldown y canal recomendado carro/moto (ADR-0013/0017), severidad `INFO/WARNING/CRITICAL` (ADR-0021), cierre real `completed/cancelled/expired` con UI de ambulancia (botones "Finalizar"/"Cancelar") y aviso real "ya pasó" al afectado (ADR-0020, cerrado 2026-09-01). Diferido a propósito, sin evidencia de que haga falta: estados intermedios `ACTIVE_CONFLICT`/`PASSED` (necesitan velocidad relativa/historial de trayectoria que no existe todavía) | Fase 2 | — (es el objetivo) | 3–4 semanas — **cerrado** |
| 4 — Simulación del corredor | Vehículos virtuales (ambulancia/carro/moto), reproducción determinística, los 12 escenarios mínimos del roadmap. ✅ Escenario 1 (10 vehículos, ADR-0022 — encontró y corrigió un bug real de muestreo en `sampleAhead`), ✅ Escenario 2 (100 vehículos — valida escala real de `findNearby`/GEOSEARCH), ✅ Escenario 3 (tres ambulancias simultáneas — valida aislamiento real entre corredores concurrentes) y ✅ Escenario 4 (vehículo fuera de ruta, 2026-09-01 — encontró y corrigió DOS bugs reales: el corredor seguía protegiendo la ruta abandonada cuando la ambulancia se desviaba en vez de proteger por dónde iba de verdad, y `computeDeviation` media distancia al vértice más cercano en vez de al segmento, causando falsos "fuera de ruta" en tramos rectos largos — este segundo bug también afectaba producción real en `location.gateway.ts`). ✅ Recálculo real de ruta al detectar desvío (2026-09-01, ver ADR-0022): antes el escenario 4 solo protegía un radio alrededor de la posición actual sin recalcular — ahora `EmergencyCorridorService` llama de verdad a Google Routes (mismo `RoutingProvider` de Fase 2, con cooldown real de 30s para no gastar de más) y actualiza la ruta activa cuando detecta desvío; decisión que el fundador pidió construir explícitamente (antes diferida a propósito por el costo real de la API). ✅ Escenario 5 (GPS con ruido, 2026-09-02 — dos partes: verificación real y directa de `validateRawReport`/`normalizeReport`, el mecanismo de defensa contra ruido que `SimulationEngineService` nunca ejercitaba antes por escribir directo a Redis, 13/13 casos sin bugs encontrados; más un escenario con ruido determinístico `sin()` sobre lo que la ambulancia reporta al corredor, con una pieza nueva `ambulanceReportNoise` que desacopla el ruido de sensor del movimiento físico real, tras encontrar que meterlo directo en la ruta distorsionaba el tiempo de viaje). ✅ Escenario 6 (GPS atrasado, 2026-09-02 — dos partes, ambas verificadas con llamadas reales, sin necesitar un escenario nuevo del motor porque este atrasa a propósito el reloj, no el movimiento: (a) bug real corregido en `validateRawReport` — un reporte atrasado/fuera de orden se rechazaba con el motivo equivocado (`implausible_jump` en vez de un nuevo `out_of_order`), por un cálculo de velocidad implícita que forzaba un `deltaSeconds` mínimo artificial; (b) gap real de visibilidad corregido en `EmergencyCorridorService.findCandidates` — nunca revisaba si la posición conocida de la AMBULANCIA misma estaba atrasada (>30s), a diferencia de los candidatos, que sí se excluyen si están atrasados desde antes; se agregó una advertencia real en logs, sin cambiar el comportamiento de degradación ya existente). ✅ Escenario 7 (usuario sin conexión, 2026-09-02 — dos partes, sin bugs encontrados por primera vez en la serie: (a) confirma que un candidato que se desconecta a mitad del corredor queda cubierto por el mismo mecanismo de posición atrasada del Escenario 6, sin necesitar manejo especial; (b) primera verificación real del barrido periódico `sweepExpired`/`CorridorExpirySweepProcessor` (cada 15 min, ADR-0020) — confirma con evidencia real, no corrige, que su disparador es el TTL de la sesión de ruta (4h), no la posición de la ambulancia: una ambulancia desconectada sigue con el corredor "activo" hasta que su ruta expire, decisión ya deliberada del fundador). ✅ Escenario 8 (reconexión WebSocket, 2026-09-02 — contraparte del Escenario 7, tampoco encontró bugs: confirma con evidencia real la diferencia entre los dos TTL reales del Location Engine — `STALE_AFTER_MS` (30s, solo marca `stale`) y el TTL interno de 300s de la clave de Redis, que la borra del todo — y que reconectar dentro o después de esa ventana se comporta bien en ambos casos: reportes aceptados, candidato vuelve a aparecer en el corredor, se puede volver a alertar si el cooldown ya venció, y la advertencia de "GPS atrasado" de la ambulancia no queda pegada tras reconectar). ✅ Escenario 9 (ambulancia cancelada, 2026-09-02 — ciclo de vida real de `POST /emergency/corridor/close`: cancelar sin candidatos previos no lanza, cancelar con un candidato alertado lo notifica con el motivo real ("cancelled", no "completed"), doble cancelación es idempotente (no reenvía notificaciones), y cancelar el corredor propio nunca toca el de otra ambulancia. Hallazgo real, documentado y NO corregido por bajo impacto/baja probabilidad: cancelar no limpia el cooldown de recálculo de ruta del conductor, así que un traslado nuevo iniciado por el mismo conductor dentro de los 30s siguientes podría caer al fallback de radio en vez de recalcular real la primera vez — sigue siendo seguro, solo menos preciso). ✅ Escenario 10 (ambulancia terminada, 2026-09-02 — primera vez que un escenario prueba `EmergencyCorridorController` real, no solo los servicios de abajo. Bug real corregido: `POST /emergency/corridor/close` con un `reason` inválido (typo del cliente, o `"expired"` mandado a mano — motivo interno que solo debería producir el barrido real) caía callado al default `"cancelled"`, notificando con la etiqueta equivocada; ahora se rechaza con 400 en vez de adivinar. El default real para `reason` omitido sigue igual). ✅ Escenario 11 (conductor entra/sale del corredor, 2026-09-02 — tampoco encontró bugs: confirma con evidencia real que `findNearby`/`findCandidates` no guardan memoria de posiciones pasadas (cada consulta es un `GEOSEARCH` real sobre la posición actual), así que entrar/salir del buffer geográfico ya funciona por construcción; lo verificado a fondo por primera vez es el dedup/cooldown de 30s a través de MÚLTIPLES transiciones del mismo candidato en el mismo traslado: reentrar en cooldown no duplica alertas, reentrar después de que el cooldown expira sí genera una alerta legítima nueva sin duplicar membresía en el set de alertados, y cerrar el corredor mientras el candidato está fuera del buffer en ese instante igual lo notifica con el motivo real). ✅ Escenario 12 (dos corredores se cruzan, 2026-09-02 — el último de los 12: bug real encontrado y corregido, motivado por evidencia real del Escenario 9. `findCandidates` solo excluía a la propia ambulancia consultante de sus resultados, nunca a OTRAS ambulancias con corredor activo — con dos rutas que se cruzan de verdad, una ambulancia podía tratar a la otra como un candidato civil más. Corregido reusando `ACTIVE_AMBULANCES_KEY` (mismo set real que ya usa `sweepExpired`, no un mecanismo nuevo): `findCandidates` ahora excluye también a cualquier otra ambulancia activa. Verificado 18/18 con Redis real y rutas que se cruzan de verdad — confirmado además revirtiendo el fix temporalmente y viendo la prueba fallar de verdad antes de restaurarlo, evidencia de que la prueba cubre el bug real. Un candidato civil real en el punto de cruce sigue siendo detectado y alertado por AMBAS ambulancias de forma independiente, sin cambios ahí — el filtro nuevo solo excluye ambulancias, nunca candidatos civiles). **Los 12 escenarios mínimos del roadmap (Etapa 7) quedan completos.** Pendiente, fuera de "escenarios mínimos": métricas de latencia/falsos positivos/conflictos perdidos, y hallazgos ya documentados y deliberadamente no corregidos (`sampleAhead` con lookahead fijo de 2km; cooldown de recálculo de ruta no se limpia al cancelar, Escenario 9) — ninguno bloqueante | Fase 3 | — | 1–2 semanas — completa (12/12) |
| 5 — Mensajería pendiente *(en paralelo, no bloquea nada de arriba)* | ~~Multimedia real a Storage (fotos/documentos/audio)~~ (ADR-0031), ~~typing/presence~~ (ADR-0029), ~~notificaciones push~~ (Web Push, primer slice — ADR-0033), ~~rate limiting~~ (ADR-0032), ~~bug de chulos~~ (ya resuelto en el código real, `hydrateMessageStatuses`/`markChatReadRemote` — no se encontró `TECHNICAL_DEBT.md` en el repo, la referencia quedó obsoleta). Pendiente real de esta fila: push para mensajes nuevos de chat (hoy Supabase-directo, sin pasar por el backend) y FCM/APNs nativos cuando exista app empaquetada | Fase 0 | No | continuo, en paralelo |
| 6 — Asistente de voz | ~~Tool registry (`activate_emergency_corridor`, `calculate_route`, etc.) + capa de autorización LLM→dominio~~ (ADR-0016), ~~conexión real Voice→Realtime→Tool Call→Result, con audio real bidireccional (micrófono real → Gemini → voz real de vuelta) verificada de punta a punta contra un micrófono real~~ (ADR-0034, dos slices — reemplaza la elección de OpenAI Realtime de ADR-0016: el fundador habló por el micrófono real de `proyecto-mensajeria` y escuchó la respuesta real de Gemini en voz, con `list_vehicles` llamada real y dato real de Supabase devuelto correctamente). Pendiente real de esta fila: "Modo conducción" manos-libres de verdad (hoy hay que tocar y sostener la pantalla — falta activación por voz, funcionar con el teléfono bloqueado, UI pensada para no distraer manejando), mejorar el downsampling (hoy decimación simple, sin filtro anti-aliasing), interrupciones/VAD del lado del cliente, prueba en dispositivo móvil real, y decidir cómo confirmar por voz `activate_emergency_corridor` (hoy siempre queda en `needs_confirmation`, nunca se ejecuta por este camino) | Fase 2 (para las tools de navegación/emergencia) | No (la ambulancia arranca por botón primero; voz se agrega después) | 2–3 semanas |
| 7 — Recordatorios por ubicación | ✅ Construido (esta fila estaba desactualizada — el código ya existe: `LocationRemindersModule`, `GeofenceTriggerService` evalúa Haversine contra la posición actual en cada `location:update` real, dispara una sola vez de forma idempotente; `ReminderCacheService` en Redis evita golpear Postgres en cada ping. `GET/POST /location-reminders`, `DELETE /location-reminders/:id`. Reusa `GET /navigation/geocode` del lado del llamador en vez de geocodificar direcciones internamente. Nota real encontrada auditando para Fase 8: el ADR que debería documentar esto (`ADR-0015-location-reminders.md`, citado por `MISSING_CAPABILITIES.md`) NO existe como archivo — brecha de documentación real, el código sí existe y está verificado (RLS real + idempotencia del disparo + smoke test contra Redis, 13/13). Pendiente real: integración HTTP/WebSocket de punta a punta con JWT real, push cuando la app está cerrada (Fase 5), dictado por voz (Fase 6), recordatorios recurrentes (no pedidos todavía) | Fase 2 | No | 1–2 semanas — completa |
| 8 — Hardening + piloto controlado | **Arrancada 2026-09-02** (Fases 3-4 ya completas y estables). ✅ Primer hallazgo real de seguridad, encontrado Y corregido el mismo día (ADR-0036): los guards globales (`APP_GUARD`, incluido el rate limiter de toda la API) NO protegen a los gateways de WebSocket — verificado armando una app NestJS real + Redis real + `socket.io-client` real (65 mensajes en ráfaga pasaron sin ningún bloqueo). Corregido con un chequeo manual real contra Redis (`checkSocketRateLimit`, mismo patrón `INCR`/`EXPIRE` que ya usa el resto del proyecto) dentro de cada handler: `location:update` (100/10s), `voice:text` (10/10s), `voice:audio-chunk` (50/5s) — límites elegidos por decisión delegada explícita del fundador ("tome la mejor"), generosos a propósito para no romper tráfico legítimo real (incluida la ráfaga de reconexión ya verificada en el Escenario 6). De paso, límite real de UNA sola sesión de Gemini Live activa por usuario (costo real por sesión) — una conexión nueva del mismo usuario cierra la vieja en vez de dejarla huérfana. La autenticación de ambos gateways SÍ seguía intacta todo este tiempo (es manual en `handleConnection`, nunca dependió de guards). Verificado 12/12 con Redis real. Pendiente, menor: reemplazar `cors: { origin: "*" }` por un dominio real antes de producción. Auditoría de "autorización de emergencia" y "privacidad de ubicación": ambas ya están bien (corredor solo se activa/consulta con `verified && active` server-side; `GET /location/me` solo devuelve la ubicación propia) — sin cambios necesarios. ✅ Primer hallazgo real de rendimiento, encontrado Y corregido el mismo día (ADR-0037): `findCandidates` pagaba `getCurrent` de Redis una vez POR CADA muestra en la que un mismo candidato aparecía (hasta 20 muestras) en vez de una vez por candidato único — medido con prueba de carga real (`loadtest-corridor.ts`, 500 candidatos reales/20 muestras): 2579→548 comandos reales de Redis por consulta, p50 182ms→25.5ms. Corregido con `LocationStateService.geosearchNearby` (dedup antes de revalidar frescura) — resultado idéntico, verificado con 6/6 casos deterministas reales. ✅ Segundo hallazgo real de rendimiento, encontrado Y corregido el mismo día (ADR-0038, "WebSocket scaling"): Socket.IO corría sin adapter de Redis — `LocationBroadcastService.notify()` (alertas reales del corredor de emergencia) solo entregaba a sockets conectados a la MISMA instancia de Node, así que con más de una instancia detrás de un load balancer un candidato conectado a otra instancia nunca recibiría una alerta real, en silencio (el endpoint HTTP responde `notified: true` igual). Verificado empíricamente con dos servidores Socket.IO reales + Redis real + `socket.io-client` real: sin adapter, el evento de verdad no cruzaba instancias; con `RedisIoAdapter` (reusa la misma `REDIS_CONNECTION` vía `.duplicate()`, sin librería de cliente Redis nueva — mismo patrón oficial de NestJS, verificado contra su ejemplo oficial y el README de `@socket.io/redis-adapter` antes de escribir código), sí cruza; entrega local (caso de hoy, una sola instancia) sin cambios. 3/3 casos reales. ✅ Tercer hallazgo real (ADR-0039, Rendimiento + Seguridad), esta vez contra Postgres/Supabase REALES (el fundador conectó el proyecto real "Copiloto" a este entorno): auditoría real de la única tabla del dominio Emergency Corridor (`emergency_vehicles`) con Supabase Advisors reales encontró una FK sin índice (`verified_by`), una policy RLS que reevaluaba `auth.uid()` por fila, y una función `SECURITY DEFINER` (`is_verified_ambulance_driver`) ejecutable por CUALQUIERA sin autenticar (confirmado sin uso real ni en el backend ni en ninguna RLS) — las tres corregidas con una migración real aplicada al proyecto real, verificado antes/después con los Advisors reales (los 4 findings desaparecieron). Se auditó TODO el esquema real (no solo esta tabla) — se encontraron más hallazgos reales en las tablas de mensajería (`chats`/`messages`/`statuses`/etc.), documentados en ADR-0039 pero deliberadamente NO corregidos (dominio de `proyecto-mensajeria/`, fuera de este alcance). Con esto, Rendimiento de la Fase 8 queda cubierto en su totalidad para el dominio real de este backend (Redis tuning vía ADR-0037/0038, DB indexes/PostGIS vía ADR-0039 — PostGIS instalado pero sin consumidor real todavía, decisión ya documentada). Pendiente real: `cors: { origin: "*" }` sin resolver (ADR-0036), `auth_leaked_password_protection` de Supabase Auth (config del dashboard, no de esquema), y el piloto real (1 ambulancia simulada + 5–10 conductores → 1–3 ambulancias + 30–50 → 5–10 ambulancias + 100–500) sin empezar | Fases 3–4 completas y estables | — | variable, no avanza por calendario sino por criterios de salida — en curso |
| 9+ — Mobility Intelligence / Traffic Prediction / Signal Priority | Eventos de vehículos pesados, riesgo de tráfico, simulador de semáforos | Fase 8 | No | largo plazo, después del MVP |

*Los estimados son de esfuerzo de desarrollo enfocado, no fechas de calendario
garantizadas — el propio roadmap del proyecto es explícito en esto: **"no avanzar
de etapa por calendario, avanzar cuando se cumpla: funcional + probado + observable
+ seguro + documentado + simulado donde aplique."**

## Ruta crítica para tener la ambulancia funcionando de verdad

```
Fase 1 (Fundación) → Fase 2 (Location & Navigation) → Fase 3 (Emergency Corridor)
→ Fase 4 (Simulación) → recién ahí piloto controlado con conductores reales
```

Eso son ~7–11 semanas de trabajo enfocado (fases 1–4) antes de tener un corredor de
emergencia real y validado por simulación — no antes, porque sin simulación no hay
forma responsable de probar "conflicto detectado" con vehículos reales en la calle.

La Fase 5 (resto de mensajería) puede avanzar en paralelo sin quitarle tiempo a la
ambulancia, porque no comparte dependencias técnicas con Location/Emergency.

## Reordenamiento 2026-08-19: Fase 7 adelantada

El fundador pidió adelantar la Fase 7 (Recordatorios por ubicación) y
preguntó cuáles fases la preceden. Respuesta, mirando la tabla de arriba:
Fase 7 depende únicamente de **Fase 2 (Location & Navigation)**, que ya está
100% completa y verificada contra Google Maps real. No depende de Fase 3
(Emergency Corridor, en curso), ni de Fase 4 (Simulación) ni de Fase 6
(Asistente de voz) — esas dependencias son solo de numeración de tabla, no
técnicas.

**Orden de ejecución ajustado** (los números de fase no cambian, para no
romper referencias cruzadas en ADRs existentes; lo que cambia es el orden en
que se ejecutan):

```
Fase 3 (Emergency Corridor, en curso) → Fase 7 (Recordatorios por ubicación)
→ Fase 4 (Simulación) → Fase 6 (Asistente de voz)
```

Fase 7 se adelanta frente a Fase 4 y Fase 6 porque: (a) no depende
técnicamente de ninguna de las dos, (b) reutiliza infraestructura que ya
existe y está verificada (`LocationStateService`, el stream de
`location:update` por WebSocket), y (c) es la pieza que hace realidad el
requisito del fundador de que la plataforma funcione completa como app de
mensajería para cualquier usuario, tenga o no vehículo (input de texto
primero; dictado por voz es una capacidad de Fase 6, se conecta después). La
Fase 4 (Simulación del corredor) sigue bloqueada por completar Fase 3, así
que no se pierde nada adelantando Fase 7 mientras tanto — es tiempo que de
otra forma quedaría esperando.

Como paso previo a Fase 7 se construyó el registro de vehículos y "modo de
manejo" (`docs/decisions/ADR-0014-vehicle-registration-and-driving-mode.md`)
— no es parte formal de ninguna fase numerada del roadmap original, pero es
un requisito de producto nuevo del fundador (carro+moto, modo de manejo) que
tenía sentido cerrar antes de entrar a recordatorios, dado que ambos tocan
"estado del usuario en este momento".

## Próximo paso concreto

Para arrancar la Fase 1 ya mismo, el primer bloque de trabajo es:
habilitar PostGIS en el proyecto Supabase, definir la capa de permisos/autorización
(quién puede activar una emergencia), y decidir si se usa Redis administrado o se
arranca con jobs más simples mientras el volumen es bajo (evitar complejidad
innecesaria, regla del propio proyecto de "no introducir infraestructura distribuida
sin evidencia de necesidad").

# Decisiones — índice

Historial de decisiones de alto nivel del proyecto. El detalle screen-por-screen del
front-end tiene su propio changelog ("Historial de decisiones") dentro de
`docs/architecture/Orden-Frontend-Lovable-CoPiloto.md`; aquí solo quedan los hitos.

| Fecha | Decisión | Detalle |
|---|---|---|
| 2026-08-12 | Construir una app de mensajería propia (clon funcional de WhatsApp), no orquestar la app real de WhatsApp. | `docs/product/Ficha-04-CoPiloto.md` §11 |
| 2026-08-12 | División en dos frentes: front-end en Lovable (usuario), back-end en Claude (una vez entregado el front). | `docs/architecture/Orden-Frontend-Lovable-CoPiloto.md` |
| 2026-08-12 | Fase front-end con datos 100% simulados/mock, sin Supabase real — reversión explícita por mala experiencia previa de seguridad con Lovable. | `docs/architecture/Orden-Frontend-Lovable-CoPiloto.md` §0 |
| 2026-08-12 | Identidad visual propia (violeta/índigo `#5B4FE5` + ámbar `#F5A623`, sin cola en burbujas, Manrope, Phosphor) para diferenciarse claramente de WhatsApp. | `docs/architecture/Orden-Frontend-Lovable-CoPiloto.md` §6.1 |
| 2026-08-12 | Patentación / protección de propiedad intelectual marcada como prioridad a evaluar antes de escalar o mostrar el producto públicamente. | `docs/product/Ficha-04-CoPiloto.md` §9, punto 6 |
| 2026-08-12/13 | Fase 1 (MVP) y Fase 2 (paridad WhatsApp) del front-end completadas en Lovable. Fase 3 (VoIP real, proximidad) queda en pausa. | `docs/architecture/Orden-Frontend-Lovable-CoPiloto.md` |
| 2026-08-13 | Pasar a la fase de back-end. El usuario crea su propio proyecto de Supabase; el export de Lovable se entrega como zip (`proyecto-mensajeria.zip`, en la raíz del repo, pendiente de inspeccionar y cruzar contra el esquema). | `docs/architecture/Especificacion-Backend-Supabase-CoPiloto.md` |
| 2026-08-18 | Adopción de la estructura de tres niveles (`CLAUDE.md` + `.claude/skills/puntos-movilidad-engineering/` + `docs/`) para todo el proyecto "Puntos Movilidad", del cual CoPiloto (mensajería/asistente) es una parte. Reorganización de archivos existentes hacia esta estructura. | este commit |
| 2026-08-18 | Auditoría inicial del repositorio: front-end real inspeccionado (TanStack Start + React 19), cero backend, patrón de acciones/hooks aislados confirmado como listo para voz. | `docs/architecture/CURRENT_ARCHITECTURE.md`, `REUSE_MATRIX.md`, `MISSING_CAPABILITIES.md`, `TECHNICAL_DEBT.md` |
| 2026-08-18 | Confirmado: el proyecto Supabase "Copiloto" (`wrkuusacwkdazfwynhkz`, `ca-central-1`) ya existe y está activo, sin tablas todavía. Deja de ser un bloqueo para empezar el backend real. | `docs/architecture/MISSING_CAPABILITIES.md` |
| 2026-08-18 | Esquema real de base de datos aplicado sobre "Copiloto": 13 tablas, RLS en todas, 4 buckets de Storage, funciones helper restringidas a `authenticated`. Se agregó CI (GitHub Actions: lint+typecheck+build) para el front-end. | `docs/decisions/ADR-0001-esquema-backend.md`, `.github/workflows/ci.yml` |
| 2026-08-18 | Se regeneró `bun.lock` del front-end: el original de Lovable apuntaba a su proxy privado de npm, lo cual rompía CI (403) aunque funcionara en instalaciones locales normales. Se corrigieron además ~100 errores de formato (prettier/eslint) preexistentes en el código de Lovable, con `--fix` (solo estilo, sin cambios de lógica). | `docs/architecture/TECHNICAL_DEBT.md` §1 |
| 2026-08-18 | Confirmado que "el logo de Lovable" que aparece al abrir la app es únicamente el favicon por defecto (`public/favicon.ico`); no hay ningún otro watermark de Lovable en la UI. Reemplazo pospuesto hasta tener un ícono propio de Copiloto — pedido explícito del fundador de dejarlo para más adelante. | `docs/architecture/TECHNICAL_DEBT.md` §9 |
| 2026-08-18 | Se conectó la verificación telefónica del onboarding a Supabase Auth real, usando **Test OTP** (números de prueba sin proveedor de SMS pagado) mientras se decide el proveedor de producción. La sesión se guarda en memoria (no localStorage) mientras la app corra solo como web/PWA, por la regla de seguridad del fundador; se migra a `@capacitor/preferences` (almacenamiento nativo) cuando exista empaquetado real. `completeOnboarding()` ahora crea/actualiza la fila real en `profiles`. | `proyecto-mensajeria/src/lib/supabase/client.ts`, `src/lib/actions/auth.ts`, `src/store/AppStore.tsx` |
| 2026-08-18 | Se intentó configurar Twilio (Messaging Service y Verify) como proveedor de SMS de producción; se abandonó por ahora — Verify redirige a un muro de "actualizar cuenta" en el plan trial y Messaging Service requiere registro A2P 10DLC (proceso de varios días). Se prioriza no perder más tiempo en esto mientras se prueba con el equipo cercano. | conversación 2026-08-18 |
| 2026-08-18 | Se confirmó y corrigió de punta a punta el atajo de números de prueba (`VITE_DEV_TEST_PHONES`): el proveedor "Email" del proyecto Supabase estaba deshabilitado por completo (no solo "Confirm email" activo), y el dominio sintético original era rechazado por Supabase. Con el proveedor Email habilitado, "Confirm email" desactivado, y un dominio de correo real (alias `+`), el flujo completo teléfono → OTP → correo → perfil → permisos quedó verificado funcionando con una sesión y una fila de `profiles` reales. | `docs/architecture/TECHNICAL_DEBT.md` §12 |

## Pendiente de decidir

- Proveedor de SMS/OTP de **producción** para verificación telefónica (Twilio /
  MessageBird / Vonage) — hoy se usa Test OTP (sin costo, números fijos) como
  solución interina, elegida explícitamente por el fundador el 2026-08-18.
- Proveedor Phone sigue deshabilitado a propósito en el Dashboard de Supabase Auth
  (no hay SMS real todavía); el atajo de números de prueba cubre las pruebas
  mientras tanto.
- Verificación de correo real (hoy sigue simulada; la spec la trata como
  identidad secundaria).
- Cruce de nombres del esquema de `docs/architecture/Especificacion-Backend-Supabase-CoPiloto.md`
  contra el código real exportado en `proyecto-mensajeria/` (ya inspeccionado en la
  auditoría del 18 de agosto; el modelo de `src/lib/domain/types.ts` coincide en general
  con la spec — cruce fino queda para cuando se apliquen las migraciones).
- Ícono/favicon propio de Copiloto para reemplazar el de Lovable (pendiente de diseño).
| 2026-08-18 | Contactos/chats/mensajes de texto conectados a Supabase con Realtime (búsqueda/alta de contacto por teléfono, chats 1-a-1 reales, mensajes de texto con sincronización en vivo y confirmación de lectura); resto de mensajería (voz, fotos/documentos, ubicación, reacciones, grupos, silenciar/fijar/archivar, mensajes que desaparecen, reenviar/editar/borrar) sigue simulado. Se encontró y corrigió un bug de diseño en las políticas RLS: `chat_participants_insert` verificaba "soy el creador del chat" con un `EXISTS` directo contra `chats`, pero esa subconsulta corre con los permisos del usuario (no bypassa RLS) y la política de lectura de `chats` exige ya ser participante — trampa circular que bloqueaba permanentemente la creación de cualquier chat 1-a-1 nuevo. Se agregó `is_chat_creator()` (función `SECURITY DEFINER`, igual que `is_chat_participant`/`is_chat_admin`) para romper el ciclo. | `proyecto-mensajeria/src/lib/actions/chats.ts`, `supabase/migrations/20260818221803_fix_chat_participants_insert_rls_recursion.sql`, `docs/architecture/TECHNICAL_DEBT.md` |
| 2026-08-18 | Se define el cronograma de ejecución para la función de ambulancia (Emergency Corridor) y el resto de funcionalidades nuevas, priorizado por dependencia técnica real: Fundación (Redis/PostGIS/permisos) → Location & Navigation (Google Maps adapters) → Emergency Corridor → Simulación, con mensajería pendiente (multimedia, push, presence) corriendo en paralelo sin bloquear la ambulancia. | `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-08-18 | Arranque de la Fase 1 del cronograma de Emergency: PostGIS habilitado en Supabase, y primera porción del dominio Emergency construida — tabla `emergency_vehicles` (ambulancias verificadas) con RLS sin autoservicio (la verificación es una operación administrativa, nunca algo que el conductor pueda auto-otorgarse) y helper `is_verified_ambulance_driver()`, mismo patrón `SECURITY DEFINER` ya usado en mensajería. Verificado con simulación transaccional de RLS: auto-inserción bloqueada, visibilidad ajena bloqueada, dueño verificado sí se ve a sí mismo. | `docs/decisions/ADR-0006-emergency-corridor.md`, `supabase/migrations/20260818234452_enable_postgis.sql`, `supabase/migrations/20260818234636_emergency_authorization_layer.sql` |
| 2026-08-18 | Decisión explícita del fundador: introducir el backend NestJS (`backend/`) ya, en vez de seguir solo con Supabase directo. Se resuelve así la decisión que había quedado pendiente en la auditoría inicial. La mensajería (`proyecto-mensajeria/`) no se toca ni se migra — sigue consumiendo Supabase directo, sin evidencia que justifique cambiarla. Primer slice del backend: cliente Supabase con service role key, guard que valida el JWT de sesión ya existente, `/health`, y `/emergency/vehicles/me`. CI extendido con un job `backend` en paralelo al de `frontend`. Redis/BullMQ quedan pendientes de que el fundador elija y provisione un proveedor. | `docs/decisions/ADR-0007-backend-nestjs.md`, `backend/` |
| 2026-08-19 | Decisión explícita y definitiva del fundador: Redis/BullMQ con **Upstash**, como infraestructura real (no opcional), pensada para no bloquear un cambio de proveedor más adelante — un único `REDIS_URL` es el único punto que conoce al proveedor. `RedisModule`/`QueueModule` en `backend/`, registro central de colas con `emergency-alerts`/`location-reminders` reservadas (sin processor, esperando sus dominios reales) y una cola `system` con job `ping` como prueba de humo. Verificado de punta a punta contra un Redis real (no simulado): encolar → procesar → consultar resultado, los tres pasos confirmados con logs y respuestas reales. `REDIS_URL` pasa a ser obligatorio para que el backend arranque. | `docs/decisions/ADR-0008-redis-upstash.md`, `backend/src/common/redis/`, `backend/src/common/queue/`, `backend/src/modules/system/` |
| 2026-08-19 | Primer slice de Fase 2 (Location & Navigation): motor de ubicación construido en `backend/src/modules/location/` — ingesta por WebSocket (`LocationGateway`, namespace `/location`, mismo patrón de auth JWT de Supabase que el resto del backend), validación deliberadamente no ingenua (rango/precisión/velocidad, desfase de reloj, y detección de salto implausible con distancia real Haversine contra la última posición conocida), estado caliente en Redis (`LocationStateService`, TTL 300s, ventana stale 30s) y endpoint `GET /location/me`. Persistencia histórica en PostGIS deliberadamente NO construida todavía — no existe consumidor real (llega con tracking del Emergency Corridor en Fase 3). Verificado con typecheck/lint/build limpios, rechazo de conexión sin token/con token inválido probado con llamadas reales a Supabase Auth, y normalización + Redis probados con datos reales (coordenadas de Medellín) contra un Redis real. Límite honesto: el camino de aceptación de punta a punta con un JWT real de usuario a través del propio WebSocket no se probó, porque requeriría credenciales que este entorno no maneja por regla de seguridad. | `docs/decisions/ADR-0009-location-engine.md`, `backend/src/modules/location/` |
| 2026-08-19 | Segundo slice de Fase 2 (Location & Navigation): adapters `RoutingProvider` (Google Routes API, `computeRoutes`) y `GeocodingProvider` (Google Geocoding API, forward + reverse) en `backend/src/modules/navigation/`, detrás de interfaces — cambiar de proveedor en el futuro es cambiar el binding en `navigation.module.ts`, sin tocar consumidores. `GoogleMapsModule` centraliza `GOOGLE_MAPS_API_KEY` (opcional en el arranque, igual que tuvo `REDIS_URL` antes de Upstash). Endpoints `/navigation/route`, `/navigation/geocode`, `/navigation/reverse-geocode` protegidos con `SupabaseAuthGuard` por el costo real de cada llamada. Aclaración de alcance importante: el "Navigation SDK" de Google es un SDK de cliente (Android/iOS), no una API de backend — no se construyó un `NavigationProvider` de backend separado por no existir tal endpoint; la navegación de backend (ETA, futuro desvío de ruta) se apoya en `RoutingProvider`. `PlacesProvider` y detección de desvío de ruta quedan diferidos a un slice futuro (sin consumidor real todavía). Verificado con typecheck/lint/build limpios; verificación real contra la API de Google queda pendiente de que el fundador provisione la key. | `docs/decisions/ADR-0010-navigation-google-maps.md`, `backend/src/modules/navigation/`, `backend/src/common/google-maps/` |
| 2026-08-19 | Cierre real del slice de Routing/Geocoding: el fundador creó la API key de Google Maps Platform (proyecto `copiloto-506002`), habilitó Routes API y Geocoding API, configuró facturación por pago por uso (sin plan de suscripción, para no sobredimensionar en esta etapa) y restringió la key por IP + por API. Verificado con llamadas reales (no simuladas) contra Google: Geocoding de "Parque Berrío, Medellín" devolvió coordenadas correctas, y Routes API calculó una ruta real Parque Berrío → El Poblado (7.287m, ~18min). Fase 2 slice 2 queda cerrado de punta a punta. | `docs/decisions/ADR-0010-navigation-google-maps.md` |
| 2026-08-19 | Tercer slice de Fase 2 (Location & Navigation): sesión de ruta activa (`RouteSessionService`, Redis) y detección de desvío (`route-deviation.ts`) — decodifica la polyline de la ruta activa y calcula distancia real (Haversine) al punto más cercano; umbral 60m. `POST/DELETE /navigation/route-session` arranca/cierra una ruta usando la última ubicación real del usuario (Location Engine) como origen, nunca un origen mandado por el cliente. `LocationGateway` calcula el desvío en cada `location:update` si hay ruta activa. Refactor de paso: `haversineMeters`/`LatLng` se centralizaron en `common/geo/` (antes duplicados). Se priorizó esto sobre `PlacesProvider` porque es la misma pieza matemática ("distancia de un punto a una geometría de ruta") que va a necesitar el Conflict Engine de Emergency Corridor en Fase 3. Verificado con datos reales: decodificación + desvío probados contra la polyline real devuelta por Google en la prueba del fundador (ADR-0010) — 6/6 casos; persistencia probada contra Redis real — 4/4 casos. Límite honesto: falta la integración end-to-end vía HTTP/WebSocket con un JWT real de usuario, mismo límite ya documentado para el Location Engine. | `docs/decisions/ADR-0011-route-deviation.md`, `backend/src/modules/route-session/`, `backend/src/common/geo/` |
| 2026-08-19 | Primer slice real de Emergency Corridor (Fase 3): `EmergencyCorridorService.findCandidates()` — el corredor de una ambulancia ES su ruta activa (reusa `RouteSessionService`, sin duplicar la noción ni crear un endpoint "arrancar corredor" separado); índice geoespacial en Redis (`LocationStateService` extendido con `GEOADD`/`GEOSEARCH`, elegido sobre PostGIS por ser posición caliente de segundos, no histórico) para encontrar candidatos dentro de un buffer fijo de 200m, muestreando hasta 20 puntos hacia adelante sobre la ruta decodificada. Limitación real de Redis (miembros GEO no expiran individualmente) mitigada revalidando cada candidato contra su `location:current` real antes de devolverlo — probado explícitamente. `GET /emergency/corridor/candidates` solo accesible para conductores con `emergency_vehicles.verified=true` y `active=true`. Se priorizó sobre `PlacesProvider` por reusar directamente Location Engine + Routing + detección de desvío ya construidos, y ser la base matemática real del Conflict Engine. Verificado con 7/7 casos reales contra Redis local real y la polyline real que devolvió Google en la prueba del fundador (mismo dato usado en ADR-0010/0011) — incluye el caso límite del candidato "vencido" en el índice GEO. Diferido a propósito: buffer dinámico por velocidad, Alert Policy (notificaciones/dedup/cooldown/voz), y ubicación en tiempo real dentro del chat (dominio de mensajería distinto — reutilizará esta misma infraestructura cuando se construya). | `docs/decisions/ADR-0012-emergency-corridor-candidates.md`, `backend/src/modules/emergency-corridor/` |
| 2026-08-19 | Segundo slice de Emergency Corridor: Alert Policy real — `AlertPolicyService` decide a quién alertar con dedup+cooldown atómico en Redis (`SET NX EX`, 30s por par ambulancia/candidato; el cooldown también cubre "expiración" sin mecanismo aparte). Entrega por el mismo WebSocket de `/location` que ya existía: cada socket se une a una room por `userId` (`LocationGateway`), y un nuevo `LocationBroadcastService` (con la referencia real al servidor Socket.IO vía `OnGatewayInit`) desacopla "quién decide alertar" de "cómo se entrega". `GET /emergency/corridor/candidates` ahora evalúa Y despacha en la misma llamada. Diferenciar carro/moto queda pendiente a propósito: no existe dato de tipo de vehículo en ningún punto del sistema — se documentó como decisión de producto abierta, no se simuló. El fundador señaló un riesgo real (candidatos con ubicación vieja/inactiva no detectados); se aclaró que la solución correcta es que la futura app cliente reporte ubicación continuamente mientras está abierta (no solo navegando) con los permisos adecuados — el backend ya lo soporta, quedó documentado como decisión de producto abierta. Verificado con Redis real (5/5, dedup/cooldown) y con un servidor+cliente Socket.IO reales (3/3, entrega real por WebSocket, aislamiento por room confirmado). | `docs/decisions/ADR-0013-alert-policy.md`, `backend/src/modules/emergency-corridor/alert-policy.service.ts`, `backend/src/modules/location/location-broadcast.service.ts` |
| 2026-08-19 | Decisión de producto del fundador (tres puntos, misma conversación): (1) confirmado y verificado que la plataforma ya funciona completa como app de mensajería para quien no tiene carro ni moto — ningún flujo real depende de tener un vehículo registrado; (2) la app debe soportar un usuario con carro Y moto, usados en días/horas distintos, vía un concepto de "Modo de manejo" (selección manual o pregunta del asistente); (3) el cliente debe registrar las placas de carro y moto, pensando en un uso de seguridad futuro todavía sin definir (no se agregó unicidad global de placa por falta de caso de uso real). Se construyó `user_vehicles` (Postgres, autoservicio, RLS `user_id = auth.uid()`, un carro y una moto por usuario) y `DrivingModeService` (Redis, TTL 24h). Desbloquea (sin conectarla todavía) la diferenciación carro/moto de Alert Policy pendiente desde ADR-0013. Se dejó explícitamente PENDIENTE, a pedido del fundador, una futura función de velocidad por sectores — con la restricción de tono ya fijada: informativa, nunca punitiva o alarmante. Verificado con RLS real (simulación transaccional con JWT real: aislamiento entre usuarios, unicidad por tipo de vehículo, tipo inválido, todos bloqueados como se esperaba) y `get_advisors` sin alertas nuevas. | `docs/decisions/ADR-0014-vehicle-registration-and-driving-mode.md`, `backend/src/modules/vehicles/`, `supabase/migrations/20260819052500_user_vehicles.sql` |
| 2026-08-19 | Reordenamiento del cronograma: el fundador pidió adelantar la Fase 7 (Recordatorios por ubicación). Se confirmó que Fase 7 depende únicamente de Fase 2 (Location & Navigation), ya completa — no de Fase 4 (Simulación) ni Fase 6 (Voz), que solo la preceden por numeración de tabla, no por dependencia técnica real. Los números de fase no cambian (para no romper referencias cruzadas existentes); el orden de ejecución sí: Fase 3 (en curso) → Fase 7 → Fase 4 → Fase 6. | `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-08-19 | Primer slice real de Fase 7 (Recordatorios por ubicación): `location_reminders` (Postgres, autoservicio, RLS `user_id = auth.uid()`) + `GeofenceTriggerService` (evalúa Haversine — misma pieza matemática ya usada en desvío de ruta y Emergency Corridor — contra la posición de cada `location:update`, dispara una sola vez de forma idempotente) + `ReminderCacheService` (Redis, lectura-a-través, TTL 24h como red de seguridad; evita consultar Postgres en cada ping de ubicación de cada usuario, mismo motivo por el que Alert Policy es pull en vez de evaluar cada `location:update`, ADR-0013). El resultado del geofence viaja en el mismo ack de `location:update` que ya devuelve `route` (`remindersTriggered`), no por `LocationBroadcastService` — evita un ciclo de módulos entre `LocationModule` y el nuevo módulo. Geocoding de direcciones de texto ("Belén, Medellín" → coordenadas) queda del lado del llamador vía `GET /navigation/geocode`, ya existente — no se duplicó aquí, por la misma razón de evitar el ciclo. Verificado con RLS real (simulación transaccional, incluye idempotencia del disparo confirmada contando filas afectadas en dos `UPDATE` sucesivos) y un smoke test real contra Redis local (13/13 casos, `LocationRemindersService` reemplazado por un stub en memoria ya que su capa Postgres se verificó por separado). | `docs/decisions/ADR-0015-location-reminders.md`, `backend/src/modules/location-reminders/`, `supabase/migrations/20260819053900_location_reminders.sql` |
| 2026-08-19 | Primer slice real de Fase 6 (Asistente de voz): Tool Registry + capa de autorización/confirmación — `AssistantModule`, 6 tools (`create_location_reminder`, `calculate_route`, `activate_emergency_corridor`, `set_driving_mode`, `get_driving_mode`, `list_vehicles`), cada una orquestando servicios de dominio ya existentes y ya verificados (`LocationRemindersService`, `RoutingProvider`/`GeocodingProvider`, `VehiclesService`/`DrivingModeService`, `EmergencyVehiclesService`/`RouteSessionService`) — ninguna tool toca Postgres/Redis/Google directo, regla de seguridad del proyecto aplicada literalmente. `activate_emergency_corridor` (la única de mayor riesgo) exige autorización real (verificado+activo, mismo chequeo que `EmergencyCorridorController`) y confirmación explícita: la primera llamada sin `confirmed:true` devuelve `needs_confirmation` sin ejecutar ningún efecto real, verificado explícitamente que `RouteSessionService.start` no se invoca hasta la segunda llamada confirmada. Se dejaron fuera a propósito, documentado como gap real: integración Realtime/STT (requiere que el fundador provisione cuenta/API key, mismo tipo de paso que Google Maps), Modo conducción (estado de cliente, no de backend), y tools de mensajería (bloqueadas porque el dominio de mensajería no tiene capa de aplicación NestJS todavía, Fase 5 sin construir). Verificado con un arranque real de `AppModule` completo (grafo de DI de los 6 módulos consumidos, sin ciclos, `@nestjs/testing` como devDependency temporal removida después) y 17/17 casos de lógica de negocio (autorización, gating de confirmación, validación) contra stubs honestos de las piezas externas ya verificadas por separado. | `docs/decisions/ADR-0016-assistant-tool-registry.md`, `backend/src/modules/assistant/` |
| 2026-08-19 | Cierre del gap de diferenciación carro/moto de Alert Policy (pendiente desde ADR-0013, desbloqueado por ADR-0014): cada evento `corridor:alert` ahora incluye `recommendedChannel` (`visual_audio` para carro, `voice_priority` para moto, `default` para quien no fijó Modo de manejo), usando `DrivingModeService` real. El texto del mensaje sigue siendo el mismo para todos a propósito — el fundador dio una frase exacta, inventar redacciones distintas sin su aprobación habría sido una decisión de producto ajena a este backend; lo que se diferencia es el canal de entrega, no la redacción. El resto del alcance original de Fase 3 (buffer dinámico por velocidad, estados `ACTIVE_CONFLICT`/`PASSED`, severidad `INFO`/`WARNING`/`CRITICAL`, cierre de corredor) sigue diferido a propósito, sin pedirse todavía. Verificado con Redis real, 6/6 casos (incluye que el cooldown existente sigue funcionando sin cambios). | `docs/decisions/ADR-0017-alert-channel-differentiation.md`, `backend/src/modules/emergency-corridor/alert-policy.service.ts` |
| 2026-08-19 | Cierre del gap de tools de mensajería dejado abierto en ADR-0016: nuevo `MessagingModule` envuelve las MISMAS tablas que ya usa `proyecto-mensajeria` (`chats`/`chat_participants`/`messages`/`contacts`/`profiles`), reimplementando a mano la misma autorización que la RLS real ya exige (nunca se confía en un `chatId`/`senderId` de argumento). Tres tools nuevas: `list_chats`, `read_messages`, `send_message` (esta última con `requiresConfirmation=true`, mismo criterio que `activate_emergency_corridor` — mandar un mensaje real a un tercero es irreversible y visible). Alcance a propósito, sigue sin resolver: solo chats 1 a 1 y mensajes de texto — notas de voz/fotos/ubicación/grupos siguen siendo simulación local en `proyecto-mensajeria`, así que el asistente tampoco puede leer/mandar una nota de voz de verdad todavía (gap independiente de Gemini/Realtime). Verificado con arranque real del `AppModule` completo (9 tools registradas, sin ciclos) y, contra datos reales de producción vía Supabase MCP (solo lectura, sin tocar el chat real de un tercero): resolución de contacto → chat real, lectura de los 41 mensajes reales, y denegación correcta a un `userId` no participante. Deliberadamente NO se probó el envío real contra el chat de producción — habría mandado un mensaje visible a un tercero real sin su consentimiento. | `docs/decisions/ADR-0018-messaging-bridge.md`, `backend/src/modules/messaging/`, `backend/src/modules/assistant/tools/{list-chats,read-messages,send-message}.tool.ts` |
| 2026-08-19 | Integración real de "Copiloto" (Modo de manejo, Recordatorios, Emergencia, Notificaciones) como 5ª pestaña dentro de `proyecto-mensajeria`, sobre la app real (no aparte, a pedido explícito del fundador: "todo es un solo producto"). Cliente HTTP nuevo (`src/lib/backend/client.ts`) + 3 hooks (`useDrivingMode`, `useLocationReminders`, `useCopilotoRealtime`) que consumen los endpoints reales ya construidos (ADR-0009 a ADR-0017), más una conexión WebSocket real y compartida al namespace `/location` que activa el reporte de GPS del navegador de verdad (cierra un gap de producto documentado desde ADR-0013). Verificado con typecheck/lint/build limpios, incluyendo build de producción completo. Pendiente: prueba end-to-end con sesión real del fundador (login + permiso de ubicación reales); el ADR de esta integración quedó como paso "Record" pendiente hasta ahora. | `docs/decisions/ADR-0019-copiloto-tab-frontend-integration.md`, `proyecto-mensajeria/src/components/copiloto/`, `proyecto-mensajeria/src/hooks/` |
| 2026-08-19 | Cierre de corredor de emergencia (`POST /emergency/corridor/close`), siguiente slice de Fase 3 tras el cronograma reordenado: avisa `corridor:closed` a todos los candidatos alertados durante el traslado (set nuevo en Redis, `corridor:alerted:{ambulanceDriverId}`, TTL espejo de la ruta activa) y libera la ruta (`RouteSessionService.clear`, reusada, no duplicada). `expired` sigue sin código propio — vence solo por TTL si nadie cierra a mano; sin job de barrido todavía, sin evidencia de que la espera silenciosa de 4h sea un problema real. Verificado con Redis real, 11/11 casos (incluye que el cooldown de alertas de ADR-0013 sigue intacto y que cerrar dos veces no falla). Buffer dinámico, estados intermedios y severidad del corredor siguen diferidos — requieren números de producto que el fundador no ha dado. | `docs/decisions/ADR-0020-emergency-corridor-closure.md`, `backend/src/modules/emergency-corridor/` |
| 2026-08-19 | Buffer dinámico por velocidad + severidad del corredor: el fundador delegó explícitamente los números ("la decisión se la dejo a usted, tome la mejor"). Radio fijo de 200m reemplazado por `clamp(150 + velocidad_mps × 8, 150, 400)` metros usando la velocidad real de la ambulancia; cada candidato/alerta ahora trae `severity` (`info`/`warning`/`critical`) calculada como fracción del buffer del momento (25%/60%), no de metros fijos — conectada también al frontend (`EmergenciaScreen.tsx`, insignia de color). Números documentados como ajustables con evidencia real, no definitivos. Verificado con Redis real + fixture de ruta real (mismo decodificador de producción), 16/16 casos, incluye velocidad de GPS con ruido confirmando el tope de 400m. `typecheck`/`lint`/`build` limpios en backend y frontend (build de producción completo). **Nota:** el fixture de verificación de este día quedó desactualizado por el hallazgo de ADR-0022 (ver fila siguiente) — la fórmula y los números no cambiaron, solo el fixture de prueba. | `docs/decisions/ADR-0021-corridor-dynamic-buffer-severity.md`, `backend/src/modules/emergency-corridor/`, `proyecto-mensajeria/src/components/copiloto/EmergenciaScreen.tsx`, `proyecto-mensajeria/src/hooks/useCopilotoRealtime.ts` |
| 2026-08-19 | Primer slice del motor de simulación (Fase 4 del roadmap, escenario 1 "una ambulancia / 10 vehículos"): `SimulationEngineService` alimenta posiciones GPS sintéticas a los servicios REALES del Conflict Engine (no reimplementa la lógica) — si el Conflict Engine cambia, el simulador prueba el código nuevo automáticamente. Construir el escenario reveló un bug real en `EmergencyCorridorService.sampleAhead`: muestreaba por índice de punto crudo del polyline, no por distancia real, dejando huecos reales sin cubrir en rutas con pocos waypoints — un candidato a mitad de ruta nunca era detectado aunque estuviera dentro del buffer. Corregido con `pointAtDistanceAlongPath` (nueva utilidad compartida en `common/geo/interpolate.ts`), muestreo cada 100m reales sin importar la densidad de puntos de entrada. Efecto colateral honesto: esto también corrigió la semántica de `distanceMeters`/`severity` de cada candidato (antes reflejaba, por accidente, distancia al origen/destino; ahora refleja correctamente distancia al punto más cercano del corredor real) — desactualizando el fixture de severidad de ADR-0021, reemplazado por uno con offsets laterales reales. Verificado con Redis real: 18/18 casos del escenario completo (incluye determinismo — misma corrida dos veces, mismo patrón de detección) + 4/4 del fixture de severidad corregido. `typecheck`/`lint`/`build` limpios. | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `backend/src/modules/simulation/`, `backend/src/common/geo/interpolate.ts` |
| 2026-08-19 | Unificación de "Notas" (pestaña principal, 100% local/mock) y "Recordatorios" (sub-pestaña de Copiloto, backend real pero solo geolocalizado) en una sola sección, a pedido del fundador. Se extendió `location_reminders` (REUSE, no tabla paralela) con `kind` (`"location"`\|`"note"`), `title`, `is_task`, `completed_at`, `archived_at`, y se relajó `latitude`/`longitude` a nullable con un constraint nuevo que exige coordenadas solo para `kind: "location"` — probado explícitamente que la base de datos lo rechaza si falta. `listPendingForCache()` (hot path de geofence) filtra `kind = "location"` explícitamente, verificado que una nota nunca entra a ese cálculo. Hallazgo de auditoría: el tool de voz `create_location_reminder` (ADR-0016) ya cubría el ejemplo exacto del fundador (recordatorio geolocalizado por voz) — una nota vieja en `MISSING_CAPABILITIES.md` lo daba por "futuro", corregido. Frontend: `useReminders.ts` + `RemindersTab`/`RemindersListScreen`/`RemindersEditorScreen` reemplazan a `useNotes`/`useLocationReminders` y sus pantallas (todas eliminadas); la sub-pestaña "Recordatorios" de Copiloto se quitó. Fuera de alcance: recordatorios por hora fija (necesita BullMQ, sin construir) y adjuntar audio real a una nota. Verificado con migración aplicada al proyecto Supabase vivo, inserts/updates/deletes de prueba reales contra el esquema (limpiados, sin dejar datos), y `typecheck`/`lint`/`build` limpios en backend y frontend (build de producción completo). No se probó el servidor NestJS de punta a punta por falta de `SUPABASE_SERVICE_ROLE_KEY` local (mismo límite de siempre). | `docs/decisions/ADR-0023-unified-notes-and-reminders.md`, `backend/src/modules/location-reminders/`, `proyecto-mensajeria/src/hooks/useReminders.ts`, `proyecto-mensajeria/src/components/reminders/` |
| 2026-08-19 | Notas de voz reales en la mensajería (antes 100% decorativas: sin permiso de micrófono, sin grabación real, `Math.random()` simulaba la onda, y el reproductor no reproducía nada). Hallazgo de REUSE: el esquema original (ADR-0001) ya tenía `messages.type: "voice"`, columnas de media/waveform, y el bucket privado `voice-notes` con RLS aplicada — cero migraciones nuevas. Se construyó `useVoiceRecorder.ts` (hook compartido, grabación real vía `MediaRecorder`+`AnalyserNode`, onda por RMS real), subida real a Storage en la ruta exacta que exige la política RLS (`chat/{chatId}/...`), inserción real en `messages`, patrón optimista+reconcile (igual que texto), y reproductor `<audio>` real con URL firmada resuelta bajo demanda al tocar play. Verificado con `typecheck`/`lint`/`build` limpios; grabación/reproducción en navegador real queda pendiente de prueba manual del fundador (sandbox sin dispositivo de audio). | `docs/decisions/ADR-0024-real-voice-notes-messaging.md`, `proyecto-mensajeria/src/hooks/useVoiceRecorder.ts`, `proyecto-mensajeria/src/lib/actions/chats.ts`, `proyecto-mensajeria/src/components/chats/VoiceNotePlayer.tsx` |
| 2026-08-19 | Ubicación real (puntual y en vivo) en la mensajería — antes `MOCK_CURRENT_LOCATION` era una coordenada fija y la "ubicación en vivo" nunca recibía posiciones nuevas. Hallazgo de REUSE: el esquema original (ADR-0001) ya tenía `location_shares` con RLS aplicada (insert/select/update por remitente y participantes); único gap real: la tabla no estaba en la publicación `supabase_realtime` — se agregó con una migración de una sola línea. Se construyó lectura real del GPS (`getRealCurrentPosition`), etiqueta de dirección vía el backend real de geocoding inversa (ADR-0010), inserción real (`messages` + `location_shares`), y para "en vivo": `watchPosition` real con throttle a ~20s, temporizador real de 15min/1h/8h, y suscripción Realtime nueva a `location_shares` (INSERT+UPDATE) para que el mapa del destinatario se mueva en vivo. Verificado con simulación transaccional de RLS (revertida, 0 filas dejadas): remitente inserta/actualiza OK, un tercero no-remitente bloqueado al intentar actualizar, no-participante bloqueado al leer. `typecheck`/`lint`/`build` limpios (build de producción completo). Pendiente: prueba manual del fundador con GPS real de navegador/Android (sandbox sin sensor de ubicación). | `docs/decisions/ADR-0025-real-location-sharing-messaging.md`, `proyecto-mensajeria/src/lib/actions/chats.ts`, `proyecto-mensajeria/src/hooks/useChats.ts` |
| 2026-08-19 | Diseño (sin código) del protocolo de cifrado de extremo a extremo, a pedido explícito del fundador ("la idea es la definición del protocolo... no nos interesa leer las conversaciones de las personas"). Se evaluaron dos caminos: Signal Protocol completo (X3DH+Double Ratchet, forward secrecy perfecta pero complejo de integrar bien en una app web) vs. `libsodium.js` (`crypto_box`, X25519, cifrado autenticado de clave pública por mensaje, sin forward secrecy per-mensaje pero con la propiedad central pedida: nadie del lado del servidor puede leer el contenido). Se eligió la segunda para v1 por menor superficie de implementación sobre una librería madura y auditada. Diseño cubre: generación de claves en el dispositivo (nunca en el servidor), tabla nueva `user_encryption_keys` (clave pública, segura de exponer), almacenamiento de la clave privada (IndexedDB interino, migra a almacenamiento seguro nativo cuando exista empaquetado Android), columnas nuevas en `messages` para ciphertext/nonce, y alcance explícito de v1 (solo texto, solo chats 1 a 1, sin multi-dispositivo ni backup de claves). Se documentó una tensión real y todavía sin decidir: las tools de asistente `read_messages`/`send_message` (ADR-0018) no podrán seguir leyendo contenido una vez cifrado — la lectura por voz de un mensaje recibido tendría que pasar a ser 100% client-side. | `docs/decisions/ADR-0026-e2e-encryption-protocol-design.md` |
| 2026-08-19 | Diseño (sin código) de nombre personalizado del asistente + reconocimiento automático de "wake word" (ej. "Estefa", análogo a "Oye Siri"), a pedido del fundador. Auditoría encontró un bloqueo real para incluso el campo simple: `updateProfile` en `useProfile.ts` es 100% local/mock — nunca escribe en la fila real de `profiles`, así que cualquier campo nuevo (incluido `assistant_name`) se perdería al recargar; se documenta como prerequisito propio, no se resolvió en este ADR para no tocar un mecanismo no relacionado sin su propio slice. Wake word real es un problema técnico distinto y más grande que el campo de texto: requiere un motor de detección de palabra clave corriendo LOCALMENTE en el dispositivo (nunca streaming continuo a un servidor, por costo/batería y por la regla de seguridad del proyecto de no escuchar en segundo plano) — se recomienda evaluar Picovoice Porcupine (wake word personalizado real, soporte Android/Web) como decisión de proveedor pendiente del fundador, mismo tipo de paso que Google Maps Platform. Diseño deja explícito el orden de dependencias: prerequisito de persistencia de perfil → proveedor de wake word → API de voz Realtime/STT (ya bloqueada) → implementación. | `docs/decisions/ADR-0027-assistant-custom-name-and-wake-word-design.md` |
| 2026-08-19 (mismo día, actualización) | Precios reales investigados para wake word (Picovoice: gratis solo 1 usuario/mes, ~US$6,000/año para 100 usuarios, ~US$30,000/año plan empresarial; alternativa gratis sin límite de usuarios: `openWakeWord`, open-source, sin soporte oficial de Android) y diseño de detección automática de "¿sigue manejando?" por velocidad real de GPS (pausa el wake word solo si la velocidad se mantiene baja varios minutos seguidos, ya usando el mismo dato de velocidad que el buffer dinámico del corredor de emergencia, ADR-0021) — ambos a pedido del fundador. **Cambio de alcance, mismo día:** el fundador pidió suspender la personalización por nombre para no trabarse en esa decisión, y en su lugar priorizar identificación de QUIÉN habla — que un comando como "envía este mensaje" solo se ejecute si es la voz del dueño real de la cuenta, no un tercero o ruido de fondo. Se agregó diseño de verificación de hablante (tecnología distinta al wake word): investigado y confirmado activo **Picovoice Eagle** (reconocimiento de hablante 100% en el dispositivo, enrolamiento en segundos, verificación desde un solo comando, integra directo con Porcupine); precio de Eagle no publicado, requiere contactar ventas. Se documentó que la confirmación explícita antes de acciones sensibles (ya existente desde ADR-0016/0018) ya mitiga parte del riesgo hoy, sin código nuevo. | `docs/decisions/ADR-0027-assistant-custom-name-and-wake-word-design.md` |
| 2026-08-20 | Corregido bug real descubierto durante la auditoría de ADR-0027: `updateProfile` (`useProfile.ts`) nunca persistía en Supabase — nombre visible, "acerca de" y foto de perfil se perdían al recargar la app. Cero migraciones nuevas (REUSE puro): `profiles` ya tenía la política RLS `profiles_update_self` y el bucket público `avatars` ya tenía sus políticas de dueño, ambas desde ADR-0001. Se construyó `updateProfileRemote`/`uploadAndSaveAvatar` (mismo patrón optimista+reconciliación que mensajes de texto y notas de voz) y se conectó `useProfile.ts`/`ProfileScreen.tsx`. Verificado con simulación transaccional de RLS (revertida, 0 filas dejadas): usuario actualiza su propio perfil OK, intento de tocar el perfil de otra persona bloqueado con 0 filas afectadas. `typecheck`/`lint`/`build` limpios (build de producción completo). Pendiente: prueba manual del fundador subiendo una foto real desde el navegador (sandbox sin cámara/galería). | `docs/decisions/ADR-0028-real-profile-persistence.md`, `proyecto-mensajeria/src/lib/actions/profile.ts`, `proyecto-mensajeria/src/hooks/useProfile.ts` |
| 2026-08-26 | Presencia en línea, "escribiendo…" y "visto por última vez" reales — el fundador pidió continuar con los pendientes de más fácil a más difícil (dejando fuera, por ahora, el asistente de voz por falta de recursos). Auditoría encontró que `chat.activity` siempre valía `"idle"` en chats reales y que `useChats.ts` exponía `participants` como 100% `MOCK_PARTICIPANTS`, sin ninguna relación con datos reales. Cero migraciones nuevas (REUSE total): `profiles.last_seen_at`, `last_seen_visibility` y hasta la función `is_contact_of` ya existían desde ADR-0001, sin conectar. Se construyó un mapa real de `participants`, presencia en línea real (Supabase Realtime Presence), "escribiendo…" real (Broadcast efímero por chat, con throttle), y "visto por última vez" real (`touchLastSeen`, heartbeat) respetando por primera vez la privacidad de `last_seen_visibility` ("contacts" resuelto vía `is_contact_of`, ya que la RLS de `contacts` es privada y no se puede leer la libreta ajena directo). Verificado con simulación transaccional de RLS/RPC contra dos cuentas reales con una relación de contacto asimétrica real (confirmado que la regla no es "nos conocemos", es "¿el dueño del perfil me tiene a MÍ en su libreta?"). De paso corrigió un bug preexistente menor: `deleteChat`/`leaveGroup`/`deleteGroup` no hacían *spread* del estado anterior, perdiendo silenciosamente cualquier campo no listado a mano. `typecheck`/`lint`/`build` limpios (build de producción completo). Pendiente: prueba manual del fundador con dos sesiones de navegador reales abiertas a la vez (sandbox sin navegador). | `docs/decisions/ADR-0029-real-presence-typing-last-seen.md`, `proyecto-mensajeria/src/hooks/useChats.ts`, `proyecto-mensajeria/src/lib/actions/chats.ts`, `proyecto-mensajeria/src/lib/actions/profile.ts` |
| 2026-08-27 | Recordatorios de nota a hora fija con BullMQ (siguiente pendiente de la lista fácil→difícil tras ADR-0029). Requirió la primera migración real desde ADR-0025/0028/0029 (esas fueron REUSE puro): `location_reminders` no tenía columna de hora — se agregó `remind_at timestamptz null` con un constraint que la restringe a `kind: "note"`. Se construyó `NoteReminderSchedulerService` (encola/cancela jobs de BullMQ, `jobId = id del recordatorio`, vive en `LocationRemindersModule`) y `NoteReminderProcessor` (dispara el aviso vía `LocationBroadcastService.notify` sobre el namespace `/location` ya existente, vive en `LocationModule` por su dependencia de `LocationBroadcastService`) — ambos conectados por nombre de cola, no por import de Nest, evitando recrear el ciclo de módulos que ADR-0015 ya evitó a propósito. `markNoteReminderTriggered` (nuevo, mismo patrón idempotente que `markTriggered`) además de a `status='pending'` filtra por `completed_at IS NULL AND archived_at IS NULL`, para que una nota completada/archivada no dispare su aviso aunque el job no se haya cancelado a tiempo. Frontend: `useReminders.ts`/`RemindersEditorScreen.tsx` (selector de fecha/hora, autoguardado), `useCopilotoRealtime.ts`/`NotificacionesScreen.tsx` (nuevo evento en vivo `reminder:due`). Verificado: migración aplicada y confirmada contra el esquema vivo; mecánica de BullMQ (disparo tras delay, cancelación real evita el disparo, reprogramar usa el nuevo delay) probada 3/3 contra Redis real local (no mock, mismo `bullmq`/`ioredis` del backend); resolución cross-módulo de la cola confirmada leyendo el código fuente instalado de `@nestjs/bullmq`. `typecheck`/`lint`/`build` limpios en backend y frontend (build de producción completo en ambos). Pendiente, honesto: arrancar el backend real completo contra Supabase/Redis de producción no fue posible aquí (requiere secretos que este asistente nunca debe recibir por chat) — queda para prueba manual del fundador. Limitación de producto documentada: sin FCM/APNs (Fase 5), el aviso solo llega con la app abierta y el socket conectado. | `docs/decisions/ADR-0030-bullmq-fixed-time-note-reminders.md`, `backend/src/modules/location-reminders/`, `backend/src/modules/location/`, `proyecto-mensajeria/src/hooks/useReminders.ts`, `proyecto-mensajeria/src/hooks/useCopilotoRealtime.ts` |
| 2026-08-27 (2) | Fotos y documentos reales en la mensajería (siguiente pendiente de la lista fácil→difícil tras ADR-0030). Hallazgo de REUSE total (cero migraciones): el esquema original ya tenía `messages.type` con `"image"`/`"document"`, las columnas genéricas de media, y un bucket privado `chat-media` con RLS ya aplicada — nada conectado, el menú de adjuntar mandaba un nombre de archivo inventado sin subir nada real. Se construyeron `uploadChatMedia`/`insertAttachmentMessage`/`resolveChatMediaUrl` (mismo patrón optimista+reconciliación que las notas de voz de ADR-0024), tres `<input type="file">` reales en el composer (cámara/galería/documento), y en la burbuja: foto real renderizada vía URL firmada y documento real abrible/descargable (antes ambos eran solo un ícono estático). **Bug real encontrado y corregido antes de tocar producción:** `chat-media` espera la ruta `{chatId}/...`, sin el prefijo `chat/` que sí usa `voice-notes` — confirmado leyendo la policy real y verificado con una simulación que reprodujo el error exacto de Postgres (`22P02`, castear el string `"chat"` a uuid) antes de corregirlo. Verificado con simulación transaccional de RLS real contra el chat y las dos cuentas reales ya usadas en ADR-0029 (4/4 casos, revertida, 0 filas). `typecheck`/`lint`/`build` limpios (build de producción completo). Pendiente, honesto: sin prueba de subida HTTP real (requiere JWT real de usuario) ni de cámara/galería en navegador real (sandbox sin esos dispositivos). | `docs/decisions/ADR-0031-real-photos-and-documents-messaging.md`, `proyecto-mensajeria/src/lib/actions/chats.ts`, `proyecto-mensajeria/src/hooks/useChats.ts`, `proyecto-mensajeria/src/components/chats/MessageComposer.tsx`, `proyecto-mensajeria/src/components/chats/MessageBubble.tsx`, `proyecto-mensajeria/src/components/chats/ChatThreadScreen.tsx` |
| 2026-08-27 (3) | Rate limiting real en el backend (siguiente pendiente de la lista fácil→difícil tras ADR-0031). Ningún endpoint tenía límite de peticiones. Se instaló `@nestjs/throttler` con storage en el mismo Redis compartido de siempre (`@nest-lab/throttler-storage-redis`, no el almacenamiento en memoria por defecto, que se desincroniza entre instancias) — límite por defecto 60/min por usuario, 20/min en `NavigationController` (dinero real en Google Maps) y `AssistantController` (ejecuta tools), 5/min en `SimulationController` (cómputo pesado), sin límite en `/health`. Se agrupa por usuario (`sub` del JWT, decodificado sin verificar firma solo como clave de bucketing — `SupabaseAuthGuard` sigue siendo quien verifica de verdad), no por IP, a propósito: el piloto controlado (Fase 8) prevé muchos conductores reales compartiendo la misma IP por NAT de operador celular, y limitar por IP los penalizaría entre sí. Detalle técnico real que forzó el diseño: el guard de rate limit corre global, ANTES que `SupabaseAuthGuard` (de controller), así que no existía todavía `request.userId` cuando el guard necesita decidir la clave. Verificado contra un Redis real local con el paquete real instalado (7/7 casos: bloqueo/desbloqueo real por Redis + decodificación de JWT). `typecheck`/`lint`/`build` limpios (1 warning esperado y aceptado, heredado de la firma de la librería). Pendiente, honesto: no se probó contra el backend arrancado de verdad (requiere secretos que este asistente nunca debe recibir por chat). | `docs/decisions/ADR-0032-rate-limiting.md`, `backend/src/common/rate-limit/`, `backend/src/common/guards/user-aware-throttler.guard.ts`, `backend/src/app.module.ts`, `backend/src/modules/navigation/navigation.controller.ts`, `backend/src/modules/assistant/assistant.controller.ts`, `backend/src/modules/simulation/simulation.controller.ts`, `backend/src/modules/health/health.controller.ts` |
| 2026-08-27 (4) | Web Push real, primer slice (siguiente pendiente tras ADR-0032; se investigó primero el "bug pausado de chulos" del cronograma viejo — ya estaba resuelto de verdad en el código, `hydrateMessageStatuses`/`markChatReadRemote`, `TECHNICAL_DEBT.md` no existe en este repo, referencia obsoleta). Auditoría antes de construir: `proyecto-mensajeria` es 100% web (Vite, sin Capacitor todavía), así que "notificaciones push" hoy significa Web Push (VAPID), no FCM/APNs nativos — se revisará junto con el empaquetado Android cuando exista. Se construyó `WebPushService` (adapter, `web-push`) + tabla real `push_subscriptions` (migración nueva, RLS dueño-únicamente) conectados como segundo canal (junto al socket de siempre) en `NoteReminderProcessor` (ADR-0030) — respeta `profiles.notification_settings`, que de paso se hizo real (antes solo vivía en memoria de la sesión, se "olvidaba" al recargar, mismo síntoma que nombre/"acerca de" antes de ADR-0028). Frontend: service worker real (`public/sw.js`, sin cachear nada, evita duplicar el aviso si ya hay una pestaña enfocada viéndolo en vivo por Realtime) + interruptor maestro real en Ajustes → Notificaciones que refleja `Notification.permission` de verdad. Fuera de alcance a propósito: push de mensajes de chat nuevos (Supabase-directo, sin pasar por este backend — necesitaría un trigger/Edge Function, decisión aparte) y alertas del Emergency Corridor (decisión de producto sobre si deben poder silenciarse, no tomada aquí). Verificado: RLS real por simulación transaccional (5/5 casos) + `get_advisors` sin hallazgos nuevos; librería `web-push` real contra un servidor HTTPS local real con llave VAPID desechable (3/3 casos: éxito no lanza, 404/410 sí lanzan con el `statusCode` que dispara la poda de suscripciones vencidas); conversión de llave VAPID probada contra una llave real generada por la misma librería. `typecheck`/`lint`/`build` limpios en backend y frontend (incluyendo que `public/sw.js` queda en el build de producción). Pendiente, honesto: sin prueba de suscripción/entrega real desde un navegador real (requiere las llaves VAPID reales del fundador, que nunca deben pasar por este chat) — queda para prueba manual. | `docs/decisions/ADR-0033-web-push-notifications.md`, `backend/src/modules/push-notifications/`, `backend/src/modules/location/note-reminder.processor.ts`, `proyecto-mensajeria/public/sw.js`, `proyecto-mensajeria/src/lib/actions/push.ts`, `proyecto-mensajeria/src/components/profile/NotificationsScreen.tsx` |
| 2026-08-31 | Cambio de proveedor de voz para el asistente (ADR-0016 había dejado "OpenAI Realtime u otro" pendiente de provisión): el fundador pidió evaluar Gemini Live API en vez de OpenAI, ya con saldo real disponible en esa cuenta (arreglo de facturación de Google AI Studio ese mismo día, administración de cuenta, sin ADR propio). Investigado antes de decidir: Gemini Live API sí soporta function calling en tiempo real, mismo formato base de JSON Schema, y sale mucho más barato que OpenAI Realtime (~$0.005/min entrada vs. $32/1M tokens de audio de OpenAI) — con el pero real de que Gemini Live sigue en Preview. Con esa información, el fundador eligió Gemini. Se construyó `GeminiLiveService` (adapter nuevo dentro de `AssistantModule` ya existente, cero cambios al Tool Registry de ADR-0016 — provider-agnostic por diseño desde el inicio) con `toGeminiParameters()` para traducir nuestro JSON Schema al enum `Type` en mayúsculas que exige el SDK de Node (`@google/genai`), detalle real encontrado instalando el paquete y corriendo `tsc --noEmit` contra sus propios tipos, no contra la página de docs (que muestra un formato distinto). Alcance deliberado: texto todavía, no audio (sin frontend con micrófono construido), y `activate_emergency_corridor` siempre viaja con `confirmed: false` pase lo que pase por voz — la tool responde `needs_confirmation` y nunca ejecuta el efecto real, decisión de producto sobre cómo debe confirmarse por voz una acción de alto riesgo queda explícitamente sin tomar aquí. Verificado: tipos del SDK real contra el diseño (0 errores tras el conversor). Pendiente, honesto: `bun install`/`typecheck`/`build` reales y una sesión Live real contra la API — requieren correr en la máquina del fundador y su `GEMINI_API_KEY` real, que nunca debe pasar por este chat. | `docs/decisions/ADR-0034-gemini-live-voice-integration.md`, `backend/src/modules/assistant/gemini-live.service.ts` |
| 2026-08-31 (2) | Cierre real de ADR-0034: sesión Gemini Live confirmada de punta a punta contra la API real, con dos bugs reales encontrados y corregidos en el camino (no una prueba limpia a la primera). (1) El modelo real disponible para la cuenta (`gemini-3.1-flash-live-preview`, encontrado corriendo `scripts/list-gemini-live-models.ts` contra `ai.models.list()` porque el nombre del ejemplo del SDK, `gemini-live-2.5-flash-preview`, no existe para esta cuenta — cierre de socket real 1008) rechaza `responseModalities: TEXT` (cierre real 1007) — corregido usando `Modality.AUDIO` + `outputAudioTranscription`, leyendo el texto de `serverContent.outputTranscription.text` en vez del getter `message.text` (que da vacío sin partes de texto en `modelTurn`). (2) Race condition real (TDZ): el SDK invoca `onmessage` de forma síncrona durante `connect()`, antes de que la promesa resuelva — con `const session = await connect()` eso producía `ReferenceError: Cannot access 'session' before initialization` real; corregido con `let session` declarada antes del `await` y `handleMessage()` tolerando `session` indefinida en los mensajes de handshake tempranos (que nunca traen `functionCall`). Con ambos corregidos, `scripts/verify-gemini-live.ts` (clase real `GeminiLiveService` + tool real `list_vehicles` → Supabase real) confirmó el flujo completo: Gemini llamó la tool de verdad y respondió "Tienes una moto registrada con la placa URU89E" — mismo vehículo real confirmado antes por REST en ADR-0016. `GEMINI_LIVE_MODEL` por defecto actualizado en `env.validation.ts`/`.env.example` al modelo real que sí funciona. | `docs/decisions/ADR-0034-gemini-live-voice-integration.md`, `backend/src/modules/assistant/gemini-live.service.ts`, `backend/src/config/env.validation.ts`, `backend/.env.example` |
| 2026-08-31 (3) | Segundo slice de ADR-0034: audio real bidireccional, confirmado con micrófono real del fundador (no solo texto por script). Backend: `GeminiLiveService.sendAudioChunk()`/`endAudioStream()` (manda audio real vía `session.sendRealtimeInput`) y `onAudio` (expone los chunks reales de audio de salida); nuevo `AssistantVoiceGateway` (`/assistant-voice`) expone todo por WebSocket, REUSE del mismo patrón de auth que `LocationGateway`. Frontend (`proyecto-mensajeria`): `lib/audio/pcm.ts` (downsample del micrófono a 16kHz + conversión PCM real), `useGeminiVoiceSession.ts` (captura con `ScriptProcessorNode`, reproducción con `AudioBufferSourceNode` encadenado, REUSE del patrón de conexión de `useCopilotoRealtime`), y `AsistenteVozScreen.tsx` como cuarta sub-pestaña ("Voz") de Copiloto. Dos bugs reales más encontrados y corregidos: (1) race condition a nivel de WebSocket — el `connect` del cliente dispara antes de que el servidor termine de abrir la sesión de Gemini, un mensaje mandado justo al conectar se perdía en silencio; corregido con un evento explícito `voice:ready` que el cliente espera. (2) TypeScript 5.7+ exige `Float32Array<ArrayBuffer>` explícito para `AudioBuffer.copyToChannel` (el tipo sin anotar se ensancha a `ArrayBufferLike`). Verificado real: el fundador tocó el micrófono en la app, preguntó en voz "¿qué vehículos tengo registrados?" y escuchó la respuesta real en voz de Gemini ("Tienes una moto registrada con placas URU89E") — primera conversación de voz de punta a punta de la plataforma. `typecheck`/`lint` de `proyecto-mensajeria` limpios (solo warnings preexistentes, no relacionados). Pendiente, honesto: "Modo conducción" manos-libres de verdad (hoy hay que tocar y sostener la pantalla), downsampling naive sin filtro anti-aliasing, `ScriptProcessorNode` en vez de `AudioWorklet`, sin interrupciones/VAD del lado del cliente, sin prueba en móvil real ni manejando. | `docs/decisions/ADR-0034-gemini-live-voice-integration.md`, `backend/src/modules/assistant/gemini-live.service.ts`, `backend/src/modules/assistant/assistant-voice.gateway.ts`, `proyecto-mensajeria/src/lib/audio/pcm.ts`, `proyecto-mensajeria/src/hooks/useGeminiVoiceSession.ts`, `proyecto-mensajeria/src/components/copiloto/AsistenteVozScreen.tsx`, `proyecto-mensajeria/src/components/copiloto/CopilotoTab.tsx` |
| 2026-09-01 | Ronda de bugs reales encontrados por el fundador probando el asistente de voz ya con conversaciones reales (no una prueba limpia — cuatro fallas distintas). (1) **El más serio**: `GeminiLiveService.executeCall()` mandaba `confirmed: false` fijo para TODAS las tools con `requiresConfirmation`, no solo `activate_emergency_corridor` — cualquier acción confirmable (ej. `send_message`) quedaba en loop de "¿confirmas?" sin ejecutarse nunca de verdad. Corregido declarando `confirmed` en el schema que ve Gemini solo para tools confirmables, y leyendo `call.args?.["confirmed"] === true` en la segunda llamada — con `activate_emergency_corridor` explícitamente excluido de la excepción (sigue forzado a `false`, decisión de producto sin tomar, sin cambios). (2) Búsqueda de contacto por nombre (`MessagingService.resolveChatByContactName`) usaba `.ilike()` de Postgres, que ignora mayúsculas pero NO tildes — "José Luis" no encontraba "Jose luis"; además solo hacía match en una dirección, así que un contacto guardado como "Jose" no aparecía al pedir "José Luis" (nombre completo real de esa persona). Corregido trayendo la libreta completa (siempre chica) y comparando en JS sin tildes, en ambas direcciones y por palabra suelta compartida. (3) Geocoding sin ningún sesgo de ubicación devolvía resultados de otro país/continente con el mismo nombre — "Belén" resolvía a Bethlehem (Medio Oriente) y "Buenos Aires" a la capital de Argentina, en vez de los barrios reales de Medellín. Corregido con filtro duro `components=country:CO` (la plataforma solo opera en Colombia) + sesgo suave `bounds` alrededor de la ubicación real conocida del usuario (`LocationStateService`, mismo patrón ya usado por `activate_emergency_corridor`) — a pedido explícito del fundador: "la app debe pedir mi ubicación en tiempo real para que funcione muy bien". (4) Geofence de 300m fijo (heredado de ADR-0015) dejaba fuera partes reales de un barrio completo pedido como recordatorio ("Buenos Aires") — se agregó `suggestedRadiusMeters` (radio estimado con Haversine desde el centro a la esquina del `viewport` real que devuelve Google cuando el resultado es un área, no un punto — acotado 300-1500m), usado como default cuando el usuario no pide un radio explícito. De paso: nueva tool de voz `create_note_reminder` (recordatorios de nota a hora fija, no solo por ubicación — ver ADR-0034). Pendiente, honesto: (2)/(3)/(4) corregidos y sincronizados a la máquina del fundador, sin reconfirmación por voz todavía en esta sesión (`device_bash` no disponible para correr los tests desde aquí) — (1) sí confirmado real por voz. | `docs/decisions/ADR-0034-gemini-live-voice-integration.md`, `backend/src/modules/assistant/gemini-live.service.ts`, `backend/src/modules/messaging/messaging.service.ts`, `backend/src/modules/navigation/providers/geocoding-provider.interface.ts`, `backend/src/modules/navigation/providers/google-geocoding.provider.ts`, `backend/src/modules/assistant/tools/create-location-reminder.tool.ts`, `backend/src/modules/assistant/tools/create-note-reminder.tool.ts` |
| 2026-09-01 (2) | Recordatorios: borrado permanente real para CUALQUIER tipo (antes `LocationRemindersService.remove()` filtraba `.eq("kind","note")` — un recordatorio de ubicación solo se podía "cancelar", quedaba visible para siempre como "Cancelado"; a pedido explícito del fundador: botón real de "borrar del todo"). El controller ahora también invalida `ReminderCacheService` al borrar (antes faltaba — un pendiente borrado sin esto seguía disparando el geofence desde una copia vieja en Redis). De paso, dos bugs UX reales encontrados y corregidos: el botón "+" de notas no creaba nada (validación de "mensaje requerido" aplicaba también a notas, no solo a recordatorios de ubicación, tanto en frontend como backend — corregida para aplicar solo a `kind==="location"`), y el editor de notas no tenía botón "Guardar" explícito (solo autosave silencioso al perder foco) — agregado, con el guardado esperado (`await`) antes de volver a la lista, para no pisar el texto recién guardado con el chequeo de "nota vacía" del `onBack`. | `backend/src/modules/location-reminders/location-reminders.service.ts`, `backend/src/modules/location-reminders/location-reminders.controller.ts`, `proyecto-mensajeria/src/hooks/useReminders.ts`, `proyecto-mensajeria/src/components/reminders/RemindersListScreen.tsx`, `proyecto-mensajeria/src/components/reminders/RemindersEditorScreen.tsx`, `proyecto-mensajeria/src/components/reminders/RemindersTab.tsx` |
| 2026-09-01 (3) | Fase 6 (Modo conducción manos-libres): barge-in real — Gemini Live manda `serverContent.interrupted:true` cuando el conductor empieza a hablar mientras el asistente todavía está respondiendo; esta señal ya existía en la API pero no se leía en ningún lado, así que la respuesta vieja seguía sonando encima de la nueva. Ahora se lee en `GeminiLiveService.handleMessage()` (nuevo callback `onInterrupted`), se reenvía por `AssistantVoiceGateway` (`voice:interrupted`), y el frontend (`useGeminiVoiceSession`) guarda cada `AudioBufferSourceNode` agendado y los corta todos (`.stop()`, válido en fuentes sonando o agendadas a futuro) al recibir la señal. Confirmado real por el fundador el mismo día: "funciono perfecto". | `docs/decisions/ADR-0034-gemini-live-voice-integration.md`, `backend/src/modules/assistant/gemini-live.service.ts`, `backend/src/modules/assistant/assistant-voice.gateway.ts`, `proyecto-mensajeria/src/hooks/useGeminiVoiceSession.ts` |
| 2026-09-01 (4) | ADR-0035: push real para mensajes de chat nuevos — cierra el gap que ADR-0033 había dejado explícitamente fuera de alcance (los mensajes se insertan directo en Supabase, ADR-0018, nunca pasan por el backend NestJS, que además hoy no tiene URL pública). Trigger real de Postgres (`public.notify_new_message`, `pg_net`, asíncrono — nunca bloquea el guardado del mensaje) que llama a una Edge Function nueva (`notify-new-message`, Deno, en la infraestructura de Supabase) en cada `INSERT` real sobre `messages`. Seguridad en dos capas: el trigger manda SOLO el `message_id` (la función vuelve a leer todo de la base real, nunca confía en el payload — mismo criterio que `MessagingService.assertParticipant`), y un secreto compartido generado por la base de datos misma (nadie lo vio en texto plano) protege la función. Respeta `chat_participants.is_muted`/`muted_until` y `notification_settings.messages` — mismas columnas reales ya existentes, sin inventar nada nuevo. Aplicado y desplegado con permiso explícito del fundador; verificado real con un `INSERT` de prueba (borrado enseguida) confirmando la cadena completa Postgres → pg_net → Edge Function → validación de secreto → relectura del mensaje real (`200 "ok"`). Pendiente, honesto: faltan las llaves VAPID como secrets de la Edge Function (el fundador debe configurarlas, nunca pasan por chat) y una prueba real con navegador/segunda cuenta. | `docs/decisions/ADR-0035-chat-message-push-notifications.md`, `supabase/migrations/20260901000000_push_new_message_webhook.sql`, `supabase/functions/notify-new-message/index.ts` |
| 2026-09-01 (5) | Cierre real del Emergency Corridor (ADR-0020), a pedido explícito del fundador ("termina con el corredor de emergencia la ambulancia"). Dos gaps honestos que quedaban documentados: (1) `expired` sin código propio — un corredor que quedaba "colgado" (app cerrada, viaje abandonado) sin que nadie llamara a `/close` nunca avisaba "ya pasó" a quien se alcanzó a alertar, solo dejaba de mandar alertas nuevas. Se construyó un barrido real (`AlertPolicyService.sweepExpired`, `CorridorExpirySweepProcessor`) sobre la cola `EMERGENCY_ALERTS` (reservada desde ADR-0008, sin processor hasta hoy) — repetible cada 15 minutos, revisa un set nuevo de Redis (`corridor:active-ambulances`) y cierra con `reason: "expired"` cualquier corredor cuya ruta (`RouteSessionService`) ya venció. (2) No existía ningún cliente que llamara a `POST /emergency/corridor/close` — se agregaron los botones reales "Llegué / Finalizar" y "Cancelar traslado" en `EmergenciaScreen.tsx` (conductor de ambulancia) y un listener real de `corridor:closed` que muestra "Ya pasó, gracias" al lado del posible afectado. De paso, auditoría encontró un gap real y honesto sin resolver: hoy no hay ningún endpoint/pantalla de administrador para verificar qué vehículo es ambulancia — solo SQL/MCP directo contra `emergency_vehicles` (ADR-0006) — operable así para el volumen del piloto (5-10 ambulancias), decisión de construir un panel aparte no tomada aquí. Verificado con Redis real local (9/9 casos, mismo método que ADR-0008/0012/0013/0020: dos ambulancias activas, el barrido detecta solo a la expirada, deja intacta a la que sigue activa, limpia su set de alertados, un segundo barrido no la re-detecta, y el cierre explícito `completed` original sigue funcionando sin regresión). `typecheck`/`lint`/`build` reales pendientes en la máquina del fundador. | `docs/decisions/ADR-0020-emergency-corridor-closure.md`, `backend/src/modules/emergency-corridor/{alert-policy.service.ts,emergency-corridor.types.ts,emergency-corridor.module.ts,corridor-expiry-sweep.processor.ts}`, `backend/src/common/queue/queue-names.ts`, `proyecto-mensajeria/src/hooks/useCopilotoRealtime.ts`, `proyecto-mensajeria/src/components/copiloto/EmergenciaScreen.tsx` |
| 2026-09-01 (6) | Panel real de administrador para el Emergency Corridor (ADR-0006), a pedido explícito del fundador: hasta ahora, verificar qué vehículo es ambulancia solo se podía hacer por SQL/MCP directo. Nuevo `AdminGuard` (corre después de `SupabaseAuthGuard`) compara el `user_id` real del JWT contra `ADMIN_USER_ID` (una sola variable de entorno — la cuenta del fundador, no un sistema de roles genérico, sin evidencia de que haga falta más de un administrador). Endpoints nuevos `emergency/admin/vehicles` (GET lista, POST verifica/asigna por teléfono del conductor — no por UUID interno, PATCH activa/desactiva sin borrar historial) en `EmergencyVehiclesService`/`EmergencyAdminController`. Pantalla nueva `AdminAmbulanciasScreen.tsx` (5ª sub-pestaña de Copiloto, "Admin") con formulario de asignación y lista de ambulancias asignadas. Detalle real encontrado auditando antes de construir: `driver_id` sí tiene un índice único real en el esquema (`pg_indexes`), aunque no aparece en `pg_constraint` por no ser un `ADD CONSTRAINT` con nombre — confirmado contra el esquema real antes de escribir el `upsert`, no asumido. `vehicle_type` se fija siempre server-side a `"ambulance"` (la única que acepta la constraint `CHECK` real de la tabla). Verificado real contra el proyecto Supabase vivo: resolución de teléfono real → `driver_id` real → upsert real → fila confirmada → revertido (0 filas de prueba dejadas), mismo método de simulación transaccional del resto del proyecto. `typecheck`/`lint`/`build` reales pendientes en la máquina del fundador. | `docs/decisions/ADR-0006-emergency-corridor.md`, `backend/src/common/guards/admin.guard.ts`, `backend/src/modules/emergency/{emergency-admin.controller.ts,emergency-vehicles.service.ts,emergency.module.ts}`, `backend/src/config/env.validation.ts`, `backend/.env.example`, `proyecto-mensajeria/src/hooks/useEmergencyAdmin.ts`, `proyecto-mensajeria/src/components/copiloto/{AdminAmbulanciasScreen.tsx,CopilotoTab.tsx}` |
| 2026-09-01 (7) | Bug real (ADR-0033): el fundador configuró por primera vez las llaves VAPID reales (en Supabase Edge Function secrets y en `backend/.env`) y el backend truena al arrancar: `TypeError: Cannot read properties of undefined (reading 'setVapidDetails')` en `new WebPushService` — nunca se había ejecutado esa rama de código porque nunca antes hubo llaves configuradas. Causa raíz: `backend/tsconfig.json` tenía `allowSyntheticDefaultImports: true` pero NO `esModuleInterop: true` (trampa clásica de TS — la primera solo afecta el chequeo de tipos, no el JS emitido); sin la segunda, `import webpush from "web-push"` compila a una referencia `.default` que en un paquete CommonJS plano como `web-push` queda `undefined` en tiempo real. Corregido agregando `esModuleInterop: true` (una línea, sin romper nada existente). Confirmado real: el fundador reinició el backend y arrancó limpio. | `docs/decisions/ADR-0033-web-push-notifications.md`, `backend/tsconfig.json` |
| 2026-09-01 (8) | Fase 4 (Simulación) desbloqueada al cerrarse Fase 3 el mismo día — Escenario 2 del roadmap ("una ambulancia / 100 vehículos", ADR-0022): a diferencia del escenario 1 (correctitud con una mezcla chica), este valida escala real. `LocationStateService.findNearby` usa Redis GEOSEARCH por radio, no un escaneo de todos los vehículos registrados, así que el costo depende de cuántos caen DENTRO del buffer, no del total en el sistema — el escenario pone 70 vehículos realmente cerca del corredor (mezcla completa de severidades, simulando tráfico denso) y 30 lejos como control negativo, mismo largo/velocidad de ruta que el escenario 1 para comparar directo. Verificado con Redis real local, clases reales instanciadas a mano (sin reimplementar el Conflict Engine), `bun run`: 8/8 casos — ningún control lejano alertado, 53/70 vehículos cercanos alertados en algún paso, las 3 severidades aparecen, determinismo confirmado (dos corridas = mismo patrón), corredor cerrado sin residuos en Redis, y latencia real `avgFindCandidatesLatencyMs` de 3.22ms (10 vehículos) a 8.73ms (100 vehículos) — 2.7x al 10x de vehículos, confirma que GEOSEARCH por radio escala bien para este volumen. `typecheck`/`lint` del backend completo limpios (0 errores). | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `backend/src/modules/simulation/scenarios/scenario-2-single-ambulance-100-vehicles.ts`, `backend/src/modules/simulation/simulation.controller.ts` |
| 2026-09-01 (9) | Escenario 3 del roadmap ("tres ambulancias simultáneas", ADR-0022): a diferencia de los escenarios 1 (correctitud) y 2 (escala), este valida aislamiento — que tres corredores activos al mismo tiempo no se contaminen entre sí en Redis. Rutas paralelas que no se cruzan a propósito (corredores que sí se cruzan geométricamente queda para el escenario 12). Pieza nueva: `SimulationEngineService.runConcurrent(scenarios)` (`Promise.all` real sobre el mismo `run()` de siempre, sin reimplementar nada) y un registro/endpoint separado (`POST /simulation/compound-scenarios/:name/run`, mismo criterio de "uno a la vez"). Geometría: candidato exclusivo de la ambulancia A, candidato COMPARTIDO a medio camino exacto entre las ambulancias B y C (dentro del buffer crítico de ambas), y un control lejano. Verificado con Redis real local, `bun run`: 10/10 casos — el candidato exclusivo de A nunca aparece en B ni C; el compartido se alerta de forma independiente por B Y por C (confirma que el cooldown está aislado por PAR ambulancia-candidato, no se "gasta" en la primera que lo detecta); ninguna de las tres deja residuo en Redis al cerrar; determinismo confirmado en modo concurrente. Hallazgo honesto: la prueba original asumía que paralelo sería más rápido en reloj de pared que secuencial — resultó falso contra Redis LOCAL (latencia ~0, el overhead de manejar 3 promesas a la vez pesa más que el ahorro); se corrigió la prueba para no afirmar una garantía que el sistema no hace — el beneficio real de la concurrencia depende de latencia de red real (Upstash en producción). `typecheck`/`lint` del backend completo limpios. | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `backend/src/modules/simulation/scenarios/scenario-3-three-ambulances-simultaneous.ts`, `backend/src/modules/simulation/{simulation.engine.ts,simulation.types.ts,simulation.controller.ts}` |
| 2026-09-01 (10) | Escenario 4 del roadmap ("vehículo fuera de ruta", ADR-0022) — encontró y corrigió DOS bugs reales, no uno. Interpretado para el corredor: la ambulancia se desvía de su ruta planeada a mitad de trayecto (real: tráfico, calle cerrada). Bug #1 (arquitectónico, confirmado leyendo el código antes de construir): `EmergencyCorridorService.findCandidates` siempre muestreaba hacia adelante sobre la ruta PLANEADA original sin importar cuánto se desviara el GPS real — `location.gateway.ts` ya avisa AL CONDUCTOR "te saliste de tu ruta" pero nunca dispara nada hacia el corredor. Primera corrida real: el corredor seguía "protegiendo" la calle abandonada (falsa alarma) y nunca alertaba a quien estaba en el camino real (el peligro de verdad, sin aviso). Corregido: `findCandidates` ahora detecta desvío real (mismo `computeDeviation`/60m de `location.gateway.ts`) y cae a proteger el radio alrededor de la posición ACTUAL en vez de mirar hacia adelante sobre una ruta abandonada — no recalcula una ruta nueva (decisión del fundador, implica costo real de Google Routes), pero cierra el hueco de seguridad inmediato. Bug #2 (encontrado verificando el fix del #1, regresión real en escenarios 1 y 2): `computeDeviation` medía distancia al VÉRTICE más cercano de la ruta, no al segmento — con una ruta de solo 2 puntos (o cualquier tramo recto largo de una ruta real de Google con pocos waypoints), a mitad de camino ambos extremos quedan lejos aunque el punto esté justo sobre la línea, así que declaraba "fuera de ruta" estando perfectamente bien. El comentario original de esa función ya marcaba la proyección punto-segmento como mejora diferida "si la evidencia de uso real muestra que hace falta" — esta regresión real fue esa evidencia. Corregido con `distanceToPathMeters` nueva en `common/geo/interpolate.ts` (proyección punto-segmento real). Efecto colateral honesto: esto también corrige el comportamiento real de producción en `location.gateway.ts` — cualquier conductor en un tramo recto largo pudo haber recibido avisos falsos de "te saliste de la ruta" antes de este fix. Verificado con Redis real local, `bun run`: 8/8 casos, incluyendo regresión completa de los escenarios 1/2/3 sin cambios en sus resultados tras ambos fixes. `typecheck`/`lint` del backend completo limpios. | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `backend/src/modules/simulation/scenarios/scenario-4-vehicle-off-route.ts`, `backend/src/modules/simulation/{simulation.engine.ts,simulation.types.ts,simulation.controller.ts}`, `backend/src/modules/emergency-corridor/emergency-corridor.service.ts`, `backend/src/modules/route-session/route-deviation.ts`, `backend/src/common/geo/interpolate.ts` |
| 2026-09-01 (11) | Recálculo real de ruta al detectar desvío (ADR-0022) — el fundador pidió construir explícitamente lo que el Escenario 4 había dejado a propósito fuera de alcance por su costo real de API ("hay que hacerlo"). `EmergencyCorridorService.findCandidates` ahora, al detectar desvío, llama de verdad a `RoutingProvider` (mismo Google Routes/`ROUTING_PROVIDER` de Fase 2 — no se duplicó el binding, se importó `NavigationModule`) desde la posición actual hasta el destino ORIGINAL de la ambulancia, y sobrescribe la ruta activa en `RouteSessionService` — así el corredor Y el aviso al conductor (`location.gateway.ts`, mismo estado) quedan con la ruta corregida sin recalcular dos veces. Cooldown real de 30s (`SET NX EX`, mismo patrón que `AlertPolicyService`) para no gastar dinero real en cada consulta del corredor (cada 5-10s) mientras el desvío sigue activo. Si la API falla o está en cooldown, cae al mismo fallback de radio del Escenario 4 — nunca rompe el corredor por un fallo externo. Verificado con Redis real local, `bun run`: 15/15 casos (recálculo real con provider fake determinístico — la única pieza de pago real del sistema, mismo criterio que "nunca mockear infraestructura propia, sí las APIs de terceros de pago para no gastar dinero real en cada corrida) más regresión completa de escenarios 1-4 sin cambios (5, 53, aislamiento por par, y el escenario 4 re-verificado con el recálculo ya activo). Hallazgo honesto, fuera de alcance de este cambio: `sampleAhead` sigue con un lookahead fijo de 2km sin importar el tramo real restante — puede sobre-alertar temprano o dejar huecos a mitad de tramo en rutas con pocos waypoints (misma familia de bug vértice-vs-segmento ya corregida en `computeDeviation`, no aplicada aquí porque no era lo pedido); documentado en ADR-0022 para si el fundador pide priorizarlo con evidencia real. Prueba de humo contra Google Routes real: no se hizo (costo real, sin confirmación explícita del fundador para gastarlo) — queda disponible cuando la quiera. `typecheck`/`lint` del backend completo limpios (0 errores). | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `backend/src/modules/emergency-corridor/{emergency-corridor.service.ts,emergency-corridor.module.ts}` |
| 2026-09-01 (12) | Corrección real: 3 de los 10 archivos del Escenario 4 (`simulation.types.ts`, `simulation.engine.ts`, `simulation.controller.ts`) quedaron con una versión vieja al copiarse a la máquina del fundador — sus fechas de modificación parecían recientes, pero les faltaba el campo `ambulancePlannedRoutePoints`/la lógica de ruta planeada distinta/el registro del escenario 4, y `bun run build` real del fundador lo confirmó con un error de tipos. Diagnosticado archivo por archivo (no adivinado) comparando contenido real contra lo esperado, corregidos los 3, re-verificados con Redis real (11/11 casos) y `typecheck`/`lint`/`build` limpios antes de volver a sincronizar. Lección aplicada: la fecha de modificación de un archivo no es evidencia suficiente de que su CONTENIDO es el correcto — hay que verificar contenido real cuando algo falla del lado del fundador. | `backend/src/modules/simulation/{simulation.types.ts,simulation.engine.ts,simulation.controller.ts}` |
| 2026-09-02 | Escenario 5 del roadmap ("GPS con ruido", ADR-0022), en dos partes. Parte A: primera verificación real y directa (13/13 casos, sin mocks) de `validateRawReport`/`normalizeReport` (`location-normalizer.ts`) — el mecanismo REAL de defensa contra GPS con ruido del proyecto, auditado antes de construir y confirmado que `SimulationEngineService` nunca lo ejercitaba (escribe directo a Redis) — no tenía ninguna verificación hasta hoy. Sin bugs encontrados: ruido urbano normal aceptado con calidad `good`, accuracy degradada aceptada pero marcada `low_accuracy`, un glitch de teletransporte (2km en 1s) rechazado con `implausible_jump` sin envenenar la siguiente validación, accuracy/velocidad/reloj imposibles rechazados duro. Parte B: escenario con ruido determinístico sobre lo que la ambulancia REPORTA al corredor (nunca `Math.random()`, mismo criterio del proyecto). Pieza nueva encontrada construyendo, no anticipada: `ambulanceReportNoise` en `SimulationScenario` — la primera versión metía el ruido directo en la ruta física de la ambulancia (un zigzag real), lo que distorsionaba el LARGO real recorrido y por lo tanto el tiempo de viaje, así que la zona de ruido adversarial (a caballo del umbral de 60m) nunca coincidía con el paso simulado esperado. Corregido desacoplando el ruido de sensor reportado del movimiento físico real — `SimulationEngineService` calcula la posición verdadera primero (tiempo siempre correcto) y solo transforma lo que se guarda en Redis si el escenario lo pide; sin este campo, escenarios 1-4 sin cambios. Verificado con Redis real: 4/4 casos (candidato estable detectado, control lejano nunca alertado, candidato con temblor propio a caballo del umbral crítico alertado, recálculo real disparado exactamente 1 vez — en el único paso dentro de la zona adversarial, ninguno de los pasos con ruido normal lo disparó) más regresión completa de escenarios 1-4 sin cambios. `typecheck`/`lint`/`build` del backend completo limpios. | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `backend/src/modules/simulation/scenarios/scenario-5-gps-noise.ts`, `backend/src/modules/simulation/{simulation.types.ts,simulation.engine.ts,simulation.controller.ts}` |
| 2026-09-02 (2) | Incidente real de CI, no de código: el commit del Escenario 5 (y el anterior, la finalización del Escenario 4) quedaron en rojo en GitHub Actions aunque `typecheck`/`lint`/`build` habían pasado limpios en la máquina del fundador. Diagnosticado clonando el repo real de GitHub en un ambiente aislado y corriendo exactamente los mismos comandos que CI (`bun install --frozen-lockfile` + los 3 scripts) — reproducido igual: `scenario-4-vehicle-off-route.ts` NUNCA había quedado agregado a git en ningún commit anterior (existía en disco del fundador, nunca en el `git add`), y además `interpolate.ts`/`route-deviation.ts` (la corrección real vértice-vs-segmento del Escenario 4) tampoco estaban comiteados — el fix llevaba días "aplicado" solo localmente, nunca en producción real. Root cause exacto: los comandos de `git add` de commits anteriores no incluyeron estos 3 archivos por un descuido al armarlos; como `tsc`/`eslint` locales solo miran el disco (no git), nunca lo detectaron — el error de CI (`TS2307: Cannot find module`, luego heredado como "unsafe assignment of an error typed value" en eslint) solo aparece con un checkout real y limpio, que es exactamente lo que hace GitHub Actions. Corregido con dos commits (`git add` de los 3 archivos faltantes) — confirmado verde re-clonando el repo real después del push y corriendo los 3 comandos limpio. Lección aplicada iría más allá de "mtime no es evidencia de contenido correcto" (lección de la fila anterior, 2026-09-01 (12)): ahora, "mi propio `typecheck`/`lint`/`build` local limpio tampoco es evidencia de que CI vaya a pasar" si hay archivos sin comitear — verificar `git status`/un clon real limpio cuando CI falla sin motivo aparente, en vez de asumir que el código está mal. | (ninguno — solo `git add` de archivos ya existentes y correctos: `backend/src/common/geo/interpolate.ts`, `backend/src/modules/route-session/route-deviation.ts`, `backend/src/modules/simulation/scenarios/scenario-4-vehicle-off-route.ts`) |
| 2026-09-02 (3) | Escenario 6 del roadmap ("GPS atrasado", ADR-0022), en dos partes — ninguna necesitó un escenario nuevo del motor de simulación (el simulador nunca deja "envejecer" un reporte a propósito; ambas se verifican completas con llamadas reales a las funciones/servicios, mismo criterio que la Parte A del Escenario 5). Parte A: bug real corregido en `validateRawReport` — un reporte ATRASADO (llegó tarde por red móvil, reordenado) con `clientTimestamp` igual o anterior al último ya guardado se rechazaba con el motivo EQUIVOCADO (`implausible_jump`), por un `deltaSeconds` forzado a un mínimo artificial de 0.001s que hacía ver cualquier distancia como una velocidad imposible. El reporte SÍ debía rechazarse igual (aceptar uno atrasado retrocedería la posición "actual" en el tiempo), pero con el motivo real — se agregó `out_of_order` (nuevo valor de `LocationRejectionReason`) con un chequeo explícito antes del cálculo de velocidad. Efecto colateral correcto: un duplicado exacto también se rechaza como `out_of_order` (evita reescribir/re-emitir de más). Verificado, 9/9 casos: reporte atrasado → `out_of_order`; duplicado → `out_of_order`; una ráfaga de reportes atrasados EN ORDEN entre sí (cola offline que se vacía junta) → todos aceptados; un reordenamiento real dentro de esa ráfaga → rechazado igual. Parte B: gap real de VISIBILIDAD (no de comportamiento) encontrado auditando `EmergencyCorridorService.findCandidates` — `LocationStateService.findNearby` ya excluye candidatos con posición atrasada (>30s, `STALE_AFTER_MS`), pero `findCandidates` nunca revisaba si la posición de la AMBULANCIA MISMA estaba atrasada, usándola en silencio sin ninguna señal de que estaba desactualizada. Se decidió NO inventar comportamiento nuevo sin evidencia de qué debería hacer distinto — se agregó solo una advertencia real en logs (mismo patrón que ya usa `location.gateway.ts`), citando el mismo `STALE_AFTER_MS` real exportado (no duplicado). Verificado con Redis real, 7/7 casos: ambulancia fresca sin advertencia; ambulancia atrasada con advertencia (sigue funcionando, degradado); candidato fresco sí aparece; candidato atrasado NO aparece (primera vez que se ejercita de punta a punta esa exclusión ya existente — los escenarios 1-5 nunca dejan envejecer un reporte). De paso, diseñando el script de la Parte B se reprodujo empíricamente el caso simétrico del límite ya documentado de `sampleAhead` (vértice más lejano por empate de punto flotante en una ruta de 2 puntos) — se corrigió el diseño del script, no `sampleAhead` (sigue fuera de alcance). Regresión completa de escenarios 1-5 sin cambios (5, 53, aislamiento por par, escenario 4 con recálculo activo, escenario 5 sin error). `typecheck`/`lint`/`build` del backend completo limpios. | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/modules/location/{location-normalizer.ts,location.types.ts,location-state.service.ts}`, `backend/src/modules/emergency-corridor/emergency-corridor.service.ts` |
| 2026-09-02 (4) | Escenario 7 del roadmap ("usuario sin conexión", ADR-0022), en dos partes — primera vez en la serie que NINGUNA de las dos encontró un bug, solo confirmó con evidencia real que el sistema ya se comporta bien. Auditado antes de construir: `LocationGateway` no implementa `OnGatewayDisconnect` (a diferencia de `AssistantVoiceGateway`, que sí) — no hay código reaccionando al instante a una desconexión. Parte A: un candidato que se desconecta a mitad del corredor (deja de reportar del todo) queda cubierto por el mismo mecanismo de posición atrasada ya verificado en el Escenario 6 — después de 30s sin reportar deja de aparecer como candidato, sin necesitar manejo especial; y notificarlo igual (si el corredor cierra) no rompe nada, Socket.IO simplemente no tiene a quién entregarle el evento. Parte B: primera verificación real (nunca antes ejercitada, solo existía por lectura de código) de `AlertPolicyService.sweepExpired()`/`CorridorExpirySweepProcessor` (el barrido periódico cada 15 min, ADR-0020) — confirma con evidencia real, sin corregir nada, la brecha ya documentada y deliberada: el disparador del barrido es el TTL de la SESIÓN DE RUTA (4h), no la posición de la ambulancia — si la ambulancia se desconecta, su corredor sigue "activo" (y sigue pudiendo alertar candidatos nuevos) hasta que la ruta misma expire, no antes. Verificado con Redis real, 12/12 casos: candidato conectado detectado/alertado (base), candidato desconectado excluido, cierre no revienta con alguien desconectado, ambulancia desconectada con ruta aún vigente → el barrido NO cierra todavía, ruta expirada (simulada con `routeSession.clear()`) → el barrido SÍ cierra y notifica "ya pasó", barrido sin corredores activos no lanza. Sin cambios de código — solo documentación de comportamiento ya correcto, confirmado con evidencia. `typecheck`/`lint`/`build` del backend completo limpios (sin cambios que verificar, se confirmó que seguían limpios). | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-09-02 (5) | Escenario 8 del roadmap ("reconexión WebSocket", ADR-0022) — contraparte del Escenario 7, tampoco encontró ningún bug. Auditado antes de construir: `LocationGateway` no tiene código especial de reconexión (cada conexión nueva de Socket.IO autentica y se une a su room de cero, sin estado pegado al socket anterior). Hallazgo de diseño confirmado, no un bug: hay DOS TTL reales distintos que nunca se habían comparado con evidencia — `STALE_AFTER_MS` (30s, ya conocido del Escenario 6) solo marca `stale: true` sin borrar la clave; el TTL interno de 300s (5 min) de `location:current:<userId>` sí la borra del todo, y pasado eso `getCurrent()` devuelve `null`. Eso cambia cómo `validateRawReport` trata la reconexión: dentro de la ventana 30s-300s todavía compara contra un `previous` real (el candidato reconectado con una posición nueva se acepta, porque el tiempo REAL transcurrido hace la velocidad implícita plausible, no se confunde "estuvo desconectado" con "salto imposible"); después de 300s llega con `previous: null`, tratado como reporte de cero, igual que un usuario nuevo. Verificado con Redis real, 11/11 casos en tres partes: (A) candidato se desconecta y reconecta dentro de la ventana — reporte aceptado, reaparece en el corredor, se puede volver a alertar si el cooldown de 30s ya venció; (B) candidato reconecta después de que la clave expiró del todo — mismo comportamiento correcto, tratado como nuevo; (C) la ambulancia se reconecta tras haber estado atrasada — la advertencia real de "GPS atrasado" (Escenario 6) no queda pegada, desaparece sola en la siguiente llamada una vez que vuelve a reportar. Sin cambios de código. `typecheck`/`lint`/`build` del backend completo limpios (sin cambios que verificar, se confirmó que seguían limpios). | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-09-02 (6) | Escenario 9 del roadmap ("ambulancia cancelada", ADR-0022) — ciclo de vida real de `POST /emergency/corridor/close` (`reason: "cancelled"` por defecto si no se manda). Verificado con Redis real, 16/16 casos: cancelar sin candidatos previos ni registro como corredor activo no lanza (SREM sobre un set del que nunca fue miembro es un no-op seguro); cancelar con un candidato ya alertado lo notifica con el motivo REAL "cancelled" (no "completed" por defecto, la etiqueta se propaga tal cual); tras cancelar la ruta desaparece y el corredor vuelve al estado "sin ruta activa"; doble cancelación (botón tocado dos veces, reintento de red) es idempotente — no lanza ni reenvía la notificación, primera vez que se confirma esto con evidencia real; cancelar el corredor propio nunca toca el de otra ambulancia (ruta, candidatos y notificaciones de la otra intactos). Nota honesta de proceso: la primera versión del script de aislamiento entre ambulancias reusaba las mismas coordenadas para "dos ambulancias distintas" y falló — no por un bug del producto, sino porque con corredores geográficamente superpuestos, el corredor de una SÍ trata a la otra ambulancia (y a sus candidatos ya alertados) como candidatos nuevos propios. Eso es real, pero es la pregunta del Escenario 12 ("corredores que sí se cruzan", ya pendiente en el roadmap) — se corrigió el diseño de la prueba (coordenadas separadas) para no mezclar las dos preguntas, y queda anotado como evidencia real para cuando se construya el Escenario 12. Hallazgo real, documentado y NO corregido (bajo impacto, baja probabilidad): cancelar no limpia el cooldown real de recálculo de ruta del conductor (`corridor:reroute-cooldown:<id>`) — si el mismo conductor cancela y arranca un traslado nuevo dentro de los 30s siguientes que necesite recalcular de inmediato, caería al fallback de radio en vez de recalcular real la primera vez; sigue siendo seguro, solo menos preciso, y la clave está scopeada al conductor, no al traslado — agregar una limpieza cruzada entre módulos para un caso de borde así no se justificó sin evidencia de que haga falta. Sin cambios de código. `typecheck`/`lint`/`build` del backend completo limpios (sin cambios que verificar, se confirmó que seguían limpios). | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-09-02 (7) | Escenario 10 del roadmap ("ambulancia terminada", ADR-0022) — primera vez que un escenario instancia y prueba `EmergencyCorridorController` real, no solo los servicios de abajo (todos los scripts 6-9 llamaban directo a `AlertPolicyService`/`EmergencyCorridorService`). Auditado antes: `CorridorCloseReason` no tiene ninguna lógica distinta según el motivo (solo es la etiqueta notificada), así que lo único real que faltaba probar era cómo el controller interpreta el `reason` que manda el cliente. Bug real encontrado y corregido: cualquier `reason` no reconocido (typo real del cliente, o `"expired"` mandado a mano — motivo interno que solo debería producir el barrido real) caía callado al default `"cancelled"`, notificando a los candidatos con la etiqueta equivocada sin que nadie se enterara del error del cliente. Corregido con una lista explícita de motivos válidos de cliente (`completed`/`cancelled`, sin `expired`) y un rechazo real con 400 para cualquier otro valor — el default para `reason` omitido sigue siendo `cancelled`, sin cambios. Único fake del script: `EmergencyVehiclesService` (Supabase real, sin credenciales en este sandbox — mismo criterio que fakear Google Routes, infraestructura externa fuera de alcance). Verificado, 10/10 casos: `reason: "completed"` cierra y notifica con la etiqueta real; `reason` omitido sigue en `cancelled`; `reason` inválido se rechaza con 400 y sin efectos secundarios (ruta intacta); `reason: "expired"` desde un cliente también se rechaza; un conductor no verificado no puede cerrar nada (guardia ya existente, primera vez verificada a través del controller real). `typecheck`/`lint`/`build` del backend completo limpios. | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/modules/emergency-corridor/emergency-corridor.controller.ts` |
| 2026-09-02 (8) | Escenario 11 del roadmap ("conductor entra/sale del corredor", ADR-0022) — tampoco encontró bugs (segunda vez consecutiva, después del Escenario 8, que confirma comportamiento ya correcto en vez de corregir algo). Auditado antes: ni `LocationStateService.findNearby` ni `EmergencyCorridorService.findCandidates` guardan memoria de posiciones pasadas — cada consulta es un `GEOSEARCH` real sobre la posición ACTUAL del candidato — así que "entrar/salir del buffer" ya funciona por construcción, sin necesitar ningún manejo especial. Lo real que nunca se había ejercitado (Escenarios 6-10 solo mueven a cada candidato una vez) es el dedup/cooldown de 30s de `AlertPolicyService` a través de MÚLTIPLES transiciones entrada→salida→reentrada del mismo candidato durante el mismo traslado. Verificado con Redis real, 17/17 casos: candidato fuera del buffer no se alerta; al entrar se alerta y queda en el set de "alertados del traslado"; al salir sigue en ese set (para poder avisarle "ya pasó" si el corredor cierra mientras está afuera); al reentrar DENTRO del cooldown de 30s no se genera alerta nueva ni notificación duplicada (`skippedByCooldown`); al reentrar DESPUÉS de que el cooldown expira de verdad (simulado borrando la clave, mismo patrón que Escenarios 6-8) sí se genera una alerta legítima nueva, sin duplicar la membresía en el set (`SADD` es idempotente); cerrar el corredor mientras el candidato está fuera del buffer en ese instante igual lo notifica con el motivo real y vacía el set; sin ruta activa tras cerrar, `findCandidates` vuelve a devolver `null` sin romper nada. Sin cambios de código. `typecheck`/`lint`/`build` del backend completo limpios (sin cambios que verificar, se confirmó que seguían limpios). | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-09-02 (9) | Escenario 12 del roadmap ("dos corredores se cruzan", ADR-0022) — el último de los 12 escenarios mínimos, motivado por evidencia real del Escenario 9 (la primera versión de esa prueba de aislamiento reusó coordenadas idénticas para "dos ambulancias" por error y reveló el bug real por accidente). Bug real encontrado y corregido: `EmergencyCorridorService.findCandidates` solo excluía de sus resultados a la propia ambulancia consultante, nunca a OTRAS ambulancias con corredor activo — como `LocationStateService.findNearby` indexa a todos los que reportan ubicación por igual (ambulancias incluidas), con dos rutas que se cruzan de verdad una ambulancia podía aparecer como candidato civil en el corredor de la otra. Corregido reusando `ACTIVE_AMBULANCES_KEY` (exportado desde `AlertPolicyService` — mismo set real que ya usa `sweepExpired`, no un mecanismo nuevo): `findCandidates` ahora también excluye a cualquier otra ambulancia activa. Verificado con Redis real y dos rutas que se cruzan de verdad, 18/18 casos — confirmado además revirtiendo el fix temporalmente y viendo la prueba fallar de verdad (checks 2 y 3) antes de restaurarlo, evidencia real de que la prueba cubre el bug. Un candidato civil real parado en el punto de cruce sigue siendo detectado y alertado por AMBAS ambulancias de forma independiente (2 notificaciones reales distintas, una por cada `ambulanceDriverId`) — el filtro nuevo no afecta la detección de candidatos civiles, solo excluye a otras ambulancias. Cerrar el corredor de una ambulancia no toca el estado de la otra bajo cruce geográfico genuino (no solo con coordenadas separadas a propósito, como en el Escenario 9). Regresión puntual del caso base (un solo corredor, sin otras ambulancias activas) confirmó que la detección normal de candidatos no cambió. `typecheck`/`lint`/`build` del backend completo limpios. **Con este escenario se completan los 12 escenarios mínimos del roadmap (Etapa 7).** | `docs/decisions/ADR-0022-simulation-engine-first-slice.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/modules/emergency-corridor/{emergency-corridor.service.ts,alert-policy.service.ts}` |
| 2026-09-02 (10) | Arranca la Fase 8 (Hardening) — bloqueada hasta ahora por "Fase 3 y 4 completas y estables", ya se cumple. Primera auditoría (ADR-0036): ¿los guards protegen también a los gateways de WebSocket (`LocationGateway`, `AssistantVoiceGateway`), o solo a HTTP? Nunca se había verificado. Se armó una app NestJS real mínima (mismo `RateLimitModule` real — `ThrottlerModule` + `UserAwareThrottlerGuard` + Redis real) con un gateway de prueba, y se conectó con `socket.io-client` real (sockets reales distintos, no mocks). Hallazgo real: un guard registrado como `APP_GUARD` global NUNCA se invoca para un handler `@SubscribeMessage` en esta configuración — confirmado primero de forma aislada (un guard con solo un log nunca imprimió nada al recibir un mensaje real), después con el guard real de rate limiting (cero claves se crearon en Redis, 65 mensajes en ráfaga desde el mismo socket pasaron sin ningún bloqueo, muy por encima del límite configurado de 60/min). Conclusión: `location:update` (reporte de GPS en tiempo real) y los handlers de `AssistantVoiceGateway` (`voice:text`/`voice:audio-chunk`/`voice:audio-end`) no tienen ningún rate limiting real hoy. La autenticación de ambos gateways SÍ sigue intacta (es manual en `handleConnection` contra Supabase real, nunca dependió de guards) — este hallazgo es solo sobre frecuencia, no sobre autorización. Hallazgo secundario menor: ambos gateways usan `cors: { origin: "*" }`, a reemplazar por un origen real antes de producción. NO corregido en este cambio a propósito: el límite real por canal es una decisión de producto (`location:update` puede necesitar mandarse muy seguido mientras la app está abierta; `voice:audio-chunk` es tráfico legítimo de alta frecuencia durante una sesión activa) — inventar un número sin que el fundador lo confirme sería adivinar. De paso, auditando para esta fase se encontraron y corrigieron 2 filas desactualizadas en el cronograma (Fase 3 seguía listando el panel de administrador como pendiente aunque ya se construyó el 2026-09-01 vía ADR-0006; Fase 7 — Recordatorios por ubicación — seguía sin marcar aunque el código ya existe, verificado real) y se documentó una brecha real de documentación: `ADR-0014`/`ADR-0015`, citados por nombre en varios docs, no existen como archivos. Sin cambios de código — este cambio es solo de auditoría y documentación. | `docs/decisions/ADR-0036-websocket-rate-limiting-gap.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md` |
| 2026-09-02 (11) | Corrección real del hallazgo de la fila anterior (ADR-0036) — el fundador delegó explícitamente los límites reales ("quiero que tomes tú la decisión y toma la mejor", mismo criterio que ADR-0021 para el buffer dinámico). Nuevo helper genérico `checkSocketRateLimit` (`common/rate-limit/socket-rate-limit.ts`) — mismo patrón real `INCR`/`EXPIRE` de ventana fija que ya usa el resto del proyecto (`AlertPolicyService`/`EmergencyCorridorService`), no una librería nueva, porque `@nestjs/throttler` no llega a los gateways (confirmado en el hallazgo anterior). Límites elegidos, generosos a propósito para no romper tráfico legítimo real: `location:update` 100/10s (deja margen de sobra sobre una reconexión real que vacía de golpe una cola de reportes atrasados, caso YA verificado en el Escenario 6/ADR-0022), `voice:text` 10/10s (un turno de texto es una acción humana discreta), `voice:audio-chunk` 50/5s (streaming real de micrófono manda varios chunks/segundo de por sí). De paso, límite real de UNA sola sesión de Gemini Live activa por usuario (cada sesión tiene costo real) — `AssistantVoiceGateway.handleConnection` ahora cierra la sesión VIEJA de un usuario (le manda `voice:closed` y la desconecta) si el mismo usuario abre una conexión nueva, en vez de dejarla huérfana facturando en paralelo; tracking en memoria (no Redis) porque la sesión es un objeto vivo atado a este proceso. Verificado con Redis real, 12/12 casos: el helper genérico permite exactamente el límite y bloquea de ahí en más, con TTL real puesto y vuelve a permitir pasada la ventana; `LocationGateway.handleLocationUpdate` real acepta 100 de 101 reportes seguidos y descarta el 101 sin tocar el estado guardado; `voice:text`/`voice:audio-chunk` reales aceptan exactamente el límite configurado; conectar un socket nuevo del mismo usuario cierra de verdad el viejo y nunca quedan dos sesiones activas en paralelo. `typecheck`/`lint`/`build` del backend completo limpios. Pendiente, menor: reemplazar `cors: { origin: "*" }` por un dominio real antes de producción. | `docs/decisions/ADR-0036-websocket-rate-limiting-gap.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/common/rate-limit/socket-rate-limit.ts`, `backend/src/modules/location/location.gateway.ts`, `backend/src/modules/assistant/assistant-voice.gateway.ts` |
| 2026-09-02 (12) | Sigue la Fase 8 (Hardening), bloque Rendimiento. Primero auditoría de "autorización de emergencia" y "privacidad de ubicación" (Seguridad): ambas ya están bien — `EmergencyCorridorController` (`candidates`/`close`) exige `verified && active` contra `request.userId` server-derivado (nunca dato del cliente), `activate-emergency-corridor.tool.ts` (asistente de voz) hace el mismo chequeo; `GET /location/me` y `GET /emergency/vehicles/me` solo devuelven datos del propio usuario autenticado. Sin cambios necesarios ahí. Después, prueba de carga REAL (`loadtest-corridor.ts`, Redis real local, clases reales instanciadas directo, sin necesitar Supabase — todo el hot path realtime del corredor es Redis-only por diseño): reveló un N+1 real en `EmergencyCorridorService.findCandidates` — con 500 candidatos reales cerca de una ruta de 20 muestras, 2579 comandos reales de Redis por una sola consulta (`getCurrent` se pagaba una vez por cada muestra en la que un mismo candidato aparecía, antes del dedup). Corregido (ADR-0037): `LocationStateService.geosearchNearby` (solo el `GEOSEARCH`, sin `getCurrent`) + dedup entre muestras ANTES de revalidar frescura, una sola vez por candidato único — mismo resultado final, verificado con 6/6 casos deterministas reales (candidato en el radio de 2 muestras consecutivas aparece una sola vez con la distancia mínima; fuera de rango no aparece; stale >30s se filtra igual que antes). Mejora real medida: 2579→548 comandos de Redis por consulta (~4.7×), p50 182ms→25.5ms (~7×), p95 258ms→86.7ms. `typecheck`/`lint`/`build` del backend completo limpios. Scripts throwaway (`loadtest-corridor.ts`, `verify-corridor-refactor.ts`), borrados tras la corrida, no comiteados. Pendiente real: resto del bloque Rendimiento (Redis tuning, DB indexes, PostGIS, WS scaling) sin auditar; `GeofenceTriggerService` no se midió (necesita Supabase real, sin credenciales en este sandbox). | `docs/decisions/ADR-0037-corridor-findcandidates-n-plus-one.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/modules/location/location-state.service.ts`, `backend/src/modules/emergency-corridor/emergency-corridor.service.ts` |
| 2026-09-02 (13) | Sigue Rendimiento, Fase 8 — "WebSocket scaling" (roadmap Etapa 8). Auditoría de `main.ts`: Socket.IO arrancaba sin ningún adapter configurado, así que usaba el adapter en memoria por defecto — `LocationBroadcastService.notify()` (`server.to(userId).emit(...)`, el único mecanismo real de alertas server-initiated del corredor de emergencia) solo entrega a sockets conectados al MISMO proceso. Con más de una instancia real detrás de un load balancer, un candidato conectado a otra instancia nunca recibiría una alerta real — en silencio, el endpoint HTTP sigue respondiendo `notified: true`. Verificado empíricamente (`verify-ws-scaling.ts`, throwaway): dos servidores Socket.IO reales en puertos distintos + Redis real + `socket.io-client` real — SIN adapter, un evento disparado en el servidor A nunca llegó a un socket real conectado al servidor B (confirma el hallazgo); CON el fix, sí llega; entrega local (caso real de hoy, una sola instancia) sin cambios. Corregido (ADR-0038): `RedisIoAdapter` nuevo (`common/websocket/redis-io.adapter.ts`), mismo patrón oficial de NestJS (verificado contra el ejemplo real del propio repo de NestJS antes de escribir código) pero reusando `ioredis` (ya obligatorio en el proyecto vía `REDIS_CONNECTION`, duplicado con `.duplicate()` para los clientes pub/sub) en vez del paquete `redis` que usa el ejemplo oficial — confirmado primero contra el README real de `@socket.io/redis-adapter` que soporta ioredis explícitamente. Única dependencia nueva: `@socket.io/redis-adapter@8.3.0`. Se corrige ahora aunque hoy corre como una sola instancia (sin Dockerfile/Procfile en el repo, sin evidencia de despliegue multi-instancia todavía) porque el roadmap ya declara esta necesidad explícitamente, el costo es bajo (reusa infra ya obligatoria), y verificado que no cambia nada en el caso de una instancia. 3/3 casos reales. `typecheck`/`lint`/`build` del backend completo limpios. Script throwaway borrado tras la corrida, no comiteado. | `docs/decisions/ADR-0038-websocket-scaling-redis-adapter.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/common/websocket/redis-io.adapter.ts`, `backend/src/main.ts`, `backend/package.json` |
| 2026-09-02 (14) | Cierra Rendimiento, Fase 8 — DB indexes/PostGIS, esta vez contra Postgres/Supabase REALES: el fundador conectó el proyecto real "Copiloto" (`wrkuusacwkdazfwynhkz`) a este entorno ("ya conecte el sandbox, lo puedes hacer tu"), reemplazando la limitación anterior de "sin credenciales de Supabase en este sandbox". Auditoría con Supabase Advisors reales (`get_advisors`, performance + security) sobre TODO el esquema real: encontró hallazgos reales tanto en `emergency_vehicles` (dominio de este backend) como en las tablas de mensajería (`chats`/`messages`/`statuses`/`contacts`/etc., dominio de `proyecto-mensajeria/`, un repo separado). Alcance deliberado: solo se corrigió `emergency_vehicles` — regla del proyecto "no tocar áreas no relacionadas". Tres hallazgos reales corregidos (ADR-0039): (1) FK real sin índice en `verified_by`; (2) policy RLS `emergency_vehicles_select_own` reevaluaba `auth.uid()` por fila en vez de una vez por consulta (`auth_rls_initplan`); (3) función `SECURITY DEFINER` real `is_verified_ambulance_driver(uuid)` — consulta si un userId arbitrario es ambulancia verificada activa — tenía `EXECUTE` otorgado a `PUBLIC`, ejecutable por cualquiera SIN AUTENTICAR; confirmado con SQL real que ni el backend la llama (grep real sobre `backend/src/`) ni ninguna policy RLS la usa, así que se restringió el acceso (`revoke execute ... from public, anon, authenticated`, sin borrar la función). Migración real aplicada al proyecto real (`apply_migration`, `emergency_vehicles_performance_and_security_hardening`) y comiteada también como archivo local (`supabase/migrations/20260902030914_...sql` — antes de esto, el repo solo tenía UNA de las 17 migraciones ya aplicadas remotamente comiteada, gap preexistente no corregido acá). Verificado real antes/después: los 4 findings (2 performance + 2 security) sobre `emergency_vehicles`/`is_verified_ambulance_driver` desaparecieron de una segunda corrida real de `get_advisors`, más SQL directo confirmando índice creado, policy nueva, y grants revocados. Hallazgos reales de mensajería documentados por transparencia en ADR-0039 pero NO tocados (podrían estar en uso real desde `proyecto-mensajeria/`, no auditado). Con esto, Rendimiento de la Fase 8 queda cubierto para el dominio real de este backend (Redis vía ADR-0037/0038, DB vía ADR-0039). Pendiente real: `cors: { origin: "*" }` (ADR-0036), `auth_leaked_password_protection` de Supabase Auth (config de dashboard, no de esquema), y el piloto real. | `docs/decisions/ADR-0039-emergency-vehicles-db-hardening.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `supabase/migrations/20260902030914_emergency_vehicles_performance_and_security_hardening.sql` |
| 2026-09-02 (15) | Cierre de Seguridad, Fase 8 — a pedido explícito del fundador ("continua con la proteccion hay que darle prioridad a la seguridad"). Se corrigió el único pendiente de seguridad real accionable con código: `cors: { origin: "*" }` fijo en `LocationGateway`/`AssistantVoiceGateway` (señalado desde ADR-0036, nunca corregido por falta de un dominio real). Nuevo helper compartido `resolveWebSocketCorsOrigin()` (ADR-0040): fuera de producción, permisivo (`origin: true`, igual que ya hace el CORS HTTP real de `main.ts` en desarrollo); en producción SIN `CORS_ORIGIN` configurado, deshabilitado (`origin: false`) — nunca cae a `"*"` por defecto; con `CORS_ORIGIN` configurado, lista real de orígenes separados por coma. Detalle técnico real verificado ANTES de escribir código (no adivinado): el objeto `cors` del decorador `@WebSocketGateway` se evalúa cuando Node IMPORTA el archivo del gateway, antes de que `ConfigModule.forRoot()` corra — confirmado leyendo el orden real de imports de `app.module.ts` (`LocationModule` aparece en el array `imports` del `@Module` de `AppModule`, así que ya se evaluó completo, decorador incluido, antes de que el propio decorador de `AppModule` invoque `ConfigModule.forRoot()`) — por eso el helper lee `process.env` directo en vez de `ConfigService` (documentado en el código y en `.env.example`, con la advertencia real de que en producción `CORS_ORIGIN` debe venir de la plataforma de hosting, no del `.env` cargado por dotenv, que llega demasiado tarde para este caso puntual). Verificado con script real, 4/4 casos (sin `NODE_ENV`/con `development`: `true`; `production` sin `CORS_ORIGIN`: `false`; `production` con `CORS_ORIGIN`: array real separado y recortado). `typecheck`/`lint`/`build` del backend completo limpios. Se investigó también el OTRO pendiente de seguridad (`auth_leaked_password_protection` de Supabase Auth, ADR-0036/0039): confirmado con documentación oficial real de Supabase (`search_docs`) que esa función requiere plan Pro o superior, y con `get_organization` real contra la organización real ("Grupo-neggo") que el plan actual es `free` — bloqueado por facturación real, no por código ni por falta de esfuerzo; documentado como pendiente real, decisión que solo el fundador puede tomar (upgrade de plan). Con esto, Fase 8 (Seguridad + Rendimiento) queda cerrada para todo lo que le toca a este backend. | `docs/decisions/ADR-0040-websocket-cors-hardening.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/src/common/websocket/websocket-cors.ts`, `backend/src/modules/location/location.gateway.ts`, `backend/src/modules/assistant/assistant-voice.gateway.ts`, `backend/.env.example` |
| 2026-09-02 (16) | Arranque real de la Etapa 1 del piloto ("1 ambulancia simulada + 5–10 conductores", a pedido explícito del fundador: "podemos hacer esto"). Primero, la pregunta pendiente del fundador ("¿quién maneja `proyecto-mensajeria`?"): auditando `proyecto-mensajeria/vite.config.ts` se confirmó real que ese frontend usa `@lovable.dev/vite-tanstack-config` con Nitro apuntando a Cloudflare por defecto — se publica desde el propio editor de Lovable, no desde este repo/sesión (consistente con `.gitignore` excluyendo `proyecto-mensajeria/.workspace/` como "metadata interna de Lovable, repo git propio anidado"). Se confirmó también que `proyecto-mensajeria/src/lib/backend/client.ts` SÍ llama al backend NestJS real (mismo token de sesión de Supabase, `Authorization: Bearer`) y que ya existen pantallas reales wireadas (`EmergenciaScreen`, `AdminAmbulanciasScreen`, `ModoManejoScreen`) — el comentario viejo de `SimulationController` ("sin UI todavía") quedó desactualizado. Auditando qué haría falta de verdad para el piloto (no solo la lógica del corredor, ya probada desde ADR-0022 con el motor de simulación — escenario 1 ya es casi exactamente "1 ambulancia + 10 vehículos"): el bloqueo real es que el backend nunca se desplegó, solo corre en local/sandbox. Decisión delegada por el fundador ("toma tu la mejor decision") sobre dónde alojarlo: Railway, evaluado contra Render/Fly.io por soporte real de WebSockets + lectura de `PORT` ya compatible sin cambios de código + plan de prueba suficiente para un piloto de 5–10 personas. Verificado ANTES de escribir código (documentación oficial real de Railway, `docs.railway.com/guides/bun`): Railpack todavía NO detecta proyectos Bun solo, hace falta Dockerfile propio — se creó `backend/Dockerfile` (dos etapas, `oven/bun:1-alpine`), `.dockerignore`, y script `start:prod`. Verificado real sin Docker daemon disponible en este sandbox (limitación del entorno): `bun install --frozen-lockfile` (497 paquetes, lockfile sincronizado sin cambios), `bun run build` (compila real, genera `dist/main.js`), `bun dist/main.js` (Nest real arranca y falla exactamente en `validateEnv` por falta de credenciales reales en este sandbox — confirma que el artefacto compilado es válido). `HealthController` (`GET /health`) ya existía, reusado tal cual para el healthcheck real de Railway. Pendiente real, NO ejecutable por mí (creación de cuenta/proyecto y variables de entorno reales — acción de cuenta/facturación): que el fundador cree el proyecto en Railway, cargue `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`REDIS_URL`/`CORS_ORIGIN` reales en su dashboard, y confirme si `proyecto-mensajeria` ya tiene una URL publicada real en Lovable (necesaria para `CORS_ORIGIN` y `VITE_BACKEND_URL`) — runbook completo con los pasos exactos en ADR-0041. | `docs/decisions/ADR-0041-backend-deploy-railway.md`, `docs/decisions/05_CRONOGRAMA_EMERGENCY_Y_NUEVAS_FUNCIONALIDADES.md`, `backend/Dockerfile`, `backend/.dockerignore`, `backend/package.json` |
| 2026-09-02 (17) | Unificación del hosting a pedido explícito del fundador ("unifiquemos todo en uno solo... no me interesa separarlo"). Al preparar el despliegue de `proyecto-mensajeria/` se reprodujo real (build y dev) `rootRouteNode must not be undefined... src/routes/__root.tsx` contra una copia de este repo subida a este chat en una sesión anterior — el fundador cuestionó el hallazgo ("revisa bien... no perdamos tokens"), se reprodujo dos veces antes de insistir, y se llegó a "restaurar" 101 archivos desde un zip de Lovable con 4 correcciones de código encima. **Autocorrección real antes de sincronizar nada**: con el Mac conectado, se comparó por primera vez esa copia contra el repositorio REAL en `/Users/usuario/Documents/GitHub/Copiloto/proyecto-mensajeria/` (listado recursivo vía el puente de dispositivo) — reveló que el repo real ya tenía los 146 archivos completos (`__root.tsx` incluido), ya tenía `setCurrentUserId` en `mock-data.ts`, ya tenía la llamada async real (3 args) a `startChatWithUser` conectada a Supabase, y que `src/components/notes/` ya no existe ahí — fue reemplazado hace tiempo por `src/components/reminders/` (Fase 7, Recordatorios). El error SÍ era real, pero solo en la copia vieja de este chat, nunca en el código del fundador; restaurar el "Notes" viejo de Lovable habría sido un retroceso. Se descartó por completo el trabajo de "restauración" y las 4 "correcciones" — nada de eso se sincronizó al Mac. Verificación real contra el contenido real del Mac (traído vía `device_stage_files`, 146 archivos): `bun install --frozen-lockfile` (422 paquetes), `NITRO_PRESET=node-server bun run build` limpio, `typecheck` limpio, y smoke test real (`HOST=0.0.0.0`, `curl` real → `HTTP 200` con HTML real de la app) — cero cambios de código necesarios. Único hallazgo real pendiente, preexistente y no corregido acá (fuera de alcance de este cambio): 1384 errores de `prettier/prettier` en el repo real. Lo único que genuinamente faltaba era el Dockerfile: nuevo `proyecto-mensajeria/Dockerfile` (mismo patrón de ADR-0041) + `.dockerignore` — diferencia real encontrada: el build con `NITRO_PRESET=node-server` empaqueta sus propias dependencias en `.output/server/node_modules` (solo `tslib`, ~48 KB), así que la etapa final no necesita `bun install`. Pendiente real, no ejecutable por mí: que el fundador cree un segundo servicio en el mismo proyecto de Railway del backend (Root Directory=`proyecto-mensajeria`), cargue `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY`/`VITE_BACKEND_URL` reales, genere el dominio público, y luego vuelva al backend a fijar `CORS_ORIGIN` (ADR-0040) a esa URL nueva. | `docs/decisions/ADR-0042-frontend-restore-y-deploy-railway.md`, `proyecto-mensajeria/Dockerfile`, `proyecto-mensajeria/.dockerignore` |
| 2026-09-02 (18) | Despliegue real del segundo servicio en Railway, con tres bugs reales encontrados y corregidos en producción (ninguno simulado, los tres confirmados con evidencia real del propio Railway/navegador). (1) **502**: el dominio público HTTP apuntaba al puerto que Railway adivinó por defecto en vez de 3000 (el puerto real donde escucha Nitro, confirmado con el log real `Listening on: http://localhost:3000/`) — un valor "3000" indicado antes había quedado cargado en el campo separado de "TCP Proxy", no en el de la app; corregido usando "Edit Port" del dominio HTTP específicamente. (2) **500**: confirmado contra la documentación oficial real de Railway (`docs.railway.com/builds/dockerfiles`) que Railway solo pasa Variables del servicio al `docker build` si el propio Dockerfile las declara con `ARG` — sin esto, Vite horneaba el bundle con `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` vacíos; corregido agregando los `ARG` correspondientes a la etapa `builder` del Dockerfile. (3) **CORS bloqueando el navegador real** incluso después de fijar `CORS_ORIGIN` en el backend: confirmado con un `fetch()` real ejecutado desde la propia página desplegada (`blocked by CORS policy: No 'Access-Control-Allow-Origin' header`) — causa real leyendo `backend/src/main.ts`: el CORS HTTP normal (`app.enableCors(...)`) solo se activaba fuera de producción, quedando completamente deshabilitado en producción sin leer `CORS_ORIGIN` en absoluto (esa variable solo alimentaba `resolveWebSocketCorsOrigin()`, conectada únicamente a los gateways de WebSocket desde ADR-0040). Corregido reusando el mismo helper (`REUSE`, sin lógica nueva): `app.enableCors({ origin: resolveWebSocketCorsOrigin() })`. Verificado real de punta a punta: `typecheck`/`lint`/`build` del backend limpios, script real con las 4 combinaciones de `NODE_ENV`/`CORS_ORIGIN` (4/4 correctas), y un `fetch()` real ejecutado desde el navegador desplegado contra el backend desplegado devolviendo el JSON real de `/health` (`{"status":"ok",...}`) — confirmado también con capturas de pantalla reales del fundador cargando la app. Con esto, `proyecto-mensajeria` (`disciplined-ambition-production-b2c4.up.railway.app`) y el backend (`copiloto-production-b047.up.railway.app`) quedan ambos en línea y comunicándose sin CORS, en el mismo proyecto de Railway (misma facturación/dashboard, servicios separados por la razón real explicada al fundador: `VITE_*` se hornea público en el bundle del navegador, los secretos del backend nunca deben estarlo, así que no pueden compartir un mismo proceso). | `docs/decisions/ADR-0042-frontend-restore-y-deploy-railway.md`, `docs/decisions/ADR-0040-websocket-cors-hardening.md`, `proyecto-mensajeria/Dockerfile`, `backend/src/main.ts` |
| 2026-09-02 (19) | El fundador probó el flujo real de registro y encontró dos problemas reales de uso, ambos investigados en el código real antes de tocar nada (no adivinado). **OTP**: `requestPhoneOtp`/`verifyPhoneOtp` (`proyecto-mensajeria/src/lib/actions/auth.ts`) sí llaman al SDK real de Supabase (`signInWithOtp`/`verifyOtp` con `type: "sms"`), pero el proveedor "Phone" de Supabase Auth está deshabilitado (sin proveedor SMS real conectado — un intento previo con Twilio quedó pausado por el registro A2P 10DLC) — hoy solo funcionan los números fijos de `VITE_DEV_TEST_PHONES` (bypass de desarrollo, y ni siquiera ese está disponible en Railway porque esa variable nunca se declaró como `ARG` del Dockerfile ni se cargó ahí). El fundador decidió (vía pregunta directa, no asumido): retomar un proveedor SMS real — pendiente de implementar. **Sesión perdida en cada refresh**: confirmado que era una decisión previa deliberada, no un bug — `proyecto-mensajeria/src/lib/supabase/client.ts` guardaba la sesión en memoria (`MemoryStorageAdapter`) por la regla explícita anterior del fundador de "nunca localStorage". El fundador confirmó (vía pregunta directa) cambiar a almacenamiento persistente para este caso puntual. Corregido: se quitó `MemoryStorageAdapter` y el `storage` explícito — con `persistSession: true` sin `storage`, el propio SDK (`@supabase/auth-js`) usa `globalThis.localStorage` automáticamente cuando está disponible (confirmado leyendo el código real instalado en `node_modules/@supabase/auth-js/dist/main/GoTrueClient.js`, línea real `this.storage = globalThis.localStorage`) — sin lógica propia que mantener (`REUSE`). Se aclaró también al fundador que esto no abre la sesión a "cualquiera con el número": el OTP sigue verificando el número una sola vez por dispositivo/navegador, igual que WhatsApp Web/Gmail/bancos guardan la sesión ya autenticada; una alerta real de "nuevo dispositivo" (análogo a Gmail) sería una función aparte, no relacionada con este fix de almacenamiento. Verificado real: `typecheck` limpio, `lint` sin errores nuevos (los 1384 preexistentes de `prettier` no tocados), `build` real de producción exitoso, y grep del bundle generado confirmando que `MemoryStorageAdapter` ya no existe como código (solo queda en el comentario histórico). | `proyecto-mensajeria/src/lib/supabase/client.ts` |
| 2026-09-02 (20) | Intento real de conectar Twilio (probado en vivo, navegando la cuenta real del fundador — trial "My First Twilio Account"): se encontró que Messaging Services y Numbers & senders están bloqueados por el paywall de trial de Twilio ("Upgrade your account to keep building"), que la recarga mínima real exigida es de USD 20 (confirmado con el mensaje de validación real de Twilio "Enter an amount of at least $20" al intentar un monto personalizado menor), y que la recarga automática queda activada por defecto (+USD 50 adicionales cuando el saldo baje de $10) — nada de esto se pagó ni se confirmó. Se refutó con evidencia real la teoría del fundador de que la cuenta de Twilio "se creó para otro país" (el selector de país de facturación de Twilio sí acepta Colombia) — no era necesario borrar/recrear la cuenta. El fundador decidió explícitamente (a pedido directo, no asumido): **para el piloto, quitar el paso de código por completo** — el registro/login queda solo con el número de celular, sin ningún OTP. Implementado por `REUSE` (no se construyó nada nuevo): se retomó el mecanismo ya existente y ya probado de cuenta-sombra (antes gateado a `VITE_DEV_TEST_PHONES`, un whitelist fijo de números de prueba) y se generalizó a cualquier número — `signInDevShadowUser`→`signInByPhoneOnly()` en `proyecto-mensajeria/src/lib/actions/auth.ts` ahora crea/inicia sesión real de Supabase Auth (email/contraseña sintéticos derivados del número, vía alias "+" sobre `VITE_SHADOW_EMAIL_BASE`) para cualquier celular, sin pedir código. `PhoneStep.tsx` llama directo a `signInByPhoneOnly` y salta a "email"; el paso "otp" se quitó del router (`routes/index.tsx`) pero `OtpStep.tsx` y las funciones reales `requestPhoneOtp`/`verifyPhoneOtp` (contra `signInWithOtp`/`verifyOtp` de Supabase) se dejaron intactas y sin usar, documentadas para reconexión futura. **Riesgo de seguridad aceptado a propósito y documentado en el código** (no escondido): cualquiera que escriba un número ajeno puede entrar como si fuera esa persona — no hay prueba de posesión del teléfono. Aceptable solo para este piloto cerrado y pequeño (1 ambulancia + 5-10 conductores conocidos); NO debe usarse así en un lanzamiento público. `VITE_SHADOW_EMAIL_BASE` pasó a ser una variable **obligatoria** (antes opcional bajo otro nombre) — se agregó como `ARG` nuevo en el `Dockerfile` con comentario explicando que sin ella el registro falla en silencio para todo el mundo en producción. Se corrigió además una referencia colgante real introducida por el propio cambio: `EmailStep` en `routes/index.tsx` apuntaba su `onBack` al paso "otp" ya removido (caería al `default` de vuelta a la app) — se cambió a volver a "phone". Verificado real: `typecheck` limpio, lint del archivo tocado (`routes/index.tsx`) sin errores nuevos, `build` real de producción exitoso con `VITE_SHADOW_EMAIL_BASE` seteado, y smoke test real del servidor construido (`bun .output/server/index.mjs` respondiendo HTTP 200). | `proyecto-mensajeria/src/lib/actions/auth.ts`, `proyecto-mensajeria/src/components/onboarding/PhoneStep.tsx`, `proyecto-mensajeria/src/routes/index.tsx`, `proyecto-mensajeria/.env.example`, `proyecto-mensajeria/Dockerfile` |
| 2026-09-02 (21) | El fundador probó el flujo ya sin OTP de celular y quedó atascado en la pantalla de correo ("Revisa tu bandeja... no llega el código"): confirmado en el código real que ese paso siempre fue simulado (`requestEmailVerification`/`verifyEmailCode` en `proyecto-mensajeria/src/lib/actions/auth.ts` solo simulan un delay, nunca llaman a ningún proveedor de correo real — documentado así desde antes de este piloto), así que ningún correo real se envía nunca; el botón "Ya toqué el enlace del correo" ya llamaba a `submitCode("123456")`, que siempre pasa. El fundador decidió (pregunta directa, opción recomendada) aplicar el mismo criterio que ya se usó con el celular: **quitar también el paso de correo para el piloto**, en vez de solo aclarar que el botón de atajo funciona igual. Implementado en `proyecto-mensajeria/src/routes/index.tsx`: se quitaron los `case "email"`/`case "email_verify"` y el import de `EmailStep`/`EmailVerifyStep` del router — `case "phone"` salta directo a `"profile"`, y `ProfileStep.onBack` vuelve a `"phone"` (no a un paso fantasma, mismo bug-class ya corregido en la decisión (20) para "otp"). `EmailStep.tsx` y las funciones simuladas de correo en `auth.ts` se dejaron intactas y sin usar, con el mismo criterio de reconexión futura. No hizo falta tocar `AppStore.tsx`: `completeOnboarding()` ya trataba el correo como opcional (`onboardingDraft.email.trim() || null`), así que un `onboardingDraft.email` vacío no rompe el guardado del perfil. Verificado real: `typecheck` limpio, `eslint` del archivo tocado sin errores (con el `.prettierrc` real del proyecto, printWidth 100 — ver decisión (20)), `build` real de producción exitoso y smoke test del servidor construido respondiendo HTTP 200. | `proyecto-mensajeria/src/routes/index.tsx` |
| 2026-09-02 (22) | Bug real encontrado en producción: al terminar el registro (pantalla de permiso de Cámara) salía `No se pudo guardar el perfil: duplicate key value violates unique constraint "profiles_phone_key"`. Investigado en la base real (no adivinado): al renombrar en la decisión (20) el patrón de correo/contraseña sintéticos de `phoneOnlyCredentials()` (`+dev-${digits}` / `dev-test-phone-...` → `+u-${digits}` / `phone-only-...`), los 2 usuarios que ya se habían registrado antes (`+573024330410` y `+573150153636`, creados el 2026-08-18 bajo el patrón viejo) dejaron de coincidir: `signInByPhoneOnly` ahora genera un correo sintético nuevo para ese mismo celular, Supabase Auth lo trata como una cuenta distinta (id de `auth.users` distinto), y al intentar guardar el perfil con ese id nuevo, el `upsert` (que hace conflicto por `id`, no por `phone`) intenta un INSERT que choca con la fila de `profiles` que ya existía con ese `phone` bajo el id viejo. Se encontró además, al diagnosticar, una cuenta huérfana real creada por este mismo bug minutos antes (`compramosfacil.com+u-573024330410@gmail.com`, sin fila en `profiles`, sin datos reales). Corregido directo en la base de datos real (sin cambiar código, sin necesidad de redeploy): se borró la cuenta huérfana (`auth.identities`/`auth.sessions`/`auth.refresh_tokens`/`auth.users`, verificado antes que no tuviera fila en `profiles` ni en otras tablas) y se migraron las 2 cuentas reales existentes al patrón nuevo — mismo `id` de `auth.users` (por lo tanto mismo `profiles.id`, RLS/chats/contactos intactos), solo se les actualizó `email` e `encrypted_password` (vía `extensions.crypt()`/`pgcrypto`, ya instalado en el proyecto) al valor que `phoneOnlyCredentials()` genera hoy. Verificado real: `select encrypted_password = crypt(...)` confirmó que las 2 contraseñas nuevas coinciden con el hash guardado; una consulta final a `auth.users` confirmó que son los únicos 2 usuarios del proyecto y ambos quedaron consistentes con el código actual — no debería volver a pasar a menos que el patrón de `phoneOnlyCredentials()` se vuelva a renombrar sin migrar las cuentas existentes (documentado aquí como advertencia para la próxima vez). | `proyecto-mensajeria/src/lib/actions/auth.ts` (sin cambios de código; fix aplicado directo en Supabase, proyecto `wrkuusacwkdazfwynhkz`) |
| 2026-09-02 (23) | El fundador, a punto de salir a probar en la calle, preguntó puntualmente cómo se entera de un recordatorio por ubicación si no hay push — y notó correctamente que si va a estar manejando con el asistente de voz activo, el asistente le debería avisar hablado, o si no "no se estaría cumpliendo con la propuesta". Se investigó el código real (no se asumió): `remindersTriggered` (ack de `location:update`) y `reminder:due` (nota con hora fija, ADR-0030) solo alimentaban `NotificacionesScreen` — una tarjeta de texto muda en la sub-pestaña "Alertas" — sin sonido, voz ni push; y `useGeminiVoiceSession` (el micrófono/sesión Gemini Live) es un sistema totalmente aparte del socket `/location`, así que ni siquiera con una conversación de voz activa el asistente se enteraba de un recordatorio disparado. El fundador confirmó (opción recomendada, antes de salir) conectar esto YA. Corregido con `REUSE`: se usó `speechSynthesis` nativo del navegador (Web Speech API) en `useCopilotoRealtime.ts`, sin depender de que haya sesión Gemini Live activa ni tocar su pipeline de audio (que usa su propio `AudioContext`/PCM — confirmado que no hay conflicto real entre los dos sistemas) — apenas llega `remindersTriggered` o `reminder:due` por el socket, además de guardarse en el feed visual como ya hacía, se anuncia en voz ("Recordatorio: ..."). A propósito NO se incluyó `corridor:alert` (alerta de corredor de emergencia) en este cambio — es una superficie más sensible (ver skill de ingeniería del proyecto) y el fundador no lo pidió; queda como siguiente paso si lo confirma explícitamente. Verificado real: `typecheck` limpio, `eslint` del archivo tocado sin errores, `build` de producción exitoso y smoke test del servidor respondiendo HTTP 200 — la prueba de que realmente suena en un dispositivo real queda para cuando el fundador salga a probarlo. | `proyecto-mensajeria/src/hooks/useCopilotoRealtime.ts` |
| 2026-09-02 (24) | El fundador probó en la calle y encontró dos problemas reales sobre lo ya construido en la decisión (23): (1) el asistente de voz calculó una ruta y dijo dos veces "he calculado la ruta... deberías poder verla en tu aplicación de Google Maps", pero no se abrió nada — preguntó explícitamente por qué "me dijo que hizo algo cuando en realidad no lo hice"; (2) preguntó por qué el rastreo de ubicación no seguía activo al salir de la pestaña Copiloto hacia Chats/Contactos/Notas. Confirmado en el código real: `calculate_route` (`calculate-route.tool.ts`) está documentada como solo lectura a propósito — nunca abrió nada, y `startSession()` en `gemini-live.service.ts` nunca mandaba `systemInstruction`, así que el modelo tenía libertad total para narrar acciones que nunca ejecutó. Por separado, `CopilotoTab.tsx` llamaba `useCopilotoRealtime()` directamente — el socket `/location` y el `watchPosition()` real se cerraban en el cleanup del hook cada vez que esa pestaña se desmontaba, que es justo lo que pasa al cambiar de pestaña principal (`MainShell` solo renderiza una pestaña a la vez). El fundador confirmó ambas correcciones (dos preguntas, ambas opción recomendada): que el rastreo siga activo en toda la app, y que el asistente sí abra Google Maps de verdad. Implementado: (a) `OpenNavigationTool` nueva (`open-navigation.tool.ts`, mismo patrón de DI que `calculate-route.tool.ts`) geocodifica el destino y arma un link real de "Google Maps URLs" (`https://www.google.com/maps/dir/?api=1&origin=...&destination=...&travelmode=...`, formato verificado contra `developers.google.com/maps/documentation/urls/get-started`, reusando `travelModeFromDrivingMode` ya existente) — registrada en `assistant.module.ts`/`assistant-tools.service.ts`; (b) `GeminiLiveCallbacks.onToolResult` nuevo en `gemini-live.service.ts`, invocado en `handleMessage()` apenas se ejecuta cada tool call (antes de `sendToolResponse`), wireado en `assistant-voice.gateway.ts` como evento `voice:tool-result` — consumido en `useGeminiVoiceSession.ts`, que hace `window.open(mapsUrl)` solo cuando `name === "open_navigation"` y `status === "ok"` (el backend nunca podría abrir una pestaña por sí solo); (c) un `SYSTEM_INSTRUCTION` nuevo en español, wireado en la config de `startSession()`, que prohíbe explícitamente afirmar una acción antes de que su tool responda con éxito y aclara la diferencia real entre `calculate_route` (solo calcula) y `open_navigation` (la única que abre Maps); (d) rastreo hoisted vía `CopilotoRealtimeProvider`/`useCopilotoRealtimeContext` (mismo patrón `createContext`/`useContext` que `AppStore.tsx`, agregado a `useCopilotoRealtime.ts` — renombrado a `.tsx` por el JSX del provider), montado en `MainShell` (`routes/index.tsx`, envolviendo las 5 pestañas principales, no solo Copiloto — y no en `__root.tsx` porque antes de terminar el onboarding no hay sesión ni permisos de ubicación todavía); `CopilotoTab.tsx` ahora consume `useCopilotoRealtimeContext()` en vez de llamar el hook directo. Verificado real: `typecheck`/`eslint`/`build` limpios tanto en `backend/` como en `proyecto-mensajeria/` (un `as ToolOutcome` innecesario detectado por lint en `gemini-live.service.ts` se quitó — el tipo ya se infiere del retorno de `executeCall`). Nota operativa real de esta sesión: un `device_stage_files` de verificación de frescura, corrido a mitad de esta tarea, sobrescribió sin querer 5 de estos archivos con su versión vieja de Mac (por no haber sido sincronizados todavía) — se detectó por los avisos automáticos de "archivo cambió en disco" y se corrigió rehaciendo esas ediciones antes de verificar y sincronizar, sin pérdida real una vez detectado. Pendiente, honesto: no se pudo probar con micrófono real en este entorno (sin acceso a hardware real) — queda para que el fundador lo pruebe manejando. | `backend/src/modules/assistant/tools/open-navigation.tool.ts`, `backend/src/modules/assistant/assistant.module.ts`, `backend/src/modules/assistant/assistant-tools.service.ts`, `backend/src/modules/assistant/gemini-live.service.ts`, `backend/src/modules/assistant/assistant-voice.gateway.ts`, `proyecto-mensajeria/src/hooks/useGeminiVoiceSession.ts`, `proyecto-mensajeria/src/hooks/useCopilotoRealtime.tsx`, `proyecto-mensajeria/src/components/copiloto/CopilotoTab.tsx`, `proyecto-mensajeria/src/routes/index.tsx` |
| 2026-09-02 (25) | Bug real reportado por el fundador probando en la calle: creó "comprar unas pulpas" como recordatorio de ubicación por voz, pasó a ~100m del punto (recordatorio mostraba "Activo · 300m" en la UI) y nunca sonó ni apareció nada — "solo se quedó vacío". Investigado en el código real (no adivinado): `CreateLocationReminderTool.execute()` llama `LocationRemindersService.create()` DIRECTO — a diferencia de `LocationRemindersController.create()` (el único camino real hasta hoy para crear un recordatorio de ubicación desde la UI), que SIEMPRE llama `ReminderCacheService.invalidate()` justo después de crear. `GeofenceTriggerService.checkAndTrigger()` (evaluado en cada `location:update`) solo consulta esa caché de Redis (`reminders:pending:{userId}`, TTL 24h, ver su comentario real) — si ya estaba poblada por un `location:update` anterior (el caso real: el fundador ya llevaba el rastreo activo antes de crear el recordatorio por voz, con el fix de la decisión (24) de rastreo-en-toda-la-app), seguía sirviendo la lista VIEJA sin el recordatorio recién creado, así que el geofence nunca lo evaluaba — el recordatorio sí quedaba bien guardado en Postgres (por eso se veía "Activo" en la UI), el bug era puramente de caché desincronizada, no de guardado ni de GPS. Confirmado leyendo los tres archivos reales del flujo (`create-location-reminder.tool.ts`, `location-reminders.service.ts` — sin ninguna llamada a caché — y `reminder-cache.service.ts`), no solo inferido. Corregido con el mismo patrón que ya usa el controller: se inyectó `ReminderCacheService` en `CreateLocationReminderTool` y se llama `cache.invalidate(ctx.userId)` justo después de `reminders.create()`; `ReminderCacheService` no estaba exportado por `LocationRemindersModule` (solo `LocationRemindersController`, en el mismo módulo, podía usarla) — se agregó a `exports`. Se revisó `CreateNoteReminderTool` (la otra tool de voz que crea recordatorios) para descartar el mismo bug ahí: crea `kind: "note"`, que `listPendingForCache` excluye a propósito (`.eq("kind","location")`) y cuyo aviso a hora fija pasa por `NoteReminderSchedulerService`/BullMQ, un mecanismo aparte que esa tool ya invoca bien — no necesita el mismo fix. Verificado real: `typecheck`/`eslint`/`build` del backend limpios. Nota operativa real de esta sesión: al re-verificar frescura de `location-reminders.module.ts` con `device_stage_files` (nunca se había staged antes de editarlo), se sobrescribió sin querer la edición recién hecha con la versión vieja de Mac — detectado por el aviso automático de "archivo cambió en disco" y corregido rehaciendo esa edición puntual antes de verificar/sincronizar. Pendiente: que el fundador confirme en la calle que ahora sí suena. | `backend/src/modules/assistant/tools/create-location-reminder.tool.ts`, `backend/src/modules/location-reminders/location-reminders.module.ts` |
| 2026-09-03 (26) | Bug real reportado por el fundador (probado por una persona nueva registrándose): guardó un contacto nuevo como "mi amor" en su libreta y, SIN haber abierto un chat con esa persona todavía, le pidió al asistente de voz que le mandara un mensaje — no lo envió porque no encontró un chat. Al abrir el chat manualmente por la UI y repetir la petición por voz, pedir el mensaje "por el nombre como se registró esa persona en la plataforma" (el nombre visible en el chat) no lo encontró, pero pedirlo "como lo había guardado" (su propio nombre de contacto) sí lo envió. Investigado en el código real (no adivinado), dos causas distintas: (1) `MessagingService.resolveChatByContactName()` solo buscaba un chat YA EXISTENTE entre los dos y devolvía `not_found` si no había uno — a diferencia de la UI (`proyecto-mensajeria/src/lib/actions/chats.ts`, `findOrCreateIndividualChat`), que si el contacto es conocido pero nunca han hablado, SÍ crea el chat 1 a 1 ahí mismo. Fix: se agregó `createIndividualChat()` a `MessagingService` con el MISMO patrón ya probado en `findOrCreateIndividualChat` (insertar `chats` y luego `chat_participants` SIN encadenar `.select()`, por la misma razón de RLS documentada allá: la política `is_chat_participant` todavía no ve la fila de participante recién insertada dentro del mismo INSERT) — `resolveChatByContactName` ahora crea el chat si el contacto es conocido pero no hay uno compartido. (2) La segunda parte NO es un bug aislado del asistente sino una inconsistencia real de nombres en TODA la plataforma, confirmada leyendo tanto el backend (`messaging.service.ts`, `listChats()`) como el frontend real (`proyecto-mensajeria/src/lib/actions/chats.ts`, líneas ~401 y ~452): el título de un chat SIEMPRE se arma con `profiles.display_name` (el nombre con el que la otra persona se registró en la plataforma), nunca con `contacts.display_name` (el nombre que el usuario le puso en SU libreta) — así es en toda la UI (lista de chats, header del hilo, ajustes del chat), no solo en el puente de voz. El asistente, en cambio, SIEMPRE buscó por `contacts.display_name` (correcto para una libreta de contactos, ver bug 2026-08-31 de esta misma tabla) — de ahí el choque: lo que el usuario VE en el chat (nombre de plataforma) no es lo que el asistente ENCUENTRA (nombre de libreta). Se le preguntó directo al fundador si quería que el título del chat prefiriera el nombre de libreta — dijo que sí. Fix implementado (mismo día): se agregó `MessagingService.resolveDisplayNames(viewerId, otherIds)` (helper compartido, prioriza `contacts.display_name` de `viewerId` sobre `profiles.display_name`, con fallback a "Usuario") y se usa ahora tanto en `listChats()` como en `getRecentTextMessages()` (antes esta última también mostraba `senderName` con el nombre de plataforma). En paralelo, mismo criterio aplicado en el frontend real (`proyecto-mensajeria/src/lib/actions/chats.ts`): `fetchChatsAndMessages` trae en un query aparte los contactos propios del viewer para los `otherUserIds` de sus chats y arma `contactNameByOtherId`, que gana sobre `otherProfile?.display_name` al calcular `title`; `fetchSingleChat` hace el mismo query puntual (una sola persona) en paralelo con el de `profiles`. No se tocó `findOrCreateIndividualChat` — ya recibía `contact.displayName` como `title` desde sus dos call sites reales (`ChatListScreen.tsx`, `routes/index.tsx`), así que ya estaba bien. Se ajustó el mensaje de error de `SendMessageTool` para reflejar la causa real que queda (contacto no guardado en la libreta, ya no "no encontré un chat"). Verificado real: `typecheck`/`eslint` limpios en backend Y en proyecto-mensajeria (3 errores de formato `prettier/prettier` en `chats.ts`, corregidos con `eslint --fix`, sin tocar lógica). Pendiente aparte, reportado el mismo día: "Internal server error" en `/Modo de manejo` (`GET/POST vehicles`, `vehicles/driving-mode`) — se revisó `VehiclesController`/`VehiclesService`/`DrivingModeService` completos sin encontrar ningún defecto de código, y se consultaron los logs reales de Postgres del proyecto Supabase "Copiloto" (`query_logs`, últimas 24h) sin ningún error registrado que toque `user_vehicles` ni logs de severidad ERROR — no hay evidencia de un bug de código ni de Postgres; más probable un problema transitorio del lado de Railway (cold start, reinicio), pero no se puede confirmar sin logs de Railway, a los que no tengo acceso desde aquí. Pendiente: que el fundador confirme si el error se repite. | `backend/src/modules/messaging/messaging.service.ts`, `backend/src/modules/assistant/tools/send-message.tool.ts`, `proyecto-mensajeria/src/lib/actions/chats.ts` |
| 2026-09-03 (27) | Seguimiento real del "Internal server error" (decisión 26): el fundador confirmó que SIGUE pasando, ahora también en Notas (no solo Modo de manejo), y preguntó si su moto placa URU89E se había desregistrado. Verificado directo en Postgres (`execute_sql` sobre el proyecto Supabase "Copiloto"): la fila de `user_vehicles` para URU89E existe intacta (creada 2026-08-27, `vehicle_type: motorcycle`, nunca borrada) — el dato NO se perdió; lo que falla es que `GET /vehicles` no puede traerlo (de ahí que la pantalla muestre "Registrar" para los dos vehículos, como si no hubiera ninguno). Investigación del "Internal server error" en sí: (1) el texto literal "Internal server error" es el mensaje default de NestJS para una excepción NO manejada (no un `HttpException`) — `ModoManejoScreen.tsx` renderiza `error`/`actionError` tal cual viene del backend (`err.message`), así que confirma que es una excepción real sin capturar, no un 401/403 de auth (esos sí traen su propio mensaje, ej. "Token inválido o expirado") ni un fallo de red (ese cae al mensaje genérico `Error ${status} llamando a ${path}` del lado del frontend). (2) Se revisaron los logs reales de Supabase: `postgres_logs` sigue sin ningún error que toque `user_vehicles` ni severidad ERROR; `auth_logs` sí tiene eventos reales de "token is expired" (`bad_jwt`, 403) para el mismo `actor_id` del fundador, pero TODOS con `referer: http://localhost:3000` — o sea tráfico de desarrollo local, no de la app en producción (`...railway.app`) que el fundador está usando — así que no se puede conectar ese hallazgo al bug reportado sin adivinar. Conclusión honesta: el bug es real y confirmado como excepción de backend no manejada, pero señala hacia algo que pasa DENTRO de Railway (el proceso Node del backend) antes o durante la llamada a Supabase — sin logs de aplicación de Railway (a los que no tengo acceso desde aquí) no se puede aislar la causa exacta sin adivinar, así que no se aplicó ningún fix a ciegas para esto. Aparte, se investigaron dos reportes más del mismo mensaje del fundador: (a) el recordatorio de ubicación SÍ se disparó (confirmado: "Disparada"/"Cumplida" en Notas) pero el aviso de voz "casi no se escuchó" manejando con un intercomunicador Bluetooth — causa real más probable: `speechSynthesis` en navegador móvil se frena/enmudece cuando la pantalla se bloquea, y no había ningún mecanismo que evitara eso. Fix aplicado (con confirmación del fundador): Screen Wake Lock API (`navigator.wakeLock`) solicitado apenas arranca el rastreo de ubicación activo en `useCopilotoRealtime`, liberado al detener el rastreo, y re-solicitado en `visibilitychange` (el Wake Lock se libera solo cuando la pestaña se oculta) — feature-detectado, si el navegador no lo soporta simplemente no hace nada, no bloquea el rastreo. También se fijó `utterance.volume = 1` explícito en `speak()`, que antes no fijaba volumen. No es garantía total (el enrutamiento de audio hacia un Bluetooth externo tiene límites propios del navegador/SO fuera de este alcance), pero reduce el caso más común (pantalla bloqueada). (b) El asistente de voz de Copiloto no escuchaba al fundador usando su intercomunicador Q58 MAX, aunque el mismo intercomunicador sí funciona bien para llamadas, notas de voz de WhatsApp y YouTube — confirmado que usa iPhone + "Chrome". Diagnóstico (no fix, es una limitación real de plataforma, no un bug de código): Chrome en iOS corre sobre el motor WebKit de Apple (política obligatoria de Apple para TODOS los navegadores en iOS, Chrome incluido) — mismo motor que Safari, con las mismas limitaciones reales conocidas de iOS/WebKit para enrutar el micrófono de un Bluetooth externo hacia `getUserMedia` de una página web: WhatsApp/Teléfono/YouTube usan APIs nativas de iOS (`AVAudioSession`) que sí tienen control total sobre el enrutamiento de audio Bluetooth, mientras que una página web en iOS depende de lo que WebKit decida exponer, que históricamente ha sido inconsistente con micrófonos Bluetooth externos — a veces cae en silencio al micrófono del propio iPhone sin ningún error visible. `useGeminiVoiceSession.ts` ya usa `getUserMedia({ audio: true })` sin restringir el dispositivo, que es lo correcto para no forzar nada; no hay una API web que le ordene a iOS "usa el micrófono Bluetooth" — esa decisión la toma el sistema operativo. No se aplicó ningún cambio de código para esto por ser un límite real de la plataforma, no algo resoluble con JavaScript. | `proyecto-mensajeria/src/hooks/useCopilotoRealtime.tsx` |
| 2026-09-03 (28) | CAUSA RAÍZ CONFIRMADA del "Internal server error" (decisiones 26 y 27) — ya no es un pendiente. El fundador descargó los logs reales de Railway (exportados a JSON, `logs.<timestamp>.json`) y los dejó en su carpeta de Descargas; se le pidió revisar ahí porque el hallazgo previo (auth_logs de Supabase) no daba para más sin adivinar. Encontrado literal en los logs: `error: ERR max requests limit exceeded. Limit: 500000, Usage: 500000. See https://upstash.com/docs/redis/troubleshooting/max_requests_limit for details` — repetido ~1 vez por segundo durante toda la ventana de logs (no un blip puntual, un estado sostenido). Es Upstash (el proveedor de Redis usado por el backend en Railway) rechazando TODO comando porque la cuenta agotó su cuota de peticiones del período (500.000 de 500.000). Cada comando de Redis lo confirma: se ve tanto en llamadas simples (ej. `GET driving:mode:*` de `DrivingModeService`, explica el error en Modo de manejo) como en el script Lua interno de BullMQ `addJobScheduler` (usado por `NoteReminderSchedulerService` para los recordatorios con hora fija, explica el mismo error en Notas) — confirma que es un solo problema de infraestructura, no dos bugs de código distintos, y que afecta a CUALQUIER función que toque Redis (Modo de manejo, recordatorios de Notas con hora fija, geofence/rastreo de ubicación, corredor de emergencia). El dato de `user_vehicles` nunca estuvo en riesgo (ver decisión 27) — Postgres ni se llega a tocar cuando Redis ya rechazó el comando antes. No hay ningún cambio de código que arregle esto: es un límite de cuenta/plan de Upstash, no un bug — se le indicó al fundador revisar su dashboard de Upstash (revisar si el plan es de cuota mensual/diaria y cuándo resetea, o si necesita subir de plan) como único paso real para resolverlo. | (ninguno — hallazgo de infraestructura, no de código) |
| 2026-09-03 (29) | Análisis de costos reales de infraestructura para producción (sin implementar todavía — queda como referencia para decisiones futuras), a pedido directo del fundador ("necesito saber los costos de produccion de al menos unos 100 usuarios al dia"). Cuantificado con evidencia de código real (no adivinado): cada `location:update` cuesta ~6 comandos reales de Redis (rate limit + `LocationStateService.getCurrent/setCurrent` + `RouteSessionService.getActive` + `ReminderCacheService.getPending`), y el polling de `/emergency/corridor/candidates` (modo ambulancia, cada `AMBULANCE_POLL_INTERVAL_MS=8000`) cuesta ~535 comandos reales por consulta según el propio loadtest documentado en `emergency-corridor.service.ts`. El fundador aclaró un dato real de operación que cambia la escala de este segundo número: una ambulancia solo activa el corredor durante una emergencia real y desplazamiento real (no 8h continuas) — en Medellín, la ruta más larga tomaría del orden de 1 hora con el corredor abierto — así que el costo real de corredor en producción es por evento corto, no continuo; sí se necesita presupuestar para las PRUEBAS controladas (ver ítem (31) más abajo). Investigado con fuentes oficiales/primarias vigentes (Google, Supabase, Railway, Google AI): Google Routes `computeRoutes` y Geocoding API cuestan $5.00/1.000 solicitudes cada uno (tier "Cap - 100.000"); Supabase Pro es $25/mes (8GB DB, 100k MAU, 250+250GB egress); Railway cobra por cómputo/RAM/egress (~$20/vCPU/mes, ~$10/GB RAM/mes), sin cobro por comando — relevante porque Redis propio en Railway sería estructuralmente más barato que Upstash si el tráfico de corredor crece, al no cobrar por comando. Hallazgo más importante, verificado en `useGeminiVoiceSession.ts` (no adivinado): el micrófono manda audio real a Gemini Live de forma CONTINUA desde `voice:ready` hasta `stop()` — sin ningún gating de silencio del lado del cliente (`processor.onaudioprocess` manda un chunk cada ~85ms sin importar si el usuario está hablando). Confirmado con el foro oficial de Google AI Developers que Gemini Live SÍ factura el silencio al mismo precio que el habla real ("periods of silence are indeed billed at the same rate as periods when a user is actively speaking", 32 audio tokens/segundo fijo) — el costo real por usuario depende de CUÁNTO TIEMPO queda abierta la sesión (audio de entrada, $0.005/min, incluye silencio), no de cuánto habla la persona; el audio de SALIDA ($0.018/min) sí solo se factura mientras Gemini genera voz real. Esto señala una optimización de costo real y concreta, pendiente de implementar si se confirma con el fundador: cerrar/pausar automáticamente la sesión de Gemini Live tras un período de inactividad/silencio, en vez de dejarla abierta todo el trayecto. Comparado contra alternativas reales de mercado (no self-hosted, que exige GPU 24/7 con costo fijo alto sin importar el uso, mala opción a esta escala): OpenAI Realtime (`gpt-realtime-2.1-mini`) sale más caro combinado (~$0.019/min entrada + ~$0.077/min salida) que Gemini Live; Deepgram Voice Agent API (bundle STT+LLM+TTS) también sale más caro en conjunto (~$0.056-0.075/min) que el costo real efectivo de Gemini hoy — ninguna alternativa evaluada resultó más barata que arreglar primero el problema de la sesión abierta sin gating de silencio. Estimado total para 100 usuarios/día con uso "frecuente" (supuestos declarados, no medidos): entre ~$300 y ~$770/mes, dominado por Gemini Live (voz), no por Redis/Maps como se pensaba inicialmente. Fuentes primarias usadas: `ai.google.dev/gemini-api/docs/pricing`, `developers.google.com/maps/billing-and-pricing/pricing`, foro `discuss.ai.google.dev` (facturación de silencio), `supabase.com/pricing`, `railway.com` (vía terceros que citan sus tarifas oficiales). | (ninguno — análisis de costos, sin cambios de código todavía) |
| 2026-09-03 (30) | Idea nueva propuesta por el fundador, anotada para evaluar implementación (no implementada todavía): detección de accidente por los sensores del propio celular (acelerómetro/giroscopio, vía `DeviceMotion`/`DeviceOrientation` del navegador — no depende del hardware ESP32 de "Copiloto versión 2") que, al detectarse un posible accidente, envíe automáticamente una alerta a la EPS (aseguradora de salud) del usuario con la MISMA información que ya se definió para el flujo de "llamar a la policía" (ubicación, nombre y apellido — el detalle exacto de qué otros campos se envían queda por confirmar contra `assistant/tools` reales cuando se implemente). El fundador también pidió un panel/dashboard para que EPS o instituciones de emergencia puedan ver estas alertas — mismo patrón que ya se venía pensando para entregarle a la policía un panel de administrador (dado que no existe API pública de policía ni de EPS, ver ítem (31)); tiene sentido evaluar un solo panel de "Instituciones de Emergencia" con roles distintos (policía/EPS/despachador de ambulancias) en vez de dos sistemas separados. Pendiente real antes de implementar: (a) definir el algoritmo de detección de accidente por sensores del teléfono (umbral de desaceleración/impacto — riesgo real conocido de falsos positivos en este tipo de detección, no es un problema resuelto trivialmente) y su panel de pruebas para que el fundador pueda validarlo como dueño antes de dárselo a nadie más; (b) definir el contrato real de qué se le manda a la EPS y bajo qué autorización/consentimiento del usuario (dato de salud, aplica tratamiento especial). No hay código todavía — queda documentado aquí para no perder la idea entre sesiones. | (ninguno — idea documentada, no implementada) |
| 2026-09-03 (31) | El fundador quiere hacer un piloto/experimento controlado (no lanzamiento público) con 10 personas (mezcla de carros y motos) más 1 ambulancia, simulando una emergencia real y activando el corredor de emergencia con las 10 personas repartidas en la ruta — antes de considerar cualquier lanzamiento más amplio (ver ítem (29) sobre por qué 100 usuarios de una vez no es lo recomendado todavía). Requisito real bloqueante, verificado: la cuenta de Upstash sigue en el límite agotado de 500.000 comandos/mes (decisión 28) — mientras eso no se resuelva (subir de plan o mover Redis), el corredor de emergencia SEGUIRÁ fallando con el mismo "Internal server error" durante la prueba, sin importar que sean solo 11 usuarios. A esta escala (11 usuarios simultáneos, unas pocas horas), el costo de CUALQUIERA de los planes evaluados en (29) es insignificante — no es un problema de costo, es un bloqueo real de cuota que hay que resolver antes de la fecha de la prueba. El fundador confirmó que lo resuelve él mismo (plan de Upstash). | (ninguno — pendiente de que el fundador resuelva el plan de Upstash antes de la prueba) |
| 2026-09-03 (32) | Implementados los dos cambios "acotados" confirmados por el fundador de la decisión (29): (1) **Gate de silencio en Gemini Live** (`useGeminiVoiceSession.ts`): se agregó un umbral simple de energía (RMS) sobre cada buffer real de micrófono (`processor.onaudioprocess`) — mientras el RMS se mantenga bajo `SPEECH_RMS_THRESHOLD` (0.02) por más de `SILENCE_PAUSE_MS` (8000ms) seguidos, se deja de EMITIR el chunk de audio (no se manda a Gemini, no se factura); apenas el RMS vuelve a superar el umbral se retoma de inmediato (chequeo cada ~85ms). A propósito NO se cierra la sesión/el socket durante el silencio — solo se pausa el envío — para no reabrir la clase de bug real ya documentada en este mismo archivo sobre reconexión y sesiones duplicadas. Valores del umbral y del tiempo de espera SIN CALIBRAR con manejo real todavía (ruido de viento/motor en moto puede exigir ajustarlos) — pendiente que el fundador lo pruebe manejando. (2) **`manifest.json` + instalación real** (`proyecto-mensajeria/public/manifest.json`, íconos nuevos en `public/icons/` generados desde el `favicon.ico` existente por no haber todavía un ícono de marca propio — mismo pendiente de diseño ya anotado en una decisión anterior de este log —, y `src/routes/__root.tsx` con los links `rel="manifest"`/`rel="apple-touch-icon"` y los meta `apple-mobile-web-app-title`/`apple-mobile-web-app-status-bar-style` nuevos): habilita "Agregar a inicio" real en iOS Safari (instalable en el iPhone del fundador, confirmado que eso era lo que preguntaba) y es el requisito real de Google para empaquetar un TWA de Android más adelante. Verificado real: `tsc --noEmit`, `eslint` de los archivos tocados y `vite build` (incluye el build de Nitro/Cloudflare) los tres limpios; confirmado que `manifest.json` y los íconos quedan copiados dentro de `.output/public/` en el build real. Pendiente honesto: falta que el fundador lo abra en su iPhone real y confirme "Agregar a inicio" (no hay forma de probar Safari real desde este entorno). Aparte, dos preguntas reales del fundador quedan respondidas sin cambio de código todavía: (a) **Bluetooth en moto durante el piloto (10 personas, 5 motos)** — no es un problema de "Bluetooth en segundo plano" sino de si `getUserMedia()` logra enrutar el micrófono del intercomunicador (perfil HFP/SCO) en vez del micrófono del teléfono; en Android/Chrome esto suele funcionar bastante mejor que en iOS/WebKit (ya diagnosticado en la decisión (27)) pero NO está garantizado por modelo de intercomunicador/versión de Android — no hay forma de confirmarlo sin una prueba real con el teléfono y el intercomunicador de verdad de un motociclista del piloto, recomendado hacerla ANTES de la fecha del piloto, no el mismo día. Mitigación real disponible ya hoy sin código nuevo: el aviso HABLADO hacia el motociclista (alertas de geofence/corredor) usa audio de SALIDA, que sí enruta de forma confiable por Bluetooth incluso cuando el micrófono no — así que un motociclista puede participar del piloto (recibir alertas, ser rastreado en el corredor) aunque no logre darle comandos de voz al asistente mientras maneja. (b) **Datos exactos que exige la Línea 123/NUSE de Colombia al reportar una emergencia** — investigado con fuentes oficiales (Alcaldía de Bogotá, Secretaría de Seguridad): NO existe un documento público que liste el esquema exacto de campos exigidos (confirmado buscando explícitamente, no es que no se buscó) — lo que sí documentan las fuentes oficiales es que la ubicación se detecta automáticamente por el operador (Servicio de Localización de Emergencias) y que existe un chat silencioso para describir el hecho cuando no se puede hablar; el resto de campos típicos de un protocolo de despacho de emergencias (nombre de quien reporta, tipo de emergencia, número de personas involucradas, teléfono de contacto) es práctica estándar internacional, no un dato confirmado específico de NUSE Colombia. Como la tool real de "llamar a la policía" todavía no existe en este repo (solo `activate-emergency-corridor.tool.ts`), queda pendiente confirmar el esquema exacto de campos directamente con la Policía/NUSE cuando se construya esa tool, en vez de asumir un esquema no verificado. El fundador confirmó que resuelve el límite de Upstash por su cuenta y ya pagó el registro de Google Play Console (USD 25, pago único) — sin acciones pendientes de este lado para esos dos puntos. Sigue pendiente, a ejecutar después: el panel único de administración ("Instituciones de Emergencia", roles distintos policía/EPS/ambulancia) confirmado por el fundador — no se empezó a construir en esta decisión por ser una superficie nueva más grande (roles/auth, RLS, pantallas nuevas) que merece su propia auditoría antes de escribir código, siguiendo la regla del proyecto de "auditar antes de editar". | `proyecto-mensajeria/src/hooks/useGeminiVoiceSession.ts`, `proyecto-mensajeria/public/manifest.json`, `proyecto-mensajeria/public/icons/icon-192.png`, `proyecto-mensajeria/public/icons/icon-512.png`, `proyecto-mensajeria/public/icons/apple-touch-icon.png`, `proyecto-mensajeria/src/routes/__root.tsx` |
| 2026-09-03 (33) | El fundador corrigió al asistente: el esquema de datos del flujo "llamar a la policía" NO hay que buscarlo en documentación pública de NUSE/Línea 123 (decisión 32b) — hay que revisarlo en la documentación PROPIA de "Copiloto versión 2" ya cargada en este repo, que define su propio contrato, independiente de cualquier API externa. Releída directamente (no adivinado) la fuente real: el chat crudo (`fuente-bruta-historica/documento completo v2 contiene todo .md`, sección "8. Y conecta directamente con nuestra idea de Policía") y, más importante, los DOS documentos formales convertidos con `pandoc` desde `.docx` a texto plano (`COPILOTO_MASTER_SPECIFICATION_V1.docx` sección "8. Caso de uso prioritario — 'llamar a la policía'" y `COPILOTO_2030_ARQUITECTURA_ESTRATEGICA_V1.docx` sección "11. Emergencias") — las tres fuentes coinciden en el mismo paquete de datos, citado textual del master spec: **"Paquete: ID, usuario autenticado, dispositivo, ubicación, timestamp, tipo de emergencia, nivel de confianza y contactos autorizados."** Ninguna de las tres fuentes reales usa literalmente las palabras "nombre" ni "apellido" como campos separados — la identidad se define ahí como "usuario autenticado" (un solo campo lógico que resuelve contra la cuenta de la plataforma), no como nombre+apellido explícitos. Verificado además contra el esquema REAL de Postgres (`information_schema.columns` del proyecto Supabase "Copiloto", tabla `profiles`): hoy la tabla solo tiene `display_name` (un solo texto libre, ver decisiones (20)-(22) sobre el onboarding sin OTP — puede ser cualquier cosa que la persona haya escrito, no necesariamente "Nombre Apellido" estructurado), NO existen columnas separadas `first_name`/`last_name`. Conclusión honesta para cuando se construya esta tool: el paquete real ya definido en la documentación propia (ID, usuario autenticado, dispositivo, ubicación, timestamp, tipo de emergencia, nivel de confianza, contactos autorizados) es lo que hay que implementar — y si se quiere mandar nombre y apellido como campos separados y confiables (no un `display_name` libre) para que la policía/EPS puedan actuar sobre eso, hace falta agregar esos campos al perfil real (migración de schema), no asumir que ya existen. | (ninguno — corrección de lectura de documentación, sin cambios de código) |
| 2026-09-03 (34) | Implementado el flujo real "Copiloto, llama a la policía" (SOS por voz), a pedido explícito del fundador: "ve haciendo lo de sos 'llama a la policia'. y mirar a ver que si mande los datos al admin con datos reales de esa persona." Auditado antes de escribir código (REUSE, no PROPIO desde cero): se encontró que ya existe un patrón real de "panel de administrador" — `EmergencyAdminController` (`emergency/admin`, `@UseGuards(SupabaseAuthGuard, AdminGuard)`) y `AdminGuard` (autoriza por `ADMIN_USER_ID`, una sola persona, el fundador, sin sistema de roles genérico — "sería complejidad sin evidencia", ya usado desde 2026-09-01 para verificar ambulancias) — se REUSÓ tal cual, sin crear ningún mecanismo de auth nuevo. Implementado: (1) migración real `supabase/migrations/20260903190000_emergency_incidents.sql`, aplicada al proyecto real (`apply_migration`) — tabla `emergency_incidents` (id, user_id, type fijo 'policia', status creado/recibido/en_atencion/cancelado/cerrado, confidence_level, latitude/longitude/location_accuracy_meters, device — fijo 'app_movil' por no existir todavía el dispositivo ESP32 real de "Copiloto versión 2" —, snapshot_display_name/snapshot_phone/snapshot_email, authorized_contacts jsonb nullable por no existir todavía una fuente real de contactos de emergencia, created_at/updated_at), RLS habilitada con policy `emergency_incidents_select_own` (el dueño ve solo lo suyo) y a propósito SIN policy de insert/update para `authenticated` — mismo patrón exacto que `emergency_vehicles`. (2) `EmergencyIncidentsService` nuevo (`backend/src/modules/emergency/emergency-incidents.service.ts`, mismo módulo ya existente `EmergencyModule` — EXTEND, no módulo nuevo): `createPoliceIncident()` resuelve el perfil real (`profiles.display_name/phone/phone_country_code/email`) y lo guarda como FOTO real en el momento de crear el incidente (no una referencia que cambie si el usuario edita su perfil después) — responde directamente a "que mande los datos al admin con datos reales de esa persona"; `listAll()` para el panel. (3) `CallPoliceTool` nueva (`assistant/tools/call-police.tool.ts`, `name: "activate_police_sos"`, `requiresConfirmation: true` — mismo criterio de riesgo máximo que `ActivateEmergencyCorridorTool`, un "sí" mal interpretado por STT no debe crear un incidente real): exige ubicación real actual (`LocationStateService.getCurrent`, igual que la tool de corredor) antes de poder confirmar, y a propósito NO intenta llamar a ningún número ni API de policía real (no existe un canal institucional autorizado, ver decisión (33)) — el alcance real de este MVP es dejar el incidente creado con datos reales y visible para el admin. (4) Expuesto en `EmergencyAdminController` como `GET emergency/admin/incidents` (mismo controller/guard ya real de vehículos, solo se agregó el método) — el fundador ya puede consultarlo hoy mismo con su propia sesión autenticada. (5) Registrada en `AssistantToolsService`/`AssistantModule` (mismo patrón de DI que las demás 11 tools). Verificado real: `tsc --noEmit`, `eslint` de todos los archivos tocados y `nest build` los tres limpios (un error real de ESLint — `no-unsafe-assignment` al destructurar `{ data, error }` de un `.insert().select().single()` tipado `any` — corregido sin destructurar, usando `insertResult.data`/`insertResult.error`). Pendiente honesto, sin marcar como terminado: (a) no se pudo probar con voz real end-to-end en este entorno (mismo límite ya documentado para el resto de tools de voz — falta que el fundador lo pruebe hablando); (b) el panel único con roles (policía/EPS/ambulancia) sigue siendo el siguiente paso real — hoy el "admin" sigue siendo únicamente el fundador vía `ADMIN_USER_ID`, no hay UI todavía, solo el endpoint real. Nota operativa de esta sesión: el puente hacia la Mac del fundador se desconectó de nuevo justo al terminar de sincronizar — los archivos quedaron listos y verificados en el sandbox pero la sincronización a disco real quedó pendiente hasta que el puente se reconecte. | `supabase/migrations/20260903190000_emergency_incidents.sql`, `backend/src/modules/emergency/emergency-incidents.service.ts`, `backend/src/modules/emergency/emergency.module.ts`, `backend/src/modules/emergency/emergency-admin.controller.ts`, `backend/src/modules/assistant/tools/call-police.tool.ts`, `backend/src/modules/assistant/assistant-tools.service.ts`, `backend/src/modules/assistant/assistant.module.ts` |
| 2026-09-03 (35) | El fundador reportó un problema real de seguridad detectado en pruebas: "en las pruebas que yo hacia aveces me tocaba confirmar varias veces para enviar imaginese que lo necesite de emergerncia y siempre me pida lo mismo" — con `activate_police_sos` (decisión 34), a veces el asistente seguía pidiendo "¿confirmas?" después de que el fundador ya dijo que sí, sin llegar nunca a crear el incidente. Auditado el mecanismo de confirmación real (`gemini-live.service.ts` método `executeCall`, `assistant-tools.service.ts` método `execute`) — se confirmó que NO hay un bug determinístico de código específico de esta tool: el fix real de 2026-08-31 que lee `call.args?.["confirmed"] === true` sigue intacto y correctamente excluye SOLO a `activate_emergency_corridor` (excepción de seguridad permanente y deliberada, documentada en el código — activar un corredor real por voz sigue fuera de alcance a propósito). La causa más probable, evaluada honestamente: Gemini Live (el modelo) no vuelve a mandar `confirmed: true` de forma 100% confiable después de un "sí" verbal — es un riesgo de confiabilidad del modelo, no algo que se pueda "arreglar" leyendo más código del lado del backend. Implementada una red de seguridad de defensa en profundidad en `CallPoliceTool` (`backend/src/modules/assistant/tools/call-police.tool.ts`), reusando el mismo idioma real de Redis que ya usa el proyecto para ventanas de tiempo fijas (`SET`/`EXPIRE`, mismo patrón que `checkSocketRateLimit`/`AlertPolicyService`): al pedir confirmación por primera vez, se guarda una marca `sos:pending_confirm:<userId>` con TTL de 25s; si llega un SEGUNDO disparo real de `activate_police_sos` para el mismo usuario dentro de esos 25s (con o sin `confirmed: true` correctamente threaded por el modelo), se trata como confirmación real y se crea el incidente de inmediato, sin volver a preguntar. Justificación de seguridad: en una emergencia real, que la persona pida ayuda dos veces en segundos es señal suficiente — es más seguro actuar que dejarla atrapada repitiendo "sí" sin efecto mientras corre peligro. Esto es honestamente una mitigación de robustez, NO una corrección de causa raíz confirmada (no se pudo reproducir el bug exacto de forma determinística en este entorno sin voz real) — queda pendiente que el fundador confirme en su próxima prueba real si el problema desaparece. Verificado real: `tsc --noEmit`, `eslint` y `nest build` limpios. Aparte, el fundador pidió separar la cuenta de administrador de su cuenta de pruebas — aclarado con `AskUserQuestion`: eligió "Cuenta de admin distinta a mi cuenta de pruebas" (una cuenta real y separada solo para `ADMIN_USER_ID`, no un cambio de código) — pendiente real: falta que el fundador indique qué número de teléfono usará para esa cuenta nueva (no se puede inventar), para luego obtener su `id` real y darle la instrucción exacta de actualizar `ADMIN_USER_ID` en Railway. | `backend/src/modules/assistant/tools/call-police.tool.ts` |
| 2026-09-03 (36) | El fundador pidió, en dos partes: (1) "lo del admin hazlo como un dashboard aparte, que entre como con correo y con cuenta y listo y que ahi pueda ver todo, lo normal" — un panel de administrador TOTALMENTE separado de la app normal, con su propio login real de correo+contraseña; (2) BUG real reportado: "en todas las cuentas que se creaban aparecía la parte de admin y cuando le daban ahi decia que no tenian permisos... eso no lo deben ver ellos eso es mio como fundador" — la sub-pestaña "Admin" dentro de Copiloto (`CopilotoTab.tsx`) se mostraba SIEMPRE, en cualquier cuenta, y el backend la rechazaba con 403 real — confuso e indeseado para cualquier usuario de prueba que no fuera el fundador. Corregido de raíz sacando el panel de administrador de la navegación de la app normal por completo, no solo ocultándolo condicionalmente. Implementado: (1) nueva ruta real `/admin` (`src/routes/admin.tsx` + `src/components/admin/AdminDashboard.tsx`) con su propio login de correo+contraseña real (`AdminLoginScreen.tsx`, incluye "Crear cuenta" usando `supabase.auth.signUp` directo desde el navegador del fundador — a propósito NO le pedí su contraseña para crearla yo por MCP: eso violaría la regla de nunca manejar contraseñas, así que el flujo de creación de cuenta queda 100% en sus manos). (2) Aislamiento real de sesión: `src/lib/supabase/admin-client.ts` es un cliente de Supabase SEPARADO del de la app normal, con `auth.storageKey` distinto (`sb-copiloto-admin-auth`) — iniciar sesión en `/admin` NO pisa ninguna sesión de prueba abierta en la app normal en el mismo navegador, responde directamente al pedido de "separar lo del admin totalmente aparte de las pruebas que yo hago como usuario". (3) `src/lib/backend/client.ts` refactorizado de un objeto único a una fábrica `createBackendClient(supabaseClient)` (mismo comportamiento real para la app normal, cero cambio ahí) para poder reusarla en `src/lib/backend/admin-client.ts` (el mismo mecanismo real de fetch/errores, atado a la sesión separada de admin) sin duplicar código. (4) `useEmergencyAdmin` (hook ya existente) apuntado a `adminBackend` en vez del backend normal, y extendido para también traer `GET /emergency/admin/incidents` (el endpoint ya existía desde la decisión (34) pero no tenía pantalla — ahora sí, `AdminIncidentsPanel.tsx`, muestra nombre/teléfono/correo/ubicación reales de cada incidente). (5) El contenido real de la antigua `AdminAmbulanciasScreen.tsx` se movió tal cual a `components/admin/AdminVehiclesPanel.tsx` (sin cambio de comportamiento, ya no depende de `PhoneScreen`/sub-nav) y el archivo viejo se borró. (6) `CopilotoTab.tsx`: se quitó "admin" de `CopilotoSubTab`/`SUB_TABS` y su `case` — ningún usuario normal vuelve a ver esa pestaña. La autorización real de fondo NO cambió: sigue siendo 100% el backend (`AdminGuard`/`ADMIN_USER_ID`) quien decide quién es administrador — cualquiera puede crear una cuenta en `/admin`, pero solo la cuenta configurada del lado del servidor pasa del "Sin acceso". Verificado real: `tsc --noEmit`, `eslint` (con `--fix` para formateo, sin errores de lógica) y `vite build` completo (incluye Nitro/Cloudflare) los tres limpios; confirmado que `/admin` queda como ruta real generada por el plugin de TanStack Router en el build. Pendiente honesto: falta que el fundador entre a `/admin`, cree su propia cuenta de administrador (correo+contraseña que él decida — nunca se los pedí ni los voy a manejar) y me confirme el correo para que yo pueda buscar el `id` real de esa cuenta en Supabase y darle la instrucción exacta de actualizar `ADMIN_USER_ID` en Railway a ese valor. | `proyecto-mensajeria/src/routes/admin.tsx`, `proyecto-mensajeria/src/components/admin/AdminDashboard.tsx`, `proyecto-mensajeria/src/components/admin/AdminLoginScreen.tsx`, `proyecto-mensajeria/src/components/admin/AdminVehiclesPanel.tsx`, `proyecto-mensajeria/src/components/admin/AdminIncidentsPanel.tsx`, `proyecto-mensajeria/src/lib/supabase/admin-client.ts`, `proyecto-mensajeria/src/lib/backend/admin-client.ts`, `proyecto-mensajeria/src/lib/backend/client.ts`, `proyecto-mensajeria/src/hooks/useEmergencyAdmin.ts`, `proyecto-mensajeria/src/components/copiloto/CopilotoTab.tsx` (eliminado: `proyecto-mensajeria/src/components/copiloto/AdminAmbulanciasScreen.tsx`) |
| 2026-09-04 (37) | El fundador confirmó que hizo `git commit`/`git push` real de todo lo pendiente (decisión (36) y anteriores) — corrigiendo también una pregunta legítima del fundador: por qué nunca se había hecho commit/push automáticamente en esta sesión. Respuesta honesta dada: la regla de seguridad de este asistente es NO hacer `git commit`/`git push` salvo que el usuario lo pida explícitamente — todo el trabajo anterior quedó escrito real y verificado en el disco de la Mac del fundador (vía el puente de sincronización de archivos), pero nunca subido a GitHub hasta que el fundador lo pidió y lo corrió él mismo. Después de eso, el fundador compartió el archivo real `logs.<timestamp>.json` descargado de Railway (carpeta Descargas) pensando que el 404 de `/admin` era por el límite de Redis — se le aclaró que NO, el 404 era simplemente porque el código nuevo no estaba subido a GitHub todavía (ya resuelto con su push). PERO al leer ese archivo de logs completo (1000 líneas, 525 de severidad error, ventana real de ~14 segundos) se encontró un hallazgo MUCHO más grave, real y confirmado con evidencia: el backend estaba en un **loop de reinicio infinito total** — "Starting Nest application..." cada 1-3 segundos, sin llegar NUNCA a "escuchando en :PORT". Causa raíz real encontrada leyendo el código (no adivinada): `CorridorExpirySweepProcessor.onModuleInit()` (`emergency-corridor/corridor-expiry-sweep.processor.ts`) hace `await this.queue.upsertJobScheduler(...)` SIN try/catch durante el arranque del módulo; con la cuota de Upstash todavía agotada (mismo hallazgo real de la decisión (28), confirmado en los logs: `ERR max requests limit exceeded. Limit: 500000, Usage: 500000`, IDÉNTICO, no resuelto todavía pese a que el fundador dijo que lo solucionaría), ese `upsertJobScheduler` lanza una excepción real; como `onModuleInit` se espera dentro de `NestFactory.create()` en `main.ts`, y `bootstrap()` se llama como `void bootstrap()` SIN `.catch()`, el rechazo queda sin manejar y Bun mata el proceso completo — Railway lo reinicia de inmediato, vuelve a fallar exactamente igual, loop infinito. Conclusión real y grave: esto significa que TODO el backend (no solo funciones que usan Redis) ha estado inaccesible — cualquier endpoint, incluyendo los nuevos de hoy (SOS policía, panel de admin) — desde que la cuota de Upstash se agotó. Fix de código real aplicado (grep confirmó que este es el ÚNICO `onModuleInit`/`OnApplicationBootstrap` de todo el backend, o sea el único punto de falla de este tipo): se envolvió el `upsertJobScheduler` en try/catch — si falla, se loguea un warning real y el resto del backend sigue arrancando normalmente (el barrido de expiración de corredores ya está documentado como mantenimiento de baja precisión, tolerable perderlo temporalmente; que TODO el backend deje de arrancar por eso no lo es). Esto NO arregla la cuota de Upstash en sí — sigue siendo 100% una acción real de cuenta/plan que el fundador debe resolver (subir de plan o esperar el reset del período) — solo evita que un problema de Redis se propague a una caída total e indefinida del backend. Verificado real: `tsc --noEmit`, `eslint` y `nest build` limpios. Pendiente crítico y urgente: el fundador debe resolver la cuota de Upstash YA — mientras siga en 500000/500000, el barrido de expiración de corredores simplemente no corre (con este fix, silenciosamente, sin tumbar nada más), pero cualquier otra función real que dependa de Redis (Modo de manejo, recordatorios con hora fija, corredor de emergencia, rate limiting de sockets) SIGUE fallando igual que en la decisión (28) — este fix solo evita el peor escenario (caída total), no reemplaza arreglar la cuota real. | `backend/src/modules/emergency-corridor/corridor-expiry-sweep.processor.ts` |
| 2026-09-04 (38) | Causa raíz REAL del "Sin acceso... (Internal server error)" que vio el fundador al entrar por primera vez a `/admin` con su cuenta nueva (`compramosfacil.com@gmail.com`) — investigado en el código, no adivinado. `UserAwareThrottlerGuard` (`backend/src/common/guards/user-aware-throttler.guard.ts`) es un guard GLOBAL (`APP_GUARD`, `RateLimitModule`) que corre en TODA petición HTTP del backend, ANTES que `AdminGuard`/cualquier guard de controller — y su storage es Redis (`ThrottlerStorageRedisService`). Con la cuota de Upstash todavía agotada (mismo problema real de las decisiones (28) y (37), sin resolver todavía por el fundador), cada intento de contar la petición contra Redis lanzaba una excepción real sin capturar dentro de `canActivate()`, que NestJS convertía en un 500 genérico ("Internal server error") — ANTES de llegar siquiera a `AdminGuard`, así que el mensaje "Sin acceso — esta cuenta no es la cuenta de administrador configurada" que se veía en pantalla era engañoso: no tenía nada que ver con si la cuenta era o no la correcta, el panel de admin `useEmergencyAdmin` solo mostraba ese texto genérico porque la llamada real falló con algo distinto de 403. Conclusión más grave todavía: esto significa que, mientras la cuota de Upstash siguiera agotada, **NINGÚN endpoint de todo el backend respondía nunca** — ni login, ni lectura, nada — sin importar el fix de la decisión (37) (ese solo evitaba que el proceso se cayera por completo al arrancar; una vez arriba, cada petición individual seguía muriendo igual en este guard). Fix real aplicado: `canActivate()` sobreescrito con try/catch alrededor de `super.canActivate()` — "fail open" real: si Redis falla, se deja pasar la petición sin aplicar el límite (con un warning real en los logs), en vez de bloquear el 100% del tráfico por un límite que ni se puede consultar. Cuidado real tomado a propósito para no romper el caso normal: se distingue por `instanceof HttpException` — cuando alguien SÍ se pasa del límite de verdad, `ThrottlerException` (429, un `HttpException` real) se re-lanza tal cual, nunca se convierte en "dejar pasar"; solo fallas de infraestructura (errores de Redis, que no son `HttpException`) activan el fail-open. Trade-off real y aceptado a propósito, explicado en el código: mientras Redis siga caído, los límites más estrictos de endpoints costosos (proxy de Google Maps, `SimulationController`) tampoco se aplican — riesgo real pero muchísimo menor que tener el 100% de la API caída, que era la situación real hasta este fix. Verificado real: `tsc --noEmit`, `eslint` (limpio, un warning preexistente de `no-explicit-any` en `getTracker` sin relación con este cambio) y `nest build` los tres limpios. Pendiente crítico, SIN cambiar desde la decisión (37): la cuota real de Upstash sigue agotada — este fix (junto con el de la decisión 37) hace que el backend sobreviva y responda pese a eso, pero NO reemplaza resolverla de verdad; el fundador debe subir el plan o esperar el reset del período. | `backend/src/common/guards/user-aware-throttler.guard.ts` |
| 2026-09-04 (39) | Decisión real del fundador para resolver el bloqueo crítico de Upstash (decisiones (28)/(37)/(38), cuota de 500.000 comandos/mes agotada): mover Redis a un servicio Redis dentro del MISMO proyecto de Railway donde ya corre el backend (ADR-0041), en vez de subir de plan o crear otra cuenta de Upstash. Razón dada por el fundador, evaluada como correcta: el modelo de cobro es la diferencia de fondo — Upstash factura por NÚMERO de comandos (tope fijo que se agota sin aviso, como ya pasó); Railway factura por memoria/cómputo real, sin tope de peticiones — a la escala del piloto (10-11 usuarios) el costo es de centavos/mes y crece proporcional al uso real. Sin cambio de código necesario: `RedisModule` (ADR-0008) ya aísla el proveedor detrás de una sola variable `REDIS_URL` — cambiar de Upstash a Railway es solo cambiar esa variable. Verificado contra documentación oficial de Railway (`docs.railway.com/databases/redis`) antes de dar instrucciones: al provisionar Redis, Railway genera `REDIS_URL` automáticamente (mismo nombre que ya lee el código) junto con `REDISHOST`/`REDISUSER`/`REDISPORT`/`REDISPASSWORD`; se despliega privado por defecto y se referencia desde el servicio del backend con el mecanismo de "variable references" de Railway. Pendiente real, a ejecutar por el fundador (acción de dashboard/cuenta, no de código — este asistente no tiene acceso a su cuenta de Railway): (1) agregar el servicio Redis al proyecto; (2) en las variables del backend, referenciar el `REDIS_URL` del nuevo servicio Redis en vez del de Upstash; (3) redeploy; (4) repetir la prueba de humo de ADR-0008 (`POST /system/queue/ping` → `GET /system/queue/ping/:jobId`) contra la instancia nueva; (5) decidir aparte si cancela Upstash una vez confirmado. `ADR-0008` y el comentario de `redis.module.ts` ("hoy: Upstash") se actualizan solo cuando el fundador confirme la migración real hecha y verificada, mismo estándar del resto de este log. | `docs/decisions/ADR-0008-redis-upstash.md`, `backend/src/common/redis/redis.module.ts` (pendiente tras migración real) |
| 2026-09-04 (40) | A pedido del fundador, investigación real (fuentes primarias, no adivinada) sobre el acuerdo de movilidad inteligente Corea–Colombia/Medellín que compartió (minuto30.com, bloqueado por el proxy de este entorno — reconstruido con búsqueda + fetch directo de mintransporte.gov.co y semana.com, mismas fuentes primarias del hecho). Resumen: cooperación Corea del Sur (MOLIT) – Mintransporte, con Medellín como piloto real vía CITRA (Centro Integrado de Tráfico y Transporte); cifra reportada entre USD 3,5M (foco Medellín, minuto30) y USD 5M (marco nacional, mintransporte) — no reconciliada entre fuentes, reportado honestamente; ejecución 2026-2028; monitoreo de tráfico en tiempo real, IA para semaforización, cruces peatonales inteligentes, sensores de inundación, alertas en corredores de pendiente alta. Ninguna fuente encontrada menciona datos abiertos/APIs públicas, convocatoria a sector privado, ni prioridad vial/corredores para vehículos de emergencia — sin overlap ni competencia directa con el corredor de emergencia de Copiloto. Oportunidad de articulación identificada como hipótesis (no confirmada con nadie de CITRA/Alcaldía, no se contactó a nadie): Copiloto tiene un dato que CITRA no menciona tener (posición/velocidad real de usuarios civiles en movimiento, más el corredor de emergencia ya funcionando), que complementaría el monitoreo centralizado de CITRA en vez de competir con él; punto de entrada natural sería la Secretaría de Movilidad de Medellín (Luis Echeverry, citado como vocero), no Mintransporte. Detalle completo con fuentes en el documento nuevo. | `docs/product/investigacion-corea-citra-medellin.md` |
| 2026-09-05 (41) | Ejecutada la migración de Redis decidida en (39): el fundador provisionó el servicio Redis en Railway y apuntó `REDIS_URL` del backend hacia él. Primer intento entró en el mismo loop de reinicio infinito ya documentado (decisión (37)), ahora con `TypeError: Invalid URL` — confirmado con evidencia real, dos exports de logs de Railway descargados por el fundador (`logs.1788570103751.json`, `logs.1788570622039.json`). Causa raíz encontrada leyendo el segundo log (no adivinada): al editar la variable en el dashboard, la referencia `${{Redis.REDIS_URL}}` quedó pegada al FINAL del valor viejo de Upstash en vez de reemplazarlo — el log muestra las dos connection strings concatenadas sin separador (`rediss://...@ace-perch-148578.upstash.io:6379redis://...@redis.railway.internal:6379`), por lo que `ioredis` no podía parsear la URL resultante. Corregido borrando el campo completo antes de insertar la referencia nueva. El fundador confirmó el servicio "online" en el dashboard tras el segundo intento. Sin cambio de código en ningún punto — el desacople de proveedor de ADR-0008 funcionó tal como estaba pensado. `ADR-0008` y el comentario de `redis.module.ts` actualizados para reflejar Railway como proveedor actual. **Verificado real el mismo día:** el fundador corrió la prueba de humo contra la instancia real de producción — `POST /system/queue/ping` → `{"jobId":"1"}`, `GET /system/queue/ping/1` → `{"state":"completed","result":{"pong":true,"respondedAt":"2026-09-05T01:21:42.588Z"}}`. Migración de Upstash a Railway cerrada por completo, sin nada pendiente ni simulado. | `docs/decisions/ADR-0008-redis-upstash.md`, `backend/src/common/redis/redis.module.ts` |
